# iPega Phase 1 Static Clue Index

Static clues only. Each row needs later hardware capture plus normalized fixture linkage before it can become more than a hypothesis.

## Tool Provenance

| Tool | Version | Output path |
|------|---------|-------------|
| `apktool` | 3.0.2 | `.evidence/phase1/decompile/*-apktool/` |
| `jadx` | 1.5.5 | `.evidence/phase1/decompile/*-jadx/` |
| `unzip` + `strings` + Node UTF-16 string scan | system + Node 22.12.0 | ignored APK/DLL extracts under `.evidence/phase1/decompile/` |

## Static Clues

| clue_id | Source ref | Decoded path or class | Clue type | Raw clue | Hypothesis | Planned hardware test | Verification status |
|---------|------------|-----------------------|-----------|----------|------------|-----------------------|---------------------|
| ARGUN2021-BLE-001 | `docs/refs/ARGun2021.apk` | `AndroidManifest.xml`; `com/shatalmic/unityandroidbluetoothlelib/UnityBluetoothLE.java`; `Assembly-CSharp.dll` strings | BLE service path | Manifest requires `android.hardware.bluetooth_le`; managed strings include `0000fff0-0000-1000-8000-00805f9b34fb`, `0000fff5-0000-1000-8000-00805f9b34fb`, `fff1`, `Step04SubscribeBluetoothMsgfff3`, `Step06SubscribeBluetoothMsgfff1`, `WriteCharacteristic`, `SendData`. | ARGun2021 likely uses BLE service `fff0` with candidate characteristics `fff1`, `fff3`, and `fff5` for gun traffic. | Scan physical gun BLE services; if `fff0` appears, subscribe/read `fff1` and `fff3`, then record trigger/reload/stick/X/Y/A/B changes. | unverified |
| ARGUN2021-BLE-NOTIFY-001 | `docs/refs/ARGun2021.apk` | `com/shatalmic/unityandroidbluetoothlelib/UnityBluetoothLE.java` | BLE notification bridge | `onCharacteristicChanged` and `onCharacteristicRead` send `DidUpdateValueForCharacteristic~<device>~<characteristic>~<base64>` to Unity receiver `BluetoothLEReceiver`. | BLE notifications or reads are forwarded into Unity as Base64 payloads with characteristic UUIDs. | Enable notification on candidate chars and save logcat/app-observed frames for each physical control action. | unverified |
| ARGUN2021-CONTROL-001 | `docs/refs/ARGun2021.apk` | `Assembly-CSharp.dll` strings | App-observed control decode | Strings include `ARGunPressManager`, `OnARGunPressed`, `ARGunKeyCode`, `fireDown`, `fireUp`, `reLoadBulletDown`, `reLoadBulletUp`, `ARGUN4`, and `DecryptARGun4`. | Managed Unity code likely decodes gun messages into trigger/fire and reload press/release semantics after BLE input arrives. | Capture app-observed callback logs or decompile IL on a later pass; compare decoded events to raw BLE notification bytes. | unverified |
| ARGUN2021-RUMBLE-001 | `docs/refs/ARGun2021.apk` | `Assembly-CSharp.dll` strings; `UnityBluetoothLE.java` | Candidate output/phone-haptic path | Strings include `WriteCharacteristic`, `SendData`, `SendBleData01`, `fff5`; manifest also requests `android.permission.VIBRATE`. | Android `VIBRATE` is now treated as the v1 phone-haptic path; BLE `fff5` physical gun motor output remains deferred/unverified. | Preserve `phone-vibrate-001` as v1 haptic proof and keep bounded BLE writes as deferred motor evidence only. | phone_haptic_confirmed_motor_deferred |
| ARCHER-BLE-001 | `docs/refs/AR Cher_20200905_Apkpure.xapk` | `AndroidManifest.xml`; `Assembly-CSharp.dll` strings | BLE service path | XAPK/APK manifest requests `BLUETOOTH`, `BLUETOOTH_ADMIN`, and optional BLE; managed strings include `fff0`, `0000fff3-0000-1000-8000-00805f9b34fb`, `0000fff1-0000-1000-8000-00805f9b34fb`, `0000fff5-0000-1000-8000-00805f9b34fb`, `Step03ConnectBluetooth`, `Step04SubscribeBluetoothMsgfff3`, `Step06SubscribeBluetoothMsgfff1`. | ARCher likely connects to BLE service `fff0`, subscribes `fff3`, checks `fff1`, and may write `fff5`. | BLE scan and subscribe `fff3`/`fff1`; press each physical control and compare notification payloads. | unverified |
| ARCHER-BT-001 | `docs/refs/AR Cher_20200905_Apkpure.xapk` | `com/superchips/unityandroidbluetoothlib/SCUnityBluetooth.java` | Bluetooth Classic RFCOMM path | Static `mUUID` is `00001101-0000-1000-8000-00805F9B34FB`; code tries `createInsecureRfcommSocket(1)`, then falls back to `createRfcommSocketToServiceRecord(mUUID)`. | ARCher can also use Classic SPP/RFCOMM channel 1 against a paired gun/device. | Pair gun in Android settings, attempt Classic connection to channel 1/SPP UUID, and record whether bytes arrive during trigger/reload/stick/X/Y/A/B actions. | unverified |
| ARCHER-INPUT-001 | `docs/refs/AR Cher_20200905_Apkpure.xapk` | `SCUnityBluetooth.IOThread`; `UnityBluetoothLE.java` | Input frame bridge | Classic `IOThread` reads up to 5160 bytes and sends Base64 to Unity `BluetoothReceiver.OnBluetoothData`; BLE callback sends Base64 via `BluetoothLEReceiver`. | App can observe either Classic stream bytes or BLE characteristic payloads; normalized fixtures must preserve raw ref and clue id. | Capture both BLE and Classic candidates for the same trigger/reload actions, then decide which path changes with hardware input. | unverified |
| ARCHER-RUMBLE-001 | `docs/refs/AR Cher_20200905_Apkpure.xapk` | `SCUnityBluetooth.androidBluetoothSendData`; `UnityBluetoothLE.androidWriteCharacteristic`; `Assembly-CSharp.dll` strings | Deferred gun motor candidate | Classic path exposes `androidBluetoothSendData(byte[])`; BLE path exposes `WriteCharacteristic`; strings include `SendBleData01`, `fff5`, `ARGun4`, `ARGunPro`. | ARCher may send command bytes over Classic stream or BLE `fff5` for gun output/rumble or mode control. | Keep as deferred/v2 motor-research clue; do not block v1. | deferred |
| WORLDAR-BLE-001 | `docs/refs/WorldsAR_14.0_apkcombo.com.xapk` | `AndroidManifest.xml`; `Assembly-CSharp.dll` strings | BLE service path | Manifest requires BLE; managed strings match ARCher: `fff0`, `0000fff3-0000-1000-8000-00805f9b34fb`, `0000fff1-0000-1000-8000-00805f9b34fb`, `0000fff5-0000-1000-8000-00805f9b34fb`, `Step04SubscribeBluetoothMsgfff3`, `Step06SubscribeBluetoothMsgfff1`. | WorldsAR likely shares ARCher's BLE protocol shape and is a cross-check for `fff0`/`fff1`/`fff3`/`fff5`. | Repeat BLE scan/subscription tests and compare event bytes against ARCher hypotheses. | unverified |
| WORLDAR-BT-001 | `docs/refs/WorldsAR_14.0_apkcombo.com.xapk` | `com/superchips/unityandroidbluetoothlib/SCUnityBluetooth.java` | Bluetooth Classic RFCOMM path | Same SPP UUID `00001101-0000-1000-8000-00805F9B34FB`, channel-1 `createInsecureRfcommSocket`, and Base64 Unity bridge as ARCher. | WorldsAR may support Classic SPP fallback in addition to BLE. | Pair gun and run Classic channel 1/SPP test while recording input action bytes. | unverified |
| WORLDAR-RUMBLE-001 | `docs/refs/WorldsAR_14.0_apkcombo.com.xapk` | `Assembly-CSharp.dll` strings; `UnityBluetoothLE.androidWriteCharacteristic`; `SCUnityBluetooth.androidBluetoothSendData` | Deferred gun motor candidate | Strings include `WriteCharacteristic`, `sendData`, `fff5`, `ARGun4`; Java bridge supports BLE characteristic writes and Classic stream writes. | Shared write path can test whether `fff5` or Classic stream commands activate the physical vibration motor. | Keep as deferred/v2 motor-research clue; do not block v1. | deferred |
| COMMON-GAMEPAD-001 | `docs/refs/ARGun2021.apk`, `docs/refs/AR Cher_20200905_Apkpure.xapk`, `docs/refs/WorldsAR_14.0_apkcombo.com.xapk` | `com/unity3d/player/UnityPlayerActivity.java`; `UnityPlayer.java`; managed strings | Android input fallback | Unity player forwards `KeyEvent` and `MotionEvent`; managed strings include `Joystick`, `GetButtonDown`, `GetButtonUp`, `GetMouseButtonDown`, `fireDown`, `fireUp`, and reload names. | Standard Android controller events may also work, but Bluetooth clues mean diagnostic must test InputDevice and raw Bluetooth paths. | Run Android diagnostic `InputDevice`, `KeyEvent`, and `MotionEvent` checks before assuming proprietary protocol is required. | unverified |

## Linkage Rules

- Hardware plans must cite one `clue_id` in each capture manifest row.
- Normalized fixture rows must include `clue_id` and `capture_id`.
- Keep status `unverified` until static clue, hardware capture, and normalized fixture all exist.
- `ARGun Library_1.0.1_apkcombo.com.apk` yielded no Bluetooth manifest permission and no stronger protocol clue during this pass; keep it secondary.
