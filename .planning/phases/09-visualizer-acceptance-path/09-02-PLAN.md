---
phase: 09-visualizer-acceptance-path
plan: "02"
type: execute
wave: 1
depends_on: ["09-01"]
files_modified:
  - desktop-companion/build.gradle.kts
  - desktop-companion/src/main/kotlin/com/btgun/desktop/Main.kt
  - desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt
  - desktop-companion/src/main/kotlin/com/btgun/desktop/ui/VisualizerWindow.kt
  - desktop-companion/src/test/kotlin/com/btgun/desktop/ui/PairingWindowTest.kt
  - desktop-companion/src/test/kotlin/com/btgun/desktop/ui/VisualizerWindowTest.kt
autonomous: true
requirements: [VIS-01, VIS-04]
must_haves:
  truths:
    - "D-05 visualizer is a separate Swing `VisualizerWindow`, not a PairingWindow takeover."
    - "D-06 visualizer auto-opens on authenticated Android control session."
    - "D-07 PairingWindow exposes visible `Open visualizer` manual reopen action."
    - "D-08 visualizer remains open across disconnects and shows stale/disconnected state."
  artifacts:
    - path: desktop-companion/src/main/kotlin/com/btgun/desktop/ui/VisualizerWindow.kt
      provides: "separate Swing visualizer shell with lifecycle-safe empty/disconnected states"
      exports: [VisualizerWindow]
    - path: desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt
      provides: "manual visualizer reopen action and authenticated-session open seam"
      contains: "Open visualizer"
    - path: desktop-companion/src/main/kotlin/com/btgun/desktop/Main.kt
      provides: "desktop launch wiring for shared ControlServer, event hub, PairingWindow, and VisualizerWindow factory"
  key_links:
    - from: desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt
      to: desktop-companion/src/main/kotlin/com/btgun/desktop/ui/VisualizerWindow.kt
      via: "open/reopen callback"
      pattern: "Open visualizer"
    - from: desktop-companion/src/main/kotlin/com/btgun/desktop/Main.kt
      to: desktop-companion/src/main/kotlin/com/btgun/desktop/ui/DesktopUiEventHub.kt
      via: "shared dependency wiring"
      pattern: "DesktopUiEventHub"
---

<objective>
Add the user-visible opening path for the visualizer shell.

Purpose: make VIS-01 real as soon as Android authenticates while keeping PairingWindow focused on pairing/control state.
Output: separate Swing `VisualizerWindow`, manual reopen button, auto-open seam, and tests for lifecycle behavior.
</objective>

## Phase Goal

**As a** BT Gun developer/debugger, **I want to** prove the full MVP path in a simple joystick visualizer, **so that** I can validate controls, aim, recenter, latency, packet loss, and phone haptics before commercial game support.

<execution_context>
@/Users/lucas.rancez/.codex/gsd-core/workflows/execute-plan.md
@/Users/lucas.rancez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-visualizer-acceptance-path/09-CONTEXT.md
@.planning/phases/09-visualizer-acceptance-path/09-PATTERNS.md
@.planning/phases/09-visualizer-acceptance-path/09-UI-SPEC.md
@.planning/phases/09-visualizer-acceptance-path/09-VALIDATION.md
@desktop-companion/src/main/kotlin/com/btgun/desktop/Main.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/ui/DesktopUiEventHub.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/ui/VisualizerModel.kt
@desktop-companion/src/test/kotlin/com/btgun/desktop/ui/PairingWindowTest.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create VisualizerWindow shell and UI helper tests</name>
  <files>desktop-companion/build.gradle.kts, desktop-companion/src/main/kotlin/com/btgun/desktop/ui/VisualizerWindow.kt, desktop-companion/src/test/kotlin/com/btgun/desktop/ui/VisualizerWindowTest.kt</files>
  <read_first>
    @desktop-companion/build.gradle.kts
    @desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt
    @desktop-companion/src/main/kotlin/com/btgun/desktop/ui/VisualizerModel.kt
    @desktop-companion/src/test/kotlin/com/btgun/desktop/ui/PairingWindowTest.kt
    @.planning/phases/09-visualizer-acceptance-path/09-UI-SPEC.md
  </read_first>
  <behavior>
    - RED: `VisualizerWindowTestKt` fails before the shell and helper labels exist.
    - Shell exposes UI-SPEC copy: `BT Gun Visualizer`, `Waiting for authenticated session`, `Phase 9 checks pending`, `Acceptance checklist`, and `Live gamepad`.
    - Window close hides/disposes only the visualizer UI and never stops `ControlServer`, backend runtimes, packet stream, or pairing state.
  </behavior>
  <action>Register `com.btgun.desktop.ui.VisualizerWindowTestKt` in the desktop Gradle executable test list. Create `VisualizerWindow` as a separate Swing `JFrame` or equivalent class following PairingWindow EDT patterns. Add helper functions for empty/disconnected summary text, lifecycle state mapping, and haptic button label without launching Swing in tests. Use UI-SPEC spacing, typography, background colors, and copy constraints where helpers expose labels. Keep gamepad panels as placeholders bound to `VisualizerModel`; do not add profile editing or raw logs.</action>
  <acceptance_criteria>
    - Tests prove required window title, empty state, top summary, and section labels exactly match UI-SPEC.
    - Tests prove `VisualizerWindow` exposes a `show`/`open` style method and a close behavior that does not call transport stop methods.
    - Source contains no forbidden labels from UI-SPEC: `Submit`, `OK`, `Cancel`, `Save`, `Click Here`, desktop profile editing labels, or generated evidence bundle pass labels.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p desktop-companion test --tests '*VisualizerWindow*' --tests '*VisualizerModel*' --no-daemon --console=plain</automated>
  </verify>
  <done>`VisualizerWindow` shell compiles, helper tests pass, and D-05/D-08 are represented in code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add manual reopen action to PairingWindow</name>
  <files>desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt, desktop-companion/src/test/kotlin/com/btgun/desktop/ui/PairingWindowTest.kt</files>
  <read_first>
    @desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt
    @desktop-companion/src/test/kotlin/com/btgun/desktop/ui/PairingWindowTest.kt
    @.planning/phases/09-visualizer-acceptance-path/09-PATTERNS.md
    @.planning/phases/09-visualizer-acceptance-path/09-UI-SPEC.md
  </read_first>
  <behavior>
    - RED: PairingWindow tests fail until the `Open visualizer` action exists and does not replace existing pairing/haptic controls.
    - `Open visualizer` is visible regardless of current session state and calls an injected visualizer opener.
    - PairingWindow remains a pairing/status window and does not become the main visualizer per D-05/D-07.
  </behavior>
  <action>Add a constructor seam to `PairingWindow` for opening the visualizer, defaulting to a no-op or injected factory suitable for tests. Add a visible `Open visualizer` button using the exact UI-SPEC label. Keep existing QR/manual pairing, diagnostics, profile metadata, and haptic smoke behavior intact. Tests must guard against desktop profile editing labels and against moving live visualizer-only labels into PairingWindow.</action>
  <acceptance_criteria>
    - Tests prove clicking the helper/action invokes the injected opener exactly once.
    - Tests prove existing haptic smoke helper behavior and read-only Android profile diagnostics still pass.
    - PairingWindow source does not render the full guided checklist or gamepad crosshair panel.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p desktop-companion test --tests '*PairingWindow*' --tests '*VisualizerWindow*' --no-daemon --console=plain</automated>
  </verify>
  <done>PairingWindow exposes a manual reopen action while preserving existing pairing/status behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire auto-open on authenticated control session</name>
  <files>desktop-companion/src/main/kotlin/com/btgun/desktop/Main.kt, desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt, desktop-companion/src/main/kotlin/com/btgun/desktop/ui/VisualizerWindow.kt, desktop-companion/src/test/kotlin/com/btgun/desktop/ui/VisualizerWindowTest.kt</files>
  <read_first>
    @desktop-companion/src/main/kotlin/com/btgun/desktop/Main.kt
    @desktop-companion/src/main/kotlin/com/btgun/desktop/control/ControlServer.kt
    @desktop-companion/src/main/kotlin/com/btgun/desktop/ui/DesktopUiEventHub.kt
    @desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt
    @desktop-companion/src/main/kotlin/com/btgun/desktop/ui/VisualizerWindow.kt
  </read_first>
  <behavior>
    - RED: UI seam test fails until authenticated `ControlServerSessionState` triggers a one-window visualizer open.
    - Authenticated session auto-opens the visualizer per D-06; repeated authenticated events reuse/show the existing window rather than spawning duplicates.
    - Disconnect/degraded events keep the window open and update disconnected/stale state per D-08.
  </behavior>
  <action>Wire `Main.kt` so the shared `ControlServer` and `DesktopUiEventHub` feed both PairingWindow and the visualizer opener. Use `SwingUtilities.invokeLater` for UI mutations. Add a small coordinator/factory only if it keeps `main()` thin and avoids networking ownership in Swing. Tests can cover helper classes or exposed factory functions without launching a real frame.</action>
  <acceptance_criteria>
    - Tests prove one authenticated session opens the visualizer and repeated auth does not duplicate it.
    - Tests prove disconnect state does not clear checklist progress or last model context.
    - `Main.kt` still constructs identity, registry, control server, backend runtimes, event hub, PairingWindow, and visualizer factory once.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p desktop-companion test --tests '*VisualizerWindow*' --tests '*PairingWindow*' --tests '*DesktopUiEventHub*' --no-daemon --console=plain</automated>
  </verify>
  <done>Visualizer can be opened manually and auto-opens on authenticated Android session without disrupting PairingWindow or backend wiring.</done>
</task>

</tasks>

## Artifacts This Phase Produces

| Artifact | Kind | Producer |
|----------|------|----------|
| `VisualizerWindow` | desktop Swing class | Task 1 |
| `Open visualizer` | PairingWindow action label/button | Task 2 |
| authenticated auto-open coordinator/factory | desktop launch seam | Task 3 |
| `VisualizerWindowTestKt` | executable test main | Task 1 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated session callback -> visualizer open | Trusted session state triggers UI side effect. |
| PairingWindow action -> visualizer shell | Local user action opens UI without altering transport/session trust. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Spoofing | auto-open session gate | mitigate | Open only on `ControlServerSessionState.AUTHENTICATED`, not QR/manual pairing material alone. |
| T-09-06 | Denial of Service | visualizer window lifecycle | mitigate | Reuse an existing window on repeated auth events; avoid duplicate frame flood. |
| T-09-07 | Elevation of Privilege | PairingWindow reopen button | mitigate | Button opens UI only; it does not send haptic commands, alter profiles, or start sessions. |
| T-09-08 | Information Disclosure | visualizer shell labels | mitigate | Empty/disconnected UI shows no pairing secrets, stream keys, device identifiers, raw logs, or screenshots. |
| T-09-SC | Tampering | package installs | accept | No new external packages or registries; Swing platform APIs only. |
</threat_model>

<verification>
Run focused desktop UI opening tests:
<automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p desktop-companion test --tests '*VisualizerWindow*' --tests '*PairingWindow*' --tests '*DesktopUiEventHub*' --no-daemon --console=plain</automated>
</verification>

<success_criteria>
- VIS-01 is satisfied at the shell/opening level.
- D-05 through D-08 are implemented and tested.
- Visualizer open/reopen behavior adds no new transport ownership, profile editing, replay tooling, or generated evidence bundle flow.
</success_criteria>

<output>
Create `.planning/phases/09-visualizer-acceptance-path/09-02-SUMMARY.md` when done.
</output>
