---
phase: 09-visualizer-acceptance-path
plan: "04"
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - android-host/app/build.gradle.kts
  - android-host/app/src/main/java/com/btgun/host/HostSessionService.kt
  - android-host/app/src/main/java/com/btgun/host/session/DesktopControlClient.kt
  - android-host/app/src/main/java/com/btgun/host/session/VisualizerStatus.kt
  - android-host/app/src/test/java/com/btgun/host/HostSessionServiceLivenessTest.kt
  - android-host/app/src/test/java/com/btgun/host/session/DesktopControlClientTest.kt
  - android-host/app/src/test/java/com/btgun/host/session/VisualizerStatusTest.kt
autonomous: true
requirements: [VIS-03, VIS-04, PERF-01]
must_haves:
  truths:
    - "Android emits sanitized visualizer status over an authenticated control diagnostics path for recenter and aim-zero display."
    - "D-11 recenter/aim-zero data comes from Android-owned reload-hold and aim baseline state."
    - "D-12 raw debug on/off state is included without exposing raw logs or secrets."
    - "PERF-01 can receive Android elapsed-time status samples needed for offset-estimated visualizer latency."
  artifacts:
    - path: android-host/app/src/main/java/com/btgun/host/session/VisualizerStatus.kt
      provides: "Android visualizer status body model and JSON helpers"
      exports: [VisualizerStatus]
    - path: android-host/app/src/main/java/com/btgun/host/session/DesktopControlClient.kt
      provides: "authenticated diagnostics sender for nested `visualizerStatus`"
      contains: "visualizerStatus"
    - path: android-host/app/src/main/java/com/btgun/host/HostSessionService.kt
      provides: "visualizer status publication from recenter, aim baseline, raw-debug, and clock source state"
  key_links:
    - from: android-host/app/src/main/java/com/btgun/host/HostSessionService.kt
      to: android-host/app/src/main/java/com/btgun/host/session/DesktopControlClient.kt
      via: "trusted diagnostics envelope"
      pattern: "visualizerStatus"
    - from: android-host/app/src/main/java/com/btgun/host/recenter/ReloadHoldRecenter.kt
      to: android-host/app/src/main/java/com/btgun/host/session/VisualizerStatus.kt
      via: "last recenter and aim-zero state"
      pattern: "lastRecenter"
---

<objective>
Add the Android-origin status needed for recenter, aim-zero, raw-debug, and latency clock context.

Purpose: VIS-03 cannot be implemented from current UDP frames alone; Android owns reload-hold recenter and aim baseline state.
Output: sanitized Android `visualizerStatus` diagnostics body and tests proving no secrets or raw packet material are emitted.
</objective>

## Phase Goal

**As a** BT Gun developer/debugger, **I want to** prove the full MVP path in a simple joystick visualizer, **so that** I can validate controls, aim, recenter, latency, packet loss, and phone haptics before commercial game support.

<execution_context>
@/Users/lucas.rancez/.codex/gsd-core/workflows/execute-plan.md
@/Users/lucas.rancez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-visualizer-acceptance-path/09-CONTEXT.md
@.planning/phases/09-visualizer-acceptance-path/09-RESEARCH.md
@.planning/phases/09-visualizer-acceptance-path/09-PATTERNS.md
@.planning/phases/09-visualizer-acceptance-path/09-VALIDATION.md
@android-host/app/build.gradle.kts
@android-host/app/src/main/java/com/btgun/host/HostSessionService.kt
@android-host/app/src/main/java/com/btgun/host/session/DesktopControlClient.kt
@android-host/app/src/main/java/com/btgun/host/session/ControlEnvelope.kt
@android-host/app/src/main/java/com/btgun/host/recenter/ReloadHoldRecenter.kt
@android-host/app/src/main/java/com/btgun/host/profile/ProfileModels.kt
@android-host/app/src/test/java/com/btgun/host/session/DesktopControlClientTest.kt
@android-host/app/src/test/java/com/btgun/host/HostSessionServiceLivenessTest.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define sanitized Android VisualizerStatus model</name>
  <files>android-host/app/build.gradle.kts, android-host/app/src/main/java/com/btgun/host/session/VisualizerStatus.kt, android-host/app/src/test/java/com/btgun/host/session/VisualizerStatusTest.kt</files>
  <read_first>
    @android-host/app/build.gradle.kts
    @android-host/app/src/main/java/com/btgun/host/session/ControlEnvelope.kt
    @android-host/app/src/main/java/com/btgun/host/session/DesktopControlClient.kt
    @android-host/app/src/main/java/com/btgun/host/recenter/ReloadHoldRecenter.kt
    @.planning/phases/09-visualizer-acceptance-path/09-PATTERNS.md
  </read_first>
  <behavior>
    - RED: `VisualizerStatusTestKt` fails before `VisualizerStatus` exists.
    - Status JSON contains only non-secret fields: raw debug enabled, aim-zero state, recenter state/event age source, Android elapsed nanos, optional capture/send timing support, and short status labels.
    - Status rejects or omits pairing material, stream secrets, HMAC keys, private keys, raw packet bytes, full device ids, screenshots, and raw logs.
  </behavior>
  <action>Register `com.btgun.host.session.VisualizerStatusTestKt` in the Android Gradle executable test list. Create `VisualizerStatus` in the Android session package following `ControlEnvelope` and profile metadata JSON helper patterns. Use `diagnostics` message body nesting key `visualizerStatus` rather than adding a new control message type. Include typed states such as `aimZeroState`, `recenterState`, `lastRecenterElapsedNanos`, `rawDebugEnabled`, `androidElapsedNanos`, and optional `statusSequence` if useful for ordering. Keep field names mirrored for the desktop parser in Plan 09-05.</action>
  <acceptance_criteria>
    - Tests prove valid JSON body has stable field names and only expected fields.
    - Tests prove negative/blank invalid fields are rejected or normalized to unavailable.
    - Tests prove forbidden secret/device/log/screenshot field names are absent from serialized output.
    - No new control message enum value is required; body uses existing authenticated diagnostics type.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=/Users/lucas.rancez/Library/Android/sdk GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p android-host testDebugUnitTest --tests '*VisualizerStatus*' --no-daemon --console=plain</automated>
  </verify>
  <done>Android visualizer status model serializes sanitized recenter/aim/raw-debug/clock context and test coverage is registered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Send visualizer status diagnostics through DesktopControlClient</name>
  <files>android-host/app/src/main/java/com/btgun/host/session/DesktopControlClient.kt, android-host/app/src/main/java/com/btgun/host/session/VisualizerStatus.kt, android-host/app/src/test/java/com/btgun/host/session/DesktopControlClientTest.kt, android-host/app/src/test/java/com/btgun/host/session/VisualizerStatusTest.kt</files>
  <read_first>
    @android-host/app/src/main/java/com/btgun/host/session/DesktopControlClient.kt
    @android-host/app/src/main/java/com/btgun/host/session/ControlEnvelope.kt
    @android-host/app/src/main/java/com/btgun/host/session/VisualizerStatus.kt
    @android-host/app/src/test/java/com/btgun/host/session/DesktopControlClientTest.kt
  </read_first>
  <behavior>
    - RED: DesktopControlClient tests fail until a trusted-session diagnostics envelope can carry nested `visualizerStatus`.
    - Status sends only after a trusted control session exists and uses Android elapsed realtime for `sentElapsedNanos`.
    - Invalid or unavailable status does not break heartbeat, profile metadata, input stream config, or haptic command handling.
  </behavior>
  <action>Add a `sendVisualizerStatus` or equivalent method to `DesktopControlClient` that emits a `ControlMessageType.DIAGNOSTICS` envelope with a `visualizerStatus` object. Follow existing `profileMetadataEnvelope` body construction and message id style, but do not include profile contents beyond raw-debug status already allowed. Ensure the method fails closed or no-ops when no active trusted session is available, matching existing outbound metadata patterns.</action>
  <acceptance_criteria>
    - Tests prove outbound envelope type is `diagnostics` and body contains `visualizerStatus`.
    - Tests prove status is not sent before trusted control state.
    - Tests prove haptic command handling still returns `haptic_result` and profile metadata tests remain green.
    - Tests prove no secret-like values appear in serialized diagnostics body.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=/Users/lucas.rancez/Library/Android/sdk GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p android-host testDebugUnitTest --tests '*DesktopControlClient*' --tests '*VisualizerStatus*' --no-daemon --console=plain</automated>
  </verify>
  <done>Android can send sanitized visualizer status over the existing authenticated control diagnostics channel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Publish status from HostSessionService recenter and raw-debug state</name>
  <files>android-host/app/src/main/java/com/btgun/host/HostSessionService.kt, android-host/app/src/main/java/com/btgun/host/session/DesktopControlClient.kt, android-host/app/src/main/java/com/btgun/host/session/VisualizerStatus.kt, android-host/app/src/test/java/com/btgun/host/HostSessionServiceLivenessTest.kt, android-host/app/src/test/java/com/btgun/host/session/DesktopControlClientTest.kt</files>
  <read_first>
    @android-host/app/src/main/java/com/btgun/host/HostSessionService.kt
    @android-host/app/src/main/java/com/btgun/host/recenter/ReloadHoldRecenter.kt
    @android-host/app/src/main/java/com/btgun/host/profile/ProfileMapper.kt
    @android-host/app/src/main/java/com/btgun/host/profile/ProfileModels.kt
    @android-host/app/src/test/java/com/btgun/host/HostSessionServiceLivenessTest.kt
  </read_first>
  <behavior>
    - RED: HostSessionService tests fail until recenter event, aim baseline, raw-debug flag, and Android elapsed clock source produce visualizer status.
    - Holding reload and emitting recenter updates `lastRecenterElapsedNanos` and `aimZeroState` without swallowing normal reload down/up.
    - Raw debug status follows Android profile/session setting and remains off by default.
  </behavior>
  <action>Update `HostSessionService` to build and send `VisualizerStatus` through `DesktopControlClient` when trusted control exists and meaningful state changes occur: session ready/profile metadata send, raw-debug flag changes, recenter status, aim baseline change, and periodic/heartbeat-friendly status if already supported. Do not add a new foreground service action. Do not include raw provider/yaw/pitch/roll values in this status unless they are already allowed by raw debug and sanitized; those detailed values remain in UDP/raw-debug visualizer drawer when Android raw debug is enabled.</action>
  <acceptance_criteria>
    - Tests prove recenter emits a visualizer status with recentered/ready aim-zero state.
    - Tests prove rawDebugEnabled default is false and follows active Android profile/session state.
    - Tests prove status publication is no-op without trusted desktop connection.
    - Tests prove reload down/up events still fan out normally around recenter.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=/Users/lucas.rancez/Library/Android/sdk GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p android-host testDebugUnitTest --tests '*HostSessionService*' --tests '*DesktopControlClient*' --tests '*VisualizerStatus*' --no-daemon --console=plain</automated>
  </verify>
  <done>Android publishes authenticated, sanitized visualizer status needed by desktop recenter/aim-zero display.</done>
</task>

</tasks>

## Artifacts This Phase Produces

| Artifact | Kind | Producer |
|----------|------|----------|
| `android-host/.../session/VisualizerStatus.kt` | Android model file | Task 1 |
| `visualizerStatus` | diagnostics JSON body key | Task 1 |
| `sendVisualizerStatus` or equivalent | Android control client method | Task 2 |
| `aimZeroState`, `recenterState`, `lastRecenterElapsedNanos`, `rawDebugEnabled`, `androidElapsedNanos` | diagnostics fields | Tasks 1-3 |
| `VisualizerStatusTestKt` | Android executable test main | Task 1 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Android service state -> control diagnostics | Local Android state becomes authenticated desktop-visible JSON. |
| Recenter/raw-debug state -> checklist display | Android-owned calibration state influences observed visualizer rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Information Disclosure | `VisualizerStatus` JSON | mitigate | Whitelist fields and test absence of secrets, raw logs, screenshots, and full device ids. |
| T-09-14 | Spoofing | status diagnostics | mitigate | Send only over existing authenticated control diagnostics; no unauthenticated local broadcast. |
| T-09-15 | Tampering | recenter status | mitigate | Derive from `ReloadHoldRecenter` and `HostSessionService` aim baseline, not desktop-supplied fields. |
| T-09-16 | Denial of Service | status sends | mitigate | Send on meaningful state changes or bounded cadence; do not block foreground service. |
| T-09-SC | Tampering | package installs | accept | No new dependencies; Android platform and existing kotlinx serialization only. |
</threat_model>

<verification>
Run Android focused visualizer status tests:
<automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=/Users/lucas.rancez/Library/Android/sdk GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p android-host testDebugUnitTest --tests '*VisualizerStatus*' --tests '*DesktopControlClient*' --tests '*HostSessionService*' --no-daemon --console=plain</automated>
</verification>

<success_criteria>
- Android status diagnostics cover VIS-03/VIS-04 and support PERF-01 offset estimation without extending fixed UDP frame layout.
- D-11 and D-12 source-of-truth state comes from Android host service.
- No raw packet bytes, pairing material, stream secrets, device ids, screenshots, or physical gun motor rumble behavior is added.
</success_criteria>

<output>
Create `.planning/phases/09-visualizer-acceptance-path/09-04-SUMMARY.md` when done.
</output>
