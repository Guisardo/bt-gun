---
phase: "01-hardware-and-protocol-discovery"
plan: "04"
type: execute
wave: 4
depends_on:
  - "01-03"
files_modified:
  - "fixtures/ipega/normalized/README.md"
  - "fixtures/ipega/normalized/handshake.jsonl"
  - "fixtures/ipega/normalized/trigger.jsonl"
  - "fixtures/ipega/normalized/reload.jsonl"
  - "fixtures/ipega/normalized/joystick.jsonl"
  - "fixtures/ipega/normalized/buttons-xyab.jsonl"
  - "docs/evidence/manifests/phase1-captures.jsonl"
  - "tools/phase1/validate-fixtures.mjs"
autonomous: true
requirements:
  - DISC-04
  - DISC-05
  - DISC-07
must_haves:
  truths:
    - "Developer can map every captured physical control to normalized down/up or axis semantics."
    - "Developer can replay handshake and control fixtures without the physical gun."
    - "No control finding is marked verified without static clue, hardware capture, and normalized fixture linkage."
  artifacts:
    - path: "fixtures/ipega/normalized/README.md"
      provides: "Normalized fixture schema and control coverage rules"
      contains: "btgun.ipega.normalized"
    - path: "fixtures/ipega/normalized/handshake.jsonl"
      provides: "Bluetooth connection and handshake observation fixture"
      contains: "handshake"
    - path: "fixtures/ipega/normalized/trigger.jsonl"
      provides: "Trigger down/up normalized fixture"
      contains: "trigger"
    - path: "fixtures/ipega/normalized/reload.jsonl"
      provides: "Reload down/up normalized fixture"
      contains: "reload"
    - path: "fixtures/ipega/normalized/joystick.jsonl"
      provides: "Joystick axis normalized fixture"
      contains: "joystick"
    - path: "fixtures/ipega/normalized/buttons-xyab.jsonl"
      provides: "X/Y/A/B down/up normalized fixture"
      contains: "control"
    - path: "docs/evidence/manifests/phase1-captures.jsonl"
      provides: "Capture manifest refs linked to normalized control fixtures"
      contains: "normalized_fixture"
    - path: "tools/phase1/validate-fixtures.mjs"
      provides: "Control/handshake fixture coverage validation"
      exports: ["--quick", "--full"]
  key_links:
    - from: "fixtures/ipega/normalized/*.jsonl"
      to: "docs/evidence/manifests/phase1-captures.jsonl"
      via: "capture_id and raw_ref"
      pattern: "capture_id"
    - from: "fixtures/ipega/normalized/*.jsonl"
      to: "docs/protocol/ipega-phase1-clues.md"
      via: "clue_id"
      pattern: "clue_id"
---

<objective>
Create normalized handshake/control fixtures for DISC-04, DISC-05, and DISC-07.

Purpose: Convert Plan 03 hardware captures into replayable JSONL per D-13 and D-14, cover trigger, reload, joystick, X/Y/A/B, and handshake observations per D-15, and preserve the D-06/D-07/D-08 proof chain for each mapped control. Haptic proof is intentionally isolated in Plan 05.

Output: Normalized handshake/control JSONL fixtures, updated fixture README, capture manifest links, and validator coverage for no-hardware regression.
</objective>

<execution_context>
@/Users/lucas.rancez/.codex/gsd-core/workflows/execute-plan.md
@/Users/lucas.rancez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-hardware-and-protocol-discovery/01-CONTEXT.md
@.planning/phases/01-hardware-and-protocol-discovery/01-RESEARCH.md
@.planning/phases/01-hardware-and-protocol-discovery/01-VALIDATION.md
@.planning/phases/01-hardware-and-protocol-discovery/01-PATTERNS.md
@docs/protocol/ipega-phase1-clues.md
@docs/protocol/ipega-phase1-hardware.md
@docs/evidence/manifests/phase1-captures.jsonl
@tools/phase1/validate-fixtures.mjs
</context>

## Artifacts

- `fixtures/ipega/normalized/README.md` updated with JSONL schema, control coverage table, and proof-chain rules.
- `fixtures/ipega/normalized/handshake.jsonl` for connection, pairing, BLE/Classic service, notification, or socket observations.
- `fixtures/ipega/normalized/trigger.jsonl` for trigger down/up semantics.
- `fixtures/ipega/normalized/reload.jsonl` for reload down/up semantics.
- `fixtures/ipega/normalized/joystick.jsonl` for joystick X/Y axis semantics, neutral, and observed range.
- `fixtures/ipega/normalized/buttons-xyab.jsonl` for X, Y, A, and B down/up semantics.
- `docs/evidence/manifests/phase1-captures.jsonl` updated so each control/handshake capture points to normalized fixture rows.
- `tools/phase1/validate-fixtures.mjs` updated so `--quick` parses fixtures and `--full` checks control/handshake coverage.

## Source Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Protocol/control evidence can be replayed without hardware | Covered by normalized fixtures and validator |
| REQ | DISC-04, DISC-05, DISC-07 | Covered by control fixtures, raw refs, and no-hardware validator |
| RESEARCH | JSONL fixture shape, control matrix, raw refs, static-only pitfall | Covered by tasks |
| CONTEXT | D-06, D-07, D-08, D-09, D-10, D-13, D-14, D-15 | Cited in objective and task actions |

<tasks>

<task type="auto">
  <name>Task 1: Normalize handshake and control matrix fixtures</name>
  <files>fixtures/ipega/normalized/README.md, fixtures/ipega/normalized/handshake.jsonl, fixtures/ipega/normalized/trigger.jsonl, fixtures/ipega/normalized/reload.jsonl, fixtures/ipega/normalized/joystick.jsonl, fixtures/ipega/normalized/buttons-xyab.jsonl, docs/evidence/manifests/phase1-captures.jsonl</files>
  <read_first>.planning/phases/01-hardware-and-protocol-discovery/01-CONTEXT.md, .planning/phases/01-hardware-and-protocol-discovery/01-RESEARCH.md, docs/protocol/ipega-phase1-clues.md, docs/protocol/ipega-phase1-hardware.md, docs/evidence/manifests/phase1-captures.jsonl</read_first>
  <action>Convert Plan 03 hardware captures into JSONL normalized fixtures per D-13 and D-14. Cover handshake or Bluetooth connection observations plus trigger, reload, joystick axes, X, Y, A, and B per D-15. Each event row must include schema, fixture id, seq, control or observation name, kind, phase for buttons, axis value for joystick, raw ref, capture id, and clue id. Keep unknown mappings explicit with captured-failed or unverified status tied to hardware evidence. Do not infer semantics from static clue alone per D-06 and D-07.</action>
  <verify>
    <automated>node tools/phase1/validate-fixtures.mjs --quick</automated>
  </verify>
  <done>Handshake/control fixtures parse as JSONL, cover required physical controls, and every row links to capture id, raw ref, and clue id.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce control fixture coverage</name>
  <files>tools/phase1/validate-fixtures.mjs, docs/evidence/manifests/phase1-captures.jsonl</files>
  <read_first>.planning/phases/01-hardware-and-protocol-discovery/01-VALIDATION.md, fixtures/ipega/normalized/README.md, docs/evidence/manifests/phase1-captures.jsonl</read_first>
  <action>Update validator so `--quick` parses every control fixture and `--full` requires trigger, reload, joystick, X, Y, A, B, and handshake fixture coverage. Validator must require capture id, raw ref, clue id, normalized fixture path, and parseable JSONL rows before any control finding may claim verified status per D-06, D-08, D-10, D-13, D-14, and D-15. Do not include haptic feedback completion in this plan; Plan 05 owns DISC-06.</action>
  <verify>
    <automated>node tools/phase1/validate-fixtures.mjs --full</automated>
  </verify>
  <done>Full validator passes for handshake/control coverage and still leaves haptic DISC-06 completion to Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw captures -> normalized fixtures | Hardware bytes or app-observed frames become semantic controls |
| fixtures -> downstream phases | JSONL fixtures become replay source for later Android host and transport work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering | Normalized fixture semantics | mitigate | Require clue id, capture id, raw ref, and explicit control semantics in each row. |
| T-01-11 | Repudiation | Hardware action provenance | mitigate | Capture manifest stores origin, action, expected interpretation, and normalized fixture path for each evidence item. |
| T-01-12 | Information Disclosure | Raw capture refs in fixtures | mitigate | Use `local://.evidence/phase1/...` refs and keep raw data ignored; commit only sanitized small fixtures. |
| T-01-SC | Tampering | npm, pip, cargo, Gradle, or package installs | mitigate | Plan 04 performs no installs; any new package dependency must stop for blocking human legitimacy verification before use. |
</threat_model>

<verification>
Run `node tools/phase1/validate-fixtures.mjs --full`. Review fixtures so every verified control row has static clue, hardware capture, raw ref, normalized fixture, and interpretation.
</verification>

<success_criteria>
- DISC-04 capture pointers are linked to handshake/control fixtures.
- DISC-05 normalized events cover trigger, reload, joystick axes, X, Y, A, and B with down/up or axis semantics.
- DISC-07 no-hardware replay validation works for control and handshake fixtures.
- No physical gun motor failure row is allowed to satisfy DISC-06; Plan 05 uses phone vibration proof for v1.
</success_criteria>

<output>
Create `.planning/phases/01-hardware-and-protocol-discovery/01-04-SUMMARY.md` when done.
</output>
