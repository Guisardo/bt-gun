---
phase: "05-desktop-backend-contract-and-smoke-harness"
plan: "03"
type: "tdd"
wave: 3
depends_on:
  - "05-02"
files_modified:
  - "desktop-companion/build.gradle.kts"
  - "desktop-companion/src/main/kotlin/com/btgun/desktop/backend/UdpControllerStateAdapter.kt"
  - "desktop-companion/src/test/kotlin/com/btgun/desktop/backend/UdpControllerStateAdapterTest.kt"
autonomous: true
requirements:
  - "DESK-04"
  - "DESK-08"
must_haves:
  truths:
    - "Developer can replay Phase 4 UDP snapshot and edge fixtures through the trusted receiver path before backend publish per D-02 and D-06."
    - "Accepted UDP input maps into semantic controller state without configurable profile mapping per D-02, D-03, and D-10."
  artifacts:
    - path: "desktop-companion/src/main/kotlin/com/btgun/desktop/backend/UdpControllerStateAdapter.kt"
      provides: "UdpReceivedInput to SemanticControllerState adapter"
      exports: ["UdpControllerStateAdapter"]
    - path: "desktop-companion/src/test/kotlin/com/btgun/desktop/backend/UdpControllerStateAdapterTest.kt"
      provides: "Receiver-handoff and adapter invariant tests"
  key_links:
    - from: "UdpControllerStateAdapterTest.kt"
      to: "UdpInputReceiver"
      via: "handles encoded fixture datagrams through HMAC/replay guard before adapter"
      pattern: "UdpInputReceiver"
    - from: "UdpControllerStateAdapter.kt"
      to: "SemanticControllerState.kt"
      via: "maps pressedControls/stick/rawAim/stale/sequence"
      pattern: "SemanticControllerState"
---

## Phase Goal

**As a** developer, **I want to** validate shared virtual-controller behavior on both desktop targets before using the real Android stream, **so that** Windows and macOS driver work starts from a proven backend contract.

<objective>
Add the UDP receiver handoff slice: authenticated Phase 4 input becomes semantic backend state.

Purpose: prove DESK-08 starts at the real receiver boundary, not direct semantic injection, honoring D-02, D-03, D-06, D-10, D-11, and D-12.
Output: `UdpControllerStateAdapter` plus receiver-backed tests.
</objective>

<execution_context>
@/Users/lucas.rancez/.codex/gsd-core/workflows/execute-plan.md
@/Users/lucas.rancez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-desktop-backend-contract-and-smoke-harness/05-CONTEXT.md
@.planning/phases/05-desktop-backend-contract-and-smoke-harness/05-RESEARCH.md
@.planning/phases/05-desktop-backend-contract-and-smoke-harness/05-VALIDATION.md
@.planning/phases/05-desktop-backend-contract-and-smoke-harness/05-PATTERNS.md
@.planning/phases/05-desktop-backend-contract-and-smoke-harness/05-01-SUMMARY.md
@.planning/phases/05-desktop-backend-contract-and-smoke-harness/05-02-SUMMARY.md
@.planning/phases/04-input-stream-and-haptic-transport/04-CONTEXT.md
@docs/protocol/input-stream-v1-fixtures.md
@desktop-companion/build.gradle.kts
@desktop-companion/src/main/kotlin/com/btgun/desktop/backend/SemanticControllerState.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/backend/StubVirtualControllerBackend.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/transport/UdpReceivedInput.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/transport/UdpInputReceiver.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/transport/InputReplayGuard.kt
@desktop-companion/src/test/kotlin/com/btgun/desktop/transport/DesktopUdpInputRuntimeTest.kt
</context>

## Artifacts This Phase Produces

| Artifact | Kind | Produced Here | Notes |
|----------|------|---------------|-------|
| `SemanticControllerState`, `VirtualControllerDescriptor` | contract symbols | no | Created in `05-01-PLAN.md`. |
| `BackendCapabilities`, `VirtualControllerBackend`, `StubVirtualControllerBackend` | backend symbols | no | Created in `05-02-PLAN.md`. |
| `UdpControllerStateAdapter` | object | yes | Function `toState(input: UdpReceivedInput): SemanticControllerState`. |
| UDP fixture mapping rules | behavior | yes | Snapshot maps trigger/reload/B, stick 12345/-12345, aim 0.125/-0.25; edge NaN aim maps to neutral `0.0f`. |
| Receiver-backed adapter test | test | yes | Uses `UdpInputReceiver.handleDatagram` with authenticated fixture bytes before backend publish. |
| `BackendSmokeRunner`, `JunitSmokeXml`, platform main classes, smoke Gradle tasks | smoke artifacts | no | Created in `05-04-PLAN.md`. |
| `SimulatedOutputReport`, haptic smoke path, evidence manifest | haptic/evidence artifacts | no | Created in `05-05-PLAN.md`. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED UDP receiver handoff adapter tests</name>
  <read_first>
@desktop-companion/build.gradle.kts
@desktop-companion/src/main/kotlin/com/btgun/desktop/backend/SemanticControllerState.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/backend/StubVirtualControllerBackend.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/transport/UdpReceivedInput.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/transport/UdpInputReceiver.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/transport/InputReplayGuard.kt
@desktop-companion/src/test/kotlin/com/btgun/desktop/transport/DesktopUdpInputRuntimeTest.kt
@docs/protocol/input-stream-v1-fixtures.md
@.planning/phases/05-desktop-backend-contract-and-smoke-harness/05-CONTEXT.md
@.planning/phases/05-desktop-backend-contract-and-smoke-harness/05-PATTERNS.md
  </read_first>
  <files>desktop-companion/build.gradle.kts, desktop-companion/src/test/kotlin/com/btgun/desktop/backend/UdpControllerStateAdapterTest.kt</files>
  <behavior>
    - Test snapshot fixture datagram is accepted by `UdpInputReceiver` before adapter sees it.
    - Test snapshot maps pressed controls `trigger`, `reload`, `b`, `stickX=12345`, `stickY=-12345`, `aimX=0.125f`, `aimY=-0.25f`, `sourceSequence=42`.
    - Test edge fixture maps `x=true`, `stickX=-32768`, `stickY=32767`, and NaN `rawAimX/rawAimY` to neutral `0.0f`.
    - Test stale input from receiver timeout clears buttons/stick while preserving stale flag and sequence.
  </behavior>
  <action>Add `UdpControllerStateAdapterTest.kt` using main-style tests and register `com.btgun.desktop.backend.UdpControllerStateAdapterTestKt` in `desktop-companion/build.gradle.kts`. Build fixture datagrams from `GOLDEN_SNAPSHOT_FRAME_HEX` and `GOLDEN_EDGE_FRAME_HEX` constants copied from `docs/protocol/input-stream-v1-fixtures.md`; do not use direct `SemanticControllerState` injection as the acceptance path. RED must reference `UdpControllerStateAdapter.toState` before implementation.</action>
  <verify>
    <automated>cd desktop-companion && GRADLE_USER_HOME=/private/tmp/bt-gun-gradle gradle test --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>Command reaches RED and fails only because `UdpControllerStateAdapter` is missing. Test imports `com.btgun.desktop.transport.UdpInputReceiver`, `InputStreamConfig`, and `UdpInputReceiverResult`. Test source contains no configurable mapping, profile IDs, yaw/pitch/roll mapped as axes, Android preview aim, or raw HID report packing.</acceptance_criteria>
  <done>RED test is registered, fails for missing adapter only, and proves fixture replay must cross `UdpInputReceiver` before state mapping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN UDP to semantic state adapter</name>
  <read_first>
@desktop-companion/src/test/kotlin/com/btgun/desktop/backend/UdpControllerStateAdapterTest.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/backend/UdpControllerStateAdapter.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/backend/SemanticControllerState.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/transport/UdpReceivedInput.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/transport/UdpInputReceiver.kt
@docs/protocol/input-stream-v1-fixtures.md
  </read_first>
  <files>desktop-companion/src/main/kotlin/com/btgun/desktop/backend/UdpControllerStateAdapter.kt</files>
  <behavior>
    - `toState` maps `pressedControls` names to six booleans exactly.
    - `toState` copies `stickX`, `stickY`, `stale`, and `lastAcceptedSequence`.
    - `toState` maps `motion.rawAimX/rawAimY` to `aimX/aimY`, replacing `NaN` with `0.0f`.
    - `toState` ignores `yaw`, `pitch`, `roll`, provider, and capability flags for HID axes.
  </behavior>
  <action>Create `UdpControllerStateAdapter.kt` in `com.btgun.desktop.backend` with object `UdpControllerStateAdapter` and function `toState(input: UdpReceivedInput): SemanticControllerState`. Map only the D-03 semantic controls and D-10 aim axes. Do not add profile mapping, dead zone, smoothing, sensitivity, inversion, raw yaw/pitch/roll axes, analog trigger, visualizer metrics, or platform HID packing.</action>
  <verify>
    <automated>cd desktop-companion && GRADLE_USER_HOME=/private/tmp/bt-gun-gradle gradle test --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>`gradle test` passes. Source assertions: `rg -n "ProfileMapper|profile mapping|sensitivity|deadZone|dead zone|smoothing|inversion|yaw.*aim|pitch.*aim|roll.*aim|HIDVirtualDevice|VHF|HIDDriverKit" desktop-companion/src/main/kotlin/com/btgun/desktop/backend desktop-companion/src/test/kotlin/com/btgun/desktop/backend` returns no matches. `UdpControllerStateAdapterTestKt` proves fixture replay uses `UdpInputReceiverResult.Accepted` before `toState`.</acceptance_criteria>
  <done>Adapter maps accepted `UdpReceivedInput` into semantic state from Phase 4 fixture replay with no profile/HID behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated UDP datagram -> backend state | Untrusted LAN datagrams must pass Phase 4 receiver auth/replay checks before controller state publish. |
| Raw motion payload -> semantic aim axes | Raw yaw/pitch/roll/provider data must not become HID axes in Phase 5. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Spoofing | `UdpControllerStateAdapterTest` | mitigate | Acceptance tests feed authenticated fixture bytes through `UdpInputReceiver.handleDatagram`; direct semantic injection is unit-only and not used for smoke acceptance. |
| T-05-08 | Tampering | `UdpControllerStateAdapter` | mitigate | Adapter maps only `pressedControls`, stick axes, rawAimX/rawAimY, stale, and sequence; tests forbid yaw/pitch/roll HID axis mapping. |
| T-05-09 | Elevation of Privilege | UDP input path | mitigate | No new UDP listener, key, or bypass is introduced; existing `InputReplayGuard` remains the only datagram acceptance gate. |
| T-05-SC | Tampering | Package installs | accept | No package-manager install is planned; no new dependencies are added. |
</threat_model>

<verification>
Run `cd desktop-companion && GRADLE_USER_HOME=/private/tmp/bt-gun-gradle gradle test --rerun-tasks`.
Run `rg -n "ProfileMapper|profile mapping|sensitivity|deadZone|dead zone|smoothing|inversion|yaw.*aim|pitch.*aim|roll.*aim|HIDVirtualDevice|VHF|HIDDriverKit" desktop-companion/src/main/kotlin/com/btgun/desktop/backend desktop-companion/src/test/kotlin/com/btgun/desktop/backend` and confirm no matches.
</verification>

<success_criteria>
DESK-08 receiver handoff is proven by authenticated fixture replay through `UdpInputReceiver`. DESK-04 semantic state receives exactly trigger/reload/X/Y/A/B/stickX/stickY/aimX/aimY with digital trigger semantics and no profile or HID platform behavior.
</success_criteria>

<output>
Create `.planning/phases/05-desktop-backend-contract-and-smoke-harness/05-03-SUMMARY.md` when done.
</output>
