---
phase: "05-desktop-backend-contract-and-smoke-harness"
plan: "01"
type: "tdd"
wave: 1
depends_on: []
files_modified:
  - "desktop-companion/build.gradle.kts"
  - "desktop-companion/src/main/kotlin/com/btgun/desktop/backend/SemanticControllerState.kt"
  - "desktop-companion/src/main/kotlin/com/btgun/desktop/backend/VirtualControllerDescriptor.kt"
  - "desktop-companion/src/test/kotlin/com/btgun/desktop/backend/BackendContractTest.kt"
autonomous: true
requirements:
  - "DESK-04"
must_haves:
  truths:
    - "Developer can run a backend contract test proving the Phase 5 controller is one regular gamepad-like joystick device per D-09."
    - "Descriptor exposes exactly trigger, reload, X, Y, A, B, stickX, stickY, aimX, aimY, and digital trigger per D-03 and D-09 through D-12."
  artifacts:
    - path: "desktop-companion/src/main/kotlin/com/btgun/desktop/backend/SemanticControllerState.kt"
      provides: "Immutable semantic controller state for trigger/reload/X/Y/A/B/stick/aim"
      exports: ["SemanticControllerState"]
    - path: "desktop-companion/src/main/kotlin/com/btgun/desktop/backend/VirtualControllerDescriptor.kt"
      provides: "Regular gamepad-like descriptor contract and invariant check"
      exports: ["VirtualControllerDescriptor", "btGunV1Descriptor", "requireBtGunV1Invariant"]
    - path: "desktop-companion/src/test/kotlin/com/btgun/desktop/backend/BackendContractTest.kt"
      provides: "Main-style descriptor/state contract test"
  key_links:
    - from: "BackendContractTest.kt"
      to: "VirtualControllerDescriptor.kt"
      via: "asserts exact buttons, axes, digital trigger, one-device kind"
      pattern: "btGunV1Descriptor.*requireBtGunV1Invariant"
    - from: "BackendContractTest.kt"
      to: "SemanticControllerState.kt"
      via: "asserts fields and neutral defaults"
      pattern: "SemanticControllerState"
---

## Phase Goal

**As a** developer, **I want to** validate shared virtual-controller behavior on both desktop targets before using the real Android stream, **so that** Windows and macOS driver work starts from a proven backend contract.

<objective>
Create the first vertical backend contract slice: semantic controller state plus descriptor invariants for DESK-04.

Purpose: lock the gamepad-like shape before platform adapters exist, honoring D-01, D-03, D-09, D-10, D-11, and D-12.
Output: backend state/descriptor source files and a main-style Gradle test.
</objective>

<execution_context>
@/Users/lucas.rancez/.codex/gsd-core/workflows/execute-plan.md
@/Users/lucas.rancez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-desktop-backend-contract-and-smoke-harness/05-CONTEXT.md
@.planning/phases/05-desktop-backend-contract-and-smoke-harness/05-RESEARCH.md
@.planning/phases/05-desktop-backend-contract-and-smoke-harness/05-VALIDATION.md
@.planning/phases/05-desktop-backend-contract-and-smoke-harness/05-PATTERNS.md
@desktop-companion/build.gradle.kts
@desktop-companion/src/main/kotlin/com/btgun/desktop/transport/UdpReceivedInput.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/control/ControlDiagnostics.kt
@desktop-companion/src/test/kotlin/com/btgun/desktop/control/ControlChannelTest.kt
</context>

## Artifacts This Phase Produces

| Artifact | Kind | Produced Here | Notes |
|----------|------|---------------|-------|
| `SemanticControllerState` | class | yes | Fields: `trigger`, `reload`, `x`, `y`, `a`, `b`, `stickX`, `stickY`, `aimX`, `aimY`, `stale`, `sourceSequence`. |
| `VirtualControllerDescriptor` | class | yes | Fields: `deviceKind`, `buttons`, `axes`, `triggerKind`. |
| `btGunV1Descriptor` | function or value | yes | Exact descriptor: `gamepad_like_joystick`, buttons `trigger,reload,x,y,a,b`, axes `stickX,stickY,aimX,aimY`, trigger `digital`. |
| `requireBtGunV1Invariant` | function | yes | Throws if descriptor violates six-button/four-axis/digital-trigger contract. |
| `BackendCapabilities`, `UnsupportedReason`, `HapticEffectCapability` | classes | no | Created in `05-02-PLAN.md`. |
| `VirtualControllerBackend`, `StubVirtualControllerBackend` | interface/class | no | Created in `05-02-PLAN.md`. |
| `UdpControllerStateAdapter` | object | no | Created in `05-03-PLAN.md`. |
| `BackendSmokeRunner`, `JunitSmokeXml`, `MacosBackendSmokeMain`, `WindowsBackendSmokeMain` | classes | no | Created in `05-04-PLAN.md`. |
| `SimulatedOutputReport`, haptic smoke path, `-Pbtgun.smoke.haptic=true` | symbols/CLI flag | no | Created in `05-05-PLAN.md`. |
| `smokeDesktopBackendMacosStub`, `smokeDesktopBackendWindowsStub` | Gradle tasks | no | Created in `05-04-PLAN.md`. |
| `docs/evidence/manifests/phase5-desktop-backend-smoke.jsonl` | manifest | no | Created in `05-05-PLAN.md`. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED descriptor and semantic state contract tests</name>
  <read_first>
@desktop-companion/build.gradle.kts
@desktop-companion/src/test/kotlin/com/btgun/desktop/control/ControlChannelTest.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/transport/UdpReceivedInput.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/control/ControlDiagnostics.kt
@.planning/phases/05-desktop-backend-contract-and-smoke-harness/05-CONTEXT.md
@.planning/phases/05-desktop-backend-contract-and-smoke-harness/05-PATTERNS.md
  </read_first>
  <files>desktop-companion/build.gradle.kts, desktop-companion/src/test/kotlin/com/btgun/desktop/backend/BackendContractTest.kt</files>
  <behavior>
    - Test descriptor buttons equal `listOf("trigger", "reload", "x", "y", "a", "b")` per D-03 and D-11.
    - Test descriptor axes equal `listOf("stickX", "stickY", "aimX", "aimY")` per D-10.
    - Test descriptor `deviceKind` equals `gamepad_like_joystick` and `triggerKind` equals `digital` per D-09 and D-12.
    - Test `SemanticControllerState()` defaults to all buttons false, `stickX=0`, `stickY=0`, `aimX=0.0f`, `aimY=0.0f`, `stale=false`, `sourceSequence=null`.
  </behavior>
  <action>Add `BackendContractTest.kt` using the existing plain Kotlin `main()` style and local `expectEquals` / `expectTrue` helpers. Register `com.btgun.desktop.backend.BackendContractTestKt` in `desktop-companion/build.gradle.kts` `tasks.withType<Test>().doLast` list. Do not add JUnit or any new dependency. RED must reference `SemanticControllerState`, `btGunV1Descriptor`, and `requireBtGunV1Invariant` before production files exist.</action>
  <verify>
    <automated>cd desktop-companion && GRADLE_USER_HOME=/private/tmp/bt-gun-gradle gradle test --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>Command reaches RED and fails because backend contract production symbols are missing, not because Gradle wiring, package names, or unrelated tests are broken. Test source contains no `yaw`, `pitch`, `roll`, analog trigger axis, physical stick direction button fields, profile mapping, visualizer UI, VHF, CoreHID, or HIDDriverKit imports.</acceptance_criteria>
  <done>RED test is registered, fails for missing contract symbols only, and encodes exact DESK-04 descriptor/state expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN semantic state and v1 descriptor contract</name>
  <read_first>
@desktop-companion/src/test/kotlin/com/btgun/desktop/backend/BackendContractTest.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/backend/SemanticControllerState.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/backend/VirtualControllerDescriptor.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/transport/UdpReceivedInput.kt
@.planning/phases/05-desktop-backend-contract-and-smoke-harness/05-CONTEXT.md
  </read_first>
  <files>desktop-companion/src/main/kotlin/com/btgun/desktop/backend/SemanticControllerState.kt, desktop-companion/src/main/kotlin/com/btgun/desktop/backend/VirtualControllerDescriptor.kt</files>
  <behavior>
    - `SemanticControllerState` is immutable and carries only named semantic controls from D-03 plus `stale` and `sourceSequence`.
    - `VirtualControllerDescriptor` represents one regular gamepad-like joystick device with six buttons, four axes, and digital trigger.
    - `requireBtGunV1Invariant` rejects any descriptor with missing/extra/reordered v1 buttons or axes, non-`gamepad_like_joystick` kind, or non-`digital` trigger.
  </behavior>
  <action>Create `com.btgun.desktop.backend.SemanticControllerState` as a data class with default neutral values. Create `VirtualControllerDescriptor.kt` with `VirtualControllerDescriptor`, `btGunV1Descriptor`, and `requireBtGunV1Invariant`. Keep axis ranges as state values only: `stickX`/`stickY` signed `Int`, `aimX`/`aimY` `Float`. Do not expose raw yaw/pitch/roll, Android preview aim, analog trigger, profile mapping, OS HID report bytes, or platform-specific driver imports.</action>
  <verify>
    <automated>cd desktop-companion && GRADLE_USER_HOME=/private/tmp/bt-gun-gradle gradle test --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>`gradle test` passes. `BackendContractTestKt` verifies exact descriptor values and neutral state defaults. Source assertions: `rg -n "yaw|pitch|roll|analog|ProfileMapper|profile mapping|HIDVirtualDevice|VHF|HIDDriverKit|VirtualHID" desktop-companion/src/main/kotlin/com/btgun/desktop/backend desktop-companion/src/test/kotlin/com/btgun/desktop/backend` returns no matches except comments explicitly naming forbidden scope in tests are absent.</acceptance_criteria>
  <done>Semantic state, descriptor, invariant function, and Gradle test pass with no forbidden Phase 6/7/8/9 scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend contract -> future platform adapter | Shared descriptor/state can misrepresent OS-visible capabilities if it includes unsupported controls. |
| Test artifacts -> developer evidence | Contract tests can pass while hiding future scope creep if assertions are loose. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `VirtualControllerDescriptor` | mitigate | `requireBtGunV1Invariant` and `BackendContractTestKt` assert exact six-button, four-axis, digital-trigger descriptor per D-09..D-12. |
| T-05-02 | Spoofing | `SemanticControllerState` | mitigate | State uses named semantic fields only; no raw HID bytes or platform driver types can impersonate backend state in Phase 5. |
| T-05-03 | Repudiation | Main-style tests | mitigate | Register test class in Gradle so `gradle test --rerun-tasks` executes it with existing project pattern. |
| T-05-SC | Tampering | Package installs | accept | No package-manager install is planned; Phase 5 uses existing Kotlin/JVM dependencies only. |
</threat_model>

<verification>
Run `cd desktop-companion && GRADLE_USER_HOME=/private/tmp/bt-gun-gradle gradle test --rerun-tasks`.
Run `rg -n "yaw|pitch|roll|analog|ProfileMapper|profile mapping|HIDVirtualDevice|VHF|HIDDriverKit|VirtualHID" desktop-companion/src/main/kotlin/com/btgun/desktop/backend desktop-companion/src/test/kotlin/com/btgun/desktop/backend` and confirm no matches.
</verification>

<success_criteria>
DESK-04 contract exists as source, not prose. Descriptor has exactly trigger/reload/X/Y/A/B buttons, stickX/stickY/aimX/aimY axes, digital trigger, and one gamepad-like joystick device kind. Tests prove the invariant through Gradle.
</success_criteria>

<output>
Create `.planning/phases/05-desktop-backend-contract-and-smoke-harness/05-01-SUMMARY.md` when done.
</output>
