---
phase: 10-diagnostics-replay-and-v1-docs
plan: "05"
type: tdd
wave: 4
depends_on: [10-01, 10-02, 10-03, 10-04]
files_modified:
  - desktop-companion/src/main/kotlin/com/btgun/desktop/security/SecretRedactor.kt
  - desktop-companion/src/main/kotlin/com/btgun/desktop/diagnostics/DiagnosticExport.kt
  - desktop-companion/src/test/kotlin/com/btgun/desktop/diagnostics/DiagnosticExportTest.kt
  - desktop-companion/src/test/kotlin/com/btgun/desktop/security/PairingSecurityTest.kt
  - desktop-companion/build.gradle.kts
  - docs/evidence/manifests/phase10-diagnostic-export.jsonl
  - .gitignore
autonomous: true
requirements: [PERF-05, PACK-04]
user_setup: []
must_haves:
  truths:
    - "Sanitized export bundle is replay-ready and excludes raw logs/screenshots by default per D-09 through D-12."
    - "Redaction bans pairing codes, proof values, stream keys, HMAC material, private keys, full Bluetooth addresses, full serials, full Android IDs, and screenshot/log dumps per D-10."
    - "D-11: Truncated identifiers are allowed when useful after redaction; full identifiers remain banned from committed artifacts."
  artifacts:
    - "desktop-companion/src/main/kotlin/com/btgun/desktop/diagnostics/DiagnosticExport.kt"
    - "desktop-companion/src/test/kotlin/com/btgun/desktop/diagnostics/DiagnosticExportTest.kt"
    - "docs/evidence/manifests/phase10-diagnostic-export.jsonl"
  key_links:
    - "DiagnosticExport.kt -> SecretRedactor.redact -> fixtures/replay/*"
    - ".gitignore -> .evidence/ raw export storage"
---

## Phase Goal

**As a** developer, **I want to** repeat, diagnose, and document the version 1 MVP, **so that** I do not depend on hidden setup knowledge.

<objective>
Create sanitized replay-ready diagnostic export behavior.

Purpose: Let issue bundles carry diagnostics and replay references without committing raw evidence or secrets.
Output: Extended redactor, export writer, export manifest, and tests.
</objective>

<execution_context>
@/Users/lucas.rancez/.codex/gsd-core/workflows/execute-plan.md
@/Users/lucas.rancez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-diagnostics-replay-and-v1-docs/10-CONTEXT.md
@.planning/phases/10-diagnostics-replay-and-v1-docs/10-RESEARCH.md
@.planning/phases/10-diagnostics-replay-and-v1-docs/10-VALIDATION.md
@.planning/phases/10-diagnostics-replay-and-v1-docs/10-PATTERNS.md
@desktop-companion/src/main/kotlin/com/btgun/desktop/security/SecretRedactor.kt
@desktop-companion/src/test/kotlin/com/btgun/desktop/security/PairingSecurityTest.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/diagnostics/DiagnosticEvent.kt
@fixtures/replay/README.md
@docs/evidence/manifests/phase10-replay-fixtures.jsonl
</context>

## Artifacts This Phase Produces

- New path: `desktop-companion/src/main/kotlin/com/btgun/desktop/diagnostics/DiagnosticExport.kt`.
- New path: `desktop-companion/src/test/kotlin/com/btgun/desktop/diagnostics/DiagnosticExportTest.kt`.
- New path: `docs/evidence/manifests/phase10-diagnostic-export.jsonl`.
- New test entrypoint: `com.btgun.desktop.diagnostics.DiagnosticExportTestKt`.
- New symbols: `DiagnosticExportBundle`, `DiagnosticExportWriter`, `DiagnosticExportManifest`, `DiagnosticExportResult`.
- New export schema fields: `schema`, `bundle_id`, `created_elapsed`, `diagnostics_jsonl`, `replay_refs`, `app_versions`, `capability_statuses`, `manifest_ref`, `raw_included`.
- New redaction tags: `<redacted-stream-key>`, `<redacted-hmac-material>`, `<redacted-bluetooth-address>`, `<redacted-device-id>`, `<redacted-raw-evidence>`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED redaction and export tests</name>
  <files>desktop-companion/src/test/kotlin/com/btgun/desktop/diagnostics/DiagnosticExportTest.kt, desktop-companion/src/test/kotlin/com/btgun/desktop/security/PairingSecurityTest.kt, desktop-companion/build.gradle.kts</files>
  <read_first>
    - `.planning/phases/10-diagnostics-replay-and-v1-docs/10-CONTEXT.md` decisions D-09 through D-12.
    - `.planning/phases/10-diagnostics-replay-and-v1-docs/10-PATTERNS.md` sections `DiagnosticExport.kt` and `DiagnosticExportTest.kt`.
    - `desktop-companion/src/main/kotlin/com/btgun/desktop/security/SecretRedactor.kt`.
    - `desktop-companion/src/test/kotlin/com/btgun/desktop/security/PairingSecurityTest.kt`.
    - `.gitignore`.
  </read_first>
  <behavior>
    - Test 1: redactor hides stream keys, HMAC material, Bluetooth MACs, serials, Android IDs, raw log markers, and screenshot paths.
    - Test 2: export bundle contains diagnostics JSONL, replay refs, app/build versions, capability statuses, and manifest pointer.
    - Test 3: export bundle default `raw_included` is false and raw `.evidence` paths are not copied into committed output.
    - Test 4: bundle size/noise guard rejects overly large diagnostic detail lines.
  </behavior>
  <action>Add RED tests for redaction expansion and export bundle contract. Register `com.btgun.desktop.diagnostics.DiagnosticExportTestKt`. Extend `PairingSecurityTest.kt` expectations for non-pairing secrets so D-10 is enforced centrally before export writer implementation.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p desktop-companion test --tests '*Redactor*' --tests '*DiagnosticExport*' --no-daemon --console=plain</automated>
  </verify>
  <acceptance_criteria>
    - RED run fails on missing export writer or redaction rules.
    - Tests cover every forbidden category listed in D-10.
    - Tests distinguish ignored raw evidence from committed sanitized export.
  </acceptance_criteria>
  <done>RED export/redaction tests exist and fail for planned missing redactor/export behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN sanitized export writer and ignored raw boundary</name>
  <files>desktop-companion/src/main/kotlin/com/btgun/desktop/security/SecretRedactor.kt, desktop-companion/src/main/kotlin/com/btgun/desktop/diagnostics/DiagnosticExport.kt, docs/evidence/manifests/phase10-diagnostic-export.jsonl, .gitignore</files>
  <read_first>
    - `desktop-companion/src/main/kotlin/com/btgun/desktop/diagnostics/DiagnosticEvent.kt`.
    - `fixtures/replay/README.md`.
    - `docs/evidence/manifests/phase10-replay-fixtures.jsonl`.
    - `android-host/scripts/collect-phase2-host-evidence.sh`.
    - `.planning/phases/10-diagnostics-replay-and-v1-docs/10-RESEARCH.md` Pitfall 3 and Pitfall 6.
  </read_first>
  <behavior>
    - Test 1 passes with expanded central redaction rules.
    - Test 2 export emits sanitized JSONL and replay refs only, with manifest pointer.
    - Test 3 export refuses raw evidence by default and records `raw_included=false`.
  </behavior>
  <action>Implement `DiagnosticExport.kt` as a desktop-owned sanitized bundle writer. It should accept diagnostic events, replay fixture refs, app/build version strings, capability statuses, and manifest refs; apply `SecretRedactor.redact` to every text field before persistence; write only sanitized JSONL/manifest output. Update `.gitignore` only if needed so raw `.evidence/phase10/` export source files remain ignored by default. Add a sanitized schema/manifest row for the export contract.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p desktop-companion test --tests '*Redactor*' --tests '*DiagnosticExport*' --tests '*ReplayFixture*' --no-daemon --console=plain</automated>
    <automated>! rg -n "qr_secret|manual code|pairing_proof|stream key|HMAC key|private key|Bluetooth address|Android ID|serial|raw screenshot|raw log|[0-9A-Fa-f]{2}(:[0-9A-Fa-f]{2}){5}" desktop-companion/src/main/kotlin/com/btgun/desktop/diagnostics desktop-companion/src/test/kotlin/com/btgun/desktop/diagnostics docs/evidence/manifests/phase10-diagnostic-export.jsonl</automated>
  </verify>
  <acceptance_criteria>
    - `DiagnosticExport.kt` writes sanitized rows only and has no raw-log copy path by default.
    - Export bundle is replay-ready by including replay fixture refs from Plan 10-01.
    - Manifest row documents schema, redaction policy, and default raw exclusion.
  </acceptance_criteria>
  <done>Sanitized export bundle writer and redaction gate are green and ready for docs/source guards.</done>
</task>

</tasks>

<source_audit>
GOAL: Covered by repeatable sanitized issue/export artifacts.
REQ: PERF-05 and PACK-04 covered for diagnostics/security artifacts.
RESEARCH: Uses central redactor and ignored raw evidence boundary.
CONTEXT: Implements D-09, D-10, D-11, and D-12. Deferred ideas: none.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| diagnostic events/replay refs -> export bundle | Runtime data becomes persisted local artifact. |
| raw evidence path -> sanitized export | Local raw logs/screenshots may exist but must not be copied by default. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-13 | Information Disclosure | `SecretRedactor` and `DiagnosticExport` | mitigate | Central redaction tests cover pairing, stream, HMAC, private key, full identifier, screenshot, and raw-log categories. |
| T-10-14 | Denial of Service | export bundle size/detail | mitigate | Cap detail length and reject noisy/oversized lines in export tests. |
| T-10-15 | Tampering | export replay refs | mitigate | Export links to committed replay fixture ids and manifest refs, not arbitrary raw files. |
| T-10-SC | Tampering | package installs | accept | No new package-manager install tasks; research package audit says none new. |
</threat_model>

<verification>
Focused redactor/export/replay tests and export forbidden-pattern scan must pass.
</verification>

<success_criteria>
Developer can create a sanitized replay-ready diagnostic bundle that is safe to commit by default and excludes raw evidence.
</success_criteria>

<output>
Create `.planning/phases/10-diagnostics-replay-and-v1-docs/10-05-SUMMARY.md` when done.
</output>
