---
phase: 10-diagnostics-replay-and-v1-docs
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - fixtures/replay/README.md
  - fixtures/replay/udp-golden/mapped-session-001.hex
  - fixtures/replay/udp-golden/mapped-session-001.jsonl
  - fixtures/replay/expected/mapped-session-001-visualizer.json
  - docs/evidence/manifests/phase10-replay-fixtures.jsonl
  - desktop-companion/src/test/kotlin/com/btgun/desktop/replay/ReplayFixtureTest.kt
  - desktop-companion/build.gradle.kts
autonomous: true
requirements: [PERF-04]
user_setup: []
must_haves:
  truths:
    - "Developer can replay committed packet fixtures through decode, receiver auth/replay guard, mapped state, visualizer model, and metrics."
    - "Replay fixtures are small, sanitized, and linked to evidence provenance per D-01, D-02, D-03, and D-04."
  artifacts:
    - "fixtures/replay/README.md"
    - "fixtures/replay/udp-golden/mapped-session-001.hex"
    - "fixtures/replay/udp-golden/mapped-session-001.jsonl"
    - "fixtures/replay/expected/mapped-session-001-visualizer.json"
    - "docs/evidence/manifests/phase10-replay-fixtures.jsonl"
    - "desktop-companion/src/test/kotlin/com/btgun/desktop/replay/ReplayFixtureTest.kt"
  key_links:
    - "ReplayFixtureTest.kt -> UdpInputFrameCodec.authenticateAndDecode -> UdpInputReceiver -> UdpControllerStateAdapter -> VisualizerModel -> VisualizerMetrics"
    - "phase10-replay-fixtures.jsonl -> fixtures/replay/* provenance"
---

## Phase Goal

**As a** developer, **I want to** repeat, diagnose, and document the version 1 MVP, **so that** I do not depend on hidden setup knowledge.

<objective>
Create the first replay slice for PERF-04.

Purpose: Prove a replay artifact can drive the same production input path as live Android LAN input.
Output: Small replay fixture corpus, provenance manifest, and a failing-then-passing full-chain replay test.
</objective>

<execution_context>
@/Users/lucas.rancez/.codex/gsd-core/workflows/execute-plan.md
@/Users/lucas.rancez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-diagnostics-replay-and-v1-docs/10-CONTEXT.md
@.planning/phases/10-diagnostics-replay-and-v1-docs/10-RESEARCH.md
@.planning/phases/10-diagnostics-replay-and-v1-docs/10-VALIDATION.md
@.planning/phases/10-diagnostics-replay-and-v1-docs/10-PATTERNS.md
@desktop-companion/src/main/kotlin/com/btgun/desktop/transport/UdpInputFrameCodec.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/transport/UdpInputReceiver.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/backend/UdpControllerStateAdapter.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/ui/VisualizerModel.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/ui/VisualizerMetrics.kt
@desktop-companion/src/test/kotlin/com/btgun/desktop/backend/UdpControllerStateAdapterTest.kt
@docs/protocol/input-stream-v1-fixtures.md
</context>

## Artifacts This Phase Produces

- New paths: `fixtures/replay/README.md`, `fixtures/replay/udp-golden/mapped-session-001.hex`, `fixtures/replay/udp-golden/mapped-session-001.jsonl`, `fixtures/replay/expected/mapped-session-001-visualizer.json`, `docs/evidence/manifests/phase10-replay-fixtures.jsonl`, `desktop-companion/src/test/kotlin/com/btgun/desktop/replay/ReplayFixtureTest.kt`.
- New test entrypoint: `com.btgun.desktop.replay.ReplayFixtureTestKt`.
- New fixture ids: `mapped-session-001`, `phase10-replay-mapped-session-001`.
- Required fixture schema fields: `schema`, `fixture_id`, `source_manifest`, `stream.control_session_ref`, `stream.stream_session_ref`, `datagrams`, `expected`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED full-chain replay fixture test</name>
  <files>desktop-companion/src/test/kotlin/com/btgun/desktop/replay/ReplayFixtureTest.kt, desktop-companion/build.gradle.kts, fixtures/replay/README.md</files>
  <read_first>
    - `.planning/phases/10-diagnostics-replay-and-v1-docs/10-CONTEXT.md` decisions D-01 through D-04.
    - `.planning/phases/10-diagnostics-replay-and-v1-docs/10-PATTERNS.md` sections `ReplayFixtureTest.kt` and `fixtures/replay/*`.
    - `desktop-companion/src/test/kotlin/com/btgun/desktop/backend/UdpControllerStateAdapterTest.kt`.
    - `desktop-companion/src/test/kotlin/com/btgun/desktop/ui/VisualizerModelTest.kt`.
    - `desktop-companion/build.gradle.kts`.
  </read_first>
  <behavior>
    - Test 1: fixture `mapped-session-001` loads raw UDP hex and fails until fixture data exists.
    - Test 2: accepted datagrams must pass `UdpInputReceiver` before `UdpControllerStateAdapter.toState`.
    - Test 3: visualizer rows `LAN_VISUALIZER_STREAM`, `LIVE_CONTROLS`, `LATENCY_TARGET`, and `PACKET_LOSS` become observed from replay output.
    - Test 4: replay expected output includes offset quality or UDP-estimated timing, not direct Android-to-desktop monotonic subtraction.
  </behavior>
  <action>Create `ReplayFixtureTest.kt` with main-function tests and register `com.btgun.desktop.replay.ReplayFixtureTestKt` in `desktop-companion/build.gradle.kts`. Keep the RED commit focused on the failing replay contract. The test must read from `fixtures/replay/udp-golden/mapped-session-001.hex` and `fixtures/replay/expected/mapped-session-001-visualizer.json`, then call production `UdpInputReceiver` before mapping or model assertions per D-03.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p desktop-companion test --tests '*ReplayFixture*' --no-daemon --console=plain</automated>
  </verify>
  <acceptance_criteria>
    - RED run fails for missing fixture/expected replay artifact or missing replay helper, not syntax or classpath errors.
    - `ReplayFixtureTest.kt` contains `UdpInputReceiver` and `UdpControllerStateAdapter` references.
    - `ReplayFixtureTest.kt` does not construct `UdpReceivedInput` by hand for the PERF-04 happy path.
  </acceptance_criteria>
  <done>RED replay test exists, is registered, and fails for the planned missing replay corpus.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN replay corpus and expected visualizer snapshot</name>
  <files>fixtures/replay/README.md, fixtures/replay/udp-golden/mapped-session-001.hex, fixtures/replay/udp-golden/mapped-session-001.jsonl, fixtures/replay/expected/mapped-session-001-visualizer.json, docs/evidence/manifests/phase10-replay-fixtures.jsonl</files>
  <read_first>
    - `docs/protocol/input-stream-v1-fixtures.md`.
    - `docs/evidence/manifests/phase4-input-haptic-transport.jsonl`.
    - `.planning/phases/10-diagnostics-replay-and-v1-docs/10-RESEARCH.md` Pattern 1 and Pattern 2.
    - `desktop-companion/src/test/kotlin/com/btgun/desktop/transport/UdpInputFrameCodecTest.kt`.
  </read_first>
  <behavior>
    - Test 1 passes using small committed UDP hex datagrams and sanitized JSONL per D-01 and D-02.
    - Test 2 expected visualizer JSON asserts mapped controls, aim axes, packet counters, and latency target status.
    - Test 3 provenance manifest links `mapped-session-001` to sanitized Phase 4 or Phase 9 evidence, never raw logs.
  </behavior>
  <action>Add a reviewable hex-only first replay corpus under `fixtures/replay/` per D-01, D-02, and D-04. Use sanitized refs and suffixes only; do not commit raw `.evidence` logs, screenshots, full identifiers, pairing material, stream keys, HMAC material, or private keys. Add README schema/redaction policy and a JSONL manifest row linking the replay fixture to sanitized evidence provenance.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p desktop-companion test --tests '*ReplayFixture*' --tests '*UdpControllerStateAdapter*' --tests '*VisualizerMetrics*' --tests '*VisualizerModel*' --no-daemon --console=plain</automated>
    <automated>! rg -n "qr_secret|manual code|pairing_proof|stream key|HMAC key|private key|Bluetooth address|Android ID|serial|raw screenshot|raw log|[0-9A-Fa-f]{2}(:[0-9A-Fa-f]{2}){5}" fixtures/replay docs/evidence/manifests/phase10-replay-fixtures.jsonl</automated>
  </verify>
  <acceptance_criteria>
    - `mapped-session-001.hex` has only short fixture datagrams needed by the test.
    - `mapped-session-001.jsonl` starts with a schema/redaction policy row.
    - `mapped-session-001-visualizer.json` contains expected mapped state, metrics, and checklist output.
    - Redaction scan returns no matches for forbidden secrets or full identifiers.
  </acceptance_criteria>
  <done>GREEN replay test passes and the committed replay corpus is small, sanitized, and provenance-linked.</done>
</task>

</tasks>

<source_audit>
GOAL: Covered by repeatable replay artifact.
REQ: PERF-04 covered.
RESEARCH: Uses recommended `fixtures/replay/` corpus and full-chain replay path.
CONTEXT: Implements D-01, D-02, D-03, and D-04. Deferred ideas: none.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replay fixture file -> test runtime | Committed replay data enters parser, auth, mapping, and visualizer checks. |
| evidence manifest -> repo | Sanitized provenance rows become committed artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | `ReplayFixtureTest.kt` | mitigate | Replay raw datagrams through `UdpInputReceiver` and existing HMAC/session/replay guard before mapping. |
| T-10-02 | Information Disclosure | `fixtures/replay/*` | mitigate | Keep hex corpus small, sanitize JSONL, ban secrets/full identifiers with rg scan. |
| T-10-03 | Repudiation | `phase10-replay-fixtures.jsonl` | mitigate | Link each fixture to sanitized evidence refs and fixture ids. |
| T-10-SC | Tampering | package installs | accept | No new package-manager install tasks; research package audit says none new. |
</threat_model>

<verification>
Run the focused desktop replay command, existing visualizer/model tests, and forbidden-pattern scan listed above.
</verification>

<success_criteria>
PERF-04 has a committed, replayable fixture proving parser, receiver, mapped state, visualizer model, and metrics output without physical hardware.
</success_criteria>

<output>
Create `.planning/phases/10-diagnostics-replay-and-v1-docs/10-01-SUMMARY.md` when done.
</output>
