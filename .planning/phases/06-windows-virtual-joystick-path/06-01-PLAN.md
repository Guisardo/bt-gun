---
phase: 06-windows-virtual-joystick-path
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - desktop-companion/build.gradle.kts
  - desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsHidReportPacker.kt
  - desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsOutputReportMapper.kt
  - desktop-companion/src/test/kotlin/com/btgun/desktop/backend/windows/WindowsHidReportPackerTest.kt
  - desktop-companion/src/test/kotlin/com/btgun/desktop/backend/windows/WindowsOutputReportMapperTest.kt
autonomous: true
requirements: [DESK-02, DESK-05]
requirements_addressed: [DESK-02, DESK-05]
must_haves:
  truths:
    - "DESK-02: SemanticControllerState deterministically packs to Windows HID input report ID 1."
    - "DESK-05: Windows HID output report ID 2 deterministically maps to bounded HapticCommand."
    - "D-13 and D-15: Fixed Phase 5 mapping clears active buttons and stick axes when stale while preserving aim axes."
  artifacts:
    - path: "desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsHidReportPacker.kt"
      provides: "Input report ID 1 packing"
      exports: ["WindowsHidReportPacker", "WindowsInputReport", "WINDOWS_INPUT_REPORT_ID", "WINDOWS_INPUT_REPORT_LENGTH_BYTES"]
    - path: "desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsOutputReportMapper.kt"
      provides: "Output report ID 2 to phone haptic command mapping"
      exports: ["WindowsOutputReportMapper", "WINDOWS_OUTPUT_REPORT_ID", "WINDOWS_OUTPUT_REPORT_VERSION", "WINDOWS_OUTPUT_REPORT_LENGTH_BYTES"]
  key_links:
    - from: "WindowsHidReportPacker"
      to: "SemanticControllerState"
      via: "packInputReport(state)"
      pattern: "SemanticControllerState"
    - from: "WindowsOutputReportMapper"
      to: "HapticCommand"
      via: "toHapticCommand(reportBytes, commandId)"
      pattern: "HapticCommand"
---

<objective>
Create the pure Windows HID report contract for Phase 6.

Purpose: Lock report bytes before driver and bridge code consume them.
Output: Tested Kotlin packer for input report ID 1 and output report mapper for haptic report ID 2.
</objective>

<execution_context>
@/Users/lucas.rancez/.codex/gsd-core/workflows/execute-plan.md
@/Users/lucas.rancez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-windows-virtual-joystick-path/06-CONTEXT.md
@.planning/phases/06-windows-virtual-joystick-path/06-RESEARCH.md
@.planning/phases/06-windows-virtual-joystick-path/06-PATTERNS.md
@.planning/phases/06-windows-virtual-joystick-path/06-VALIDATION.md
@desktop-companion/build.gradle.kts
@desktop-companion/src/main/kotlin/com/btgun/desktop/backend/SemanticControllerState.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/backend/VirtualControllerDescriptor.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/haptics/HapticCommand.kt
@desktop-companion/src/test/kotlin/com/btgun/desktop/backend/UdpControllerStateAdapterTest.kt
@desktop-companion/src/test/kotlin/com/btgun/desktop/backend/BackendHapticSmokeTest.kt

<interfaces>
Semantic state source fields: trigger, reload, x, y, a, b, stickX, stickY, aimX, aimY, stale, sourceSequence.
Descriptor invariant: btGunV1Descriptor buttons are trigger/reload/x/y/a/b; axes are stickX/stickY/aimX/aimY; triggerKind is digital.
HapticCommand bounds: commandId nonblank, strength 0.0..1.0, durationMs 1..1000, ttlMs 1..2000, pattern nullable.
</interfaces>
</context>

## Artifacts this phase produces

- File: `desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsHidReportPacker.kt`
- Symbol: `object WindowsHidReportPacker`
- Symbol: `data class WindowsInputReport`
- Symbol: `const val WINDOWS_INPUT_REPORT_ID = 0x01`
- Symbol: `const val WINDOWS_INPUT_REPORT_LENGTH_BYTES = 10`
- Function: `WindowsHidReportPacker.packInputReport(state: SemanticControllerState): WindowsInputReport`
- File: `desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsOutputReportMapper.kt`
- Symbol: `object WindowsOutputReportMapper`
- Symbol: `const val WINDOWS_OUTPUT_REPORT_ID = 0x02`
- Symbol: `const val WINDOWS_OUTPUT_REPORT_VERSION = 0x01`
- Symbol: `const val WINDOWS_OUTPUT_REPORT_LENGTH_BYTES = 9`
- Function: `WindowsOutputReportMapper.toHapticCommand(reportBytes: ByteArray, commandId: String): HapticCommand?`
- Test classes: `WindowsHidReportPackerTestKt`, `WindowsOutputReportMapperTestKt`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for Windows report contract</name>
  <files>desktop-companion/build.gradle.kts, desktop-companion/src/test/kotlin/com/btgun/desktop/backend/windows/WindowsHidReportPackerTest.kt, desktop-companion/src/test/kotlin/com/btgun/desktop/backend/windows/WindowsOutputReportMapperTest.kt</files>
  <read_first>
    @desktop-companion/build.gradle.kts
    @desktop-companion/src/test/kotlin/com/btgun/desktop/backend/UdpControllerStateAdapterTest.kt
    @desktop-companion/src/test/kotlin/com/btgun/desktop/backend/BackendHapticSmokeTest.kt
    @.planning/phases/06-windows-virtual-joystick-path/06-PATTERNS.md
  </read_first>
  <behavior>
    - `WindowsHidReportPacker.packInputReport` packs report ID 1, six button bits, stick X/Y as signed int16 little-endian, and aim X/Y as bounded signed int16 little-endian.
    - Stale state per D-15 clears trigger/reload/X/Y/A/B and stick X/Y, keeps aim X/Y, sets stale metadata on `WindowsInputReport`.
    - `WindowsOutputReportMapper.toHapticCommand` accepts only report ID 2, version 1, length 9, zero reserved bytes, bounded strength, duration, and TTL.
    - Bad report ID, bad length, bad version, zero duration, zero TTL, oversized duration, oversized TTL, and unsupported flags return null before `HapticCommand`.
  </behavior>
  <action>
    Add failing main-style tests in package `com.btgun.desktop.backend.windows`. Register `WindowsHidReportPackerTestKt` and `WindowsOutputReportMapperTestKt` in `desktop-companion/build.gradle.kts` test class list. Use exact Phase 5 field names and D-13 fixed mapping. Do not add profile mapping, sensitivity, inversion, dead zone, smoothing, remapping, vJoy, ViGEm, LAN parsing, or Android session code per D-01, D-12, and D-14.
  </action>
  <verify>
    <automated>cd desktop-companion &amp;&amp; GRADLE_USER_HOME=/private/tmp/bt-gun-gradle gradle test --offline --no-daemon --console=plain</automated>
    <expected_failure>RED fails only because `WindowsHidReportPacker` and `WindowsOutputReportMapper` symbols do not exist yet.</expected_failure>
  </verify>
  <acceptance_criteria>
    - Test files compile far enough to fail on missing production symbols, not syntax errors.
    - Tests assert report IDs 1 and 2, exact byte lengths 10 and 9, stale behavior, and malformed output rejection.
    - Build file lists both new test main classes.
  </acceptance_criteria>
  <done>RED gate commit exists with failing report-contract tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN implementation for report packer and output mapper</name>
  <files>desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsHidReportPacker.kt, desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsOutputReportMapper.kt</files>
  <read_first>
    @desktop-companion/src/main/kotlin/com/btgun/desktop/backend/SemanticControllerState.kt
    @desktop-companion/src/main/kotlin/com/btgun/desktop/backend/VirtualControllerDescriptor.kt
    @desktop-companion/src/main/kotlin/com/btgun/desktop/haptics/HapticCommand.kt
    @.planning/phases/06-windows-virtual-joystick-path/06-RESEARCH.md
  </read_first>
  <action>
    Implement `WindowsHidReportPacker` and `WindowsOutputReportMapper` to pass RED tests. `packInputReport` must clamp stick inputs to signed int16, clamp aim floats to -1.0..1.0 before scaling to signed int16, encode little-endian, apply D-15 stale clearing, and carry `sourceSequence` in `WindowsInputReport`. `toHapticCommand` must validate all bytes before constructing `HapticCommand`, set `pattern = null`, and use caller-provided `commandId` so tests stay deterministic. Keep this code pure and side-effect free.
  </action>
  <verify>
    <automated>cd desktop-companion &amp;&amp; GRADLE_USER_HOME=/private/tmp/bt-gun-gradle gradle test --offline --no-daemon --console=plain</automated>
  </verify>
  <acceptance_criteria>
    - All new and existing desktop companion tests pass offline.
    - `rg -n "vJoy|ViGEm|ProfileMapper|dead.zone|smoothing|sensitivity|LAN|secret" desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows` returns no production-scope drift.
    - Report ID constants and byte lengths match the driver plan: input ID 1 length 10, output ID 2 version 1 length 9.
  </acceptance_criteria>
  <done>GREEN gate commit exists; REFACTOR commit optional only after tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Semantic state -> HID input bytes | Trusted companion state becomes driver-facing bytes. |
| HID output bytes -> HapticCommand | Windows-origin bytes become authenticated phone haptic command input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `WindowsHidReportPacker` | mitigate | Clamp axes, use exact report length, clear active controls on stale per D-15, and test malformed values. |
| T-06-02 | Tampering | `WindowsOutputReportMapper` | mitigate | Reject bad report ID, length, version, reserved flags, duration, TTL, and strength before `HapticCommand`. |
| T-06-03 | Information Disclosure | Kotlin report tests | mitigate | Keep report tests free of LAN session secrets, HMAC keys, QR payloads, device ids, and private keys. |
| T-06-SC | Tampering | Package installs | accept | No npm, pip, cargo, or new dependency install in this plan. |
</threat_model>

<verification>
Run local companion tests after GREEN:
`cd desktop-companion && GRADLE_USER_HOME=/private/tmp/bt-gun-gradle gradle test --offline --no-daemon --console=plain`
</verification>

<success_criteria>
- RED and GREEN TDD commits exist.
- Input report packing satisfies DESK-02 fixed mapping for later VHF bridge use.
- Output report mapping satisfies DESK-05 byte-to-haptic contract for real HID output forwarding.
</success_criteria>

<output>
Create `.planning/phases/06-windows-virtual-joystick-path/06-01-SUMMARY.md` when done.
</output>
