---
phase: 06-windows-virtual-joystick-path
plan: 06
type: execute
wave: 5
depends_on: [06-04, 06-05]
files_modified:
  - docs/windows/phase6-proof-checklist.md
  - docs/evidence/manifests/phase6-windows-virtual-joystick.jsonl
  - windows/btgun-vjoy/tools/proof/Collect-BtGunVJoyEvidence.ps1
  - windows/btgun-vjoy/tools/proof/Redact-BtGunVJoyEvidence.ps1
autonomous: false
requirements: [DESK-02, DESK-05, PACK-02]
requirements_addressed: [DESK-02, DESK-05, PACK-02]
must_haves:
  truths:
    - "DESK-02: Windows 11 x64 target sees OS-visible regular gamepad-style joystick in CLI/PnP/HID and joy.cpl."
    - "DESK-02: Live paired Android/gun stream moves Windows-visible axes/buttons; replay/fake input is not final proof."
    - "DESK-05: Real Windows HID output report maps to Android phone haptic and user confirms phone vibration."
    - "PACK-02: Evidence manifest is sanitized and links signing/install/rollback docs."
  artifacts:
    - path: "docs/windows/phase6-proof-checklist.md"
      provides: "Manual and automated Windows target proof sequence"
      contains: "joy.cpl"
    - path: "docs/evidence/manifests/phase6-windows-virtual-joystick.jsonl"
      provides: "Sanitized Phase 6 proof manifest"
      contains: "phase6-joy-cpl-visible"
    - path: "windows/btgun-vjoy/tools/proof/Collect-BtGunVJoyEvidence.ps1"
      provides: "CLI evidence collection helper"
      contains: "Get-PnpDevice"
  key_links:
    - from: "CI artifact"
      to: "192.168.1.100"
      via: "approval-gated install scripts"
      pattern: "Install-BtGunVJoy"
    - from: "Windows HID output report"
      to: "Android phone haptic"
      via: "WindowsBackendRuntime -> ControlServer.sendHapticCommand"
      pattern: "phase6-hid-output-report-phone-haptic"
---

<objective>
Prove the Windows virtual joystick path on the real target.

Purpose: Close Phase 6 with layered Windows evidence, mandatory `joy.cpl`, live Android/gun input, and real HID output-to-phone haptic proof.
Output: Proof checklist, evidence scripts, sanitized manifest, and human verification gates.
</objective>

<execution_context>
@/Users/lucas.rancez/.codex/gsd-core/workflows/execute-plan.md
@/Users/lucas.rancez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-windows-virtual-joystick-path/06-CONTEXT.md
@.planning/phases/06-windows-virtual-joystick-path/06-VALIDATION.md
@.planning/phases/06-windows-virtual-joystick-path/06-04-SUMMARY.md
@.planning/phases/06-windows-virtual-joystick-path/06-05-SUMMARY.md
@docs/windows/test-signing-and-install.md
@docs/windows/virtual-hid-strategy.md
@windows/btgun-vjoy/package/Install-BtGunVJoy.ps1
@windows/btgun-vjoy/package/Rollback-BtGunVJoy.ps1

<interfaces>
Target host per D-20: `192.168.1.100`, Windows 11 Education `10.0.22000` x64, admin-capable SSH, Java 17 available.
Final proof ids: `phase6-pnp-hid-cli`, `phase6-joy-cpl-visible`, `phase6-live-android-gun-input`, `phase6-hid-output-report-phone-haptic`, `phase6-redaction-scan`.
</interfaces>
</context>

## Artifacts this phase produces

- Doc: `docs/windows/phase6-proof-checklist.md`
- Manifest: `docs/evidence/manifests/phase6-windows-virtual-joystick.jsonl`
- Script: `windows/btgun-vjoy/tools/proof/Collect-BtGunVJoyEvidence.ps1`
- Script: `windows/btgun-vjoy/tools/proof/Redact-BtGunVJoyEvidence.ps1`
- Evidence id: `phase6-pnp-hid-cli`
- Evidence id: `phase6-joy-cpl-visible`
- Evidence id: `phase6-live-android-gun-input`
- Evidence id: `phase6-hid-output-report-phone-haptic`
- Evidence id: `phase6-redaction-scan`
- Fallback command: `btgun-hid-output-sender.exe --strength 192 --duration-ms 120 --ttl-ms 500`

<tasks>

<task type="auto">
  <name>Task 1: Proof checklist, collectors, and sanitized manifest scaffold</name>
  <files>docs/windows/phase6-proof-checklist.md, docs/evidence/manifests/phase6-windows-virtual-joystick.jsonl, windows/btgun-vjoy/tools/proof/Collect-BtGunVJoyEvidence.ps1, windows/btgun-vjoy/tools/proof/Redact-BtGunVJoyEvidence.ps1</files>
  <read_first>
    @.planning/phases/06-windows-virtual-joystick-path/06-CONTEXT.md
    @.planning/phases/06-windows-virtual-joystick-path/06-VALIDATION.md
    @docs/windows/test-signing-and-install.md
    @docs/evidence/manifests/phase5-desktop-backend-smoke.jsonl
  </read_first>
  <action>
    Create Phase 6 proof checklist and evidence helpers. Checklist must require D-07 layered CLI/PnP/HID evidence, D-08 `joy.cpl`, D-09 live Android/gun stream for final input, D-10 agent evidence plus user confirmation, D-16 real HID output report, D-17 `joy.cpl` output attempt first, D-18 fallback sender only after documenting `joy.cpl` output limitation, D-19 user phone-vibration confirmation, and D-20 target host `192.168.1.100`. Manifest scaffold must include initial rows for all proof ids and redaction status. Scripts must collect PnP/HID/game-controller CLI evidence and redact private keys, cert passwords, QR secrets, proof values, stream keys, Bluetooth addresses, device ids, and screenshots.
  </action>
  <verify>
    <automated>rg -n "phase6-pnp-hid-cli|phase6-joy-cpl-visible|phase6-live-android-gun-input|phase6-hid-output-report-phone-haptic|phase6-redaction-scan|joy.cpl|btgun-hid-output-sender|USER APPROVAL REQUIRED" docs/windows/phase6-proof-checklist.md docs/evidence/manifests/phase6-windows-virtual-joystick.jsonl windows/btgun-vjoy/tools/proof</automated>
  </verify>
  <acceptance_criteria>
    - Checklist states replay/fake input cannot satisfy final D-09 proof.
    - Manifest rows start as sanitized pending records until actual target evidence exists.
    - Collectors do not require WDK, Visual Studio, MSBuild, Git, `signtool`, `inf2cat`, or `devcon` on `192.168.1.100` per D-04 and D-21.
  </acceptance_criteria>
  <done>Proof materials exist before any target change is requested.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Approval gate for Windows target changes</name>
  <files>.planning/phases/06-windows-virtual-joystick-path/06-06-SUMMARY.md, docs/windows/phase6-proof-checklist.md</files>
  <read_first>
    @docs/windows/test-signing-and-install.md
    @docs/windows/phase6-proof-checklist.md
    @.planning/phases/06-windows-virtual-joystick-path/06-CONTEXT.md
  </read_first>
  <action>
    Present exact target commands before execution and wait for explicit user approval per D-03. Approval must cover any `bcdedit` testsigning change, reboot, test certificate import, driver install, devnode creation, and rollback/removal on `192.168.1.100`. Do not install WDK, Visual Studio, MSBuild, Git, or build tools on the target per D-04. D-22 says Secure Boot and VBS/HVCI were observed disabled, but any future boot/signing setting change still needs approval.
  </action>
  <verify>
    <automated>MISSING - human approval gate must record approved command list in `06-06-SUMMARY.md` before target actions.</automated>
  </verify>
  <acceptance_criteria>
    - User approves exact commands or execution stops.
    - Approval record includes host `192.168.1.100`, command text, expected effect, and reversal path.
    - No boot/signing/reboot/install/removal command runs before approval.
  </acceptance_criteria>
  <how-to-verify>
    1. Review exact command list shown by executor.
    2. Confirm commands match docs and do not install toolchains.
    3. Reply `approved` or list changes.
  </how-to-verify>
  <resume-signal>Type `approved` for the exact commands or describe required edits.</resume-signal>
  <done>Blocking approval obtained and recorded, or plan stops without target changes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final Windows joystick and haptic proof</name>
  <files>docs/evidence/manifests/phase6-windows-virtual-joystick.jsonl, .planning/phases/06-windows-virtual-joystick-path/06-06-SUMMARY.md</files>
  <read_first>
    @docs/windows/phase6-proof-checklist.md
    @docs/windows/test-signing-and-install.md
    @windows/btgun-vjoy/tools/proof/Collect-BtGunVJoyEvidence.ps1
    @windows/btgun-vjoy/tools/proof/Redact-BtGunVJoyEvidence.ps1
  </read_first>
  <action>
    After Task 2 approval, install the CI-built artifact on `192.168.1.100`, collect CLI/PnP/HID evidence, open `control joy.cpl`, capture agent visual evidence that Windows Game Controllers lists `BT Gun VJoy`, pair live Android/gun stream to desktop companion with `btgun.windows.driver.enabled=true`, move trigger/buttons/stick/aim and verify Windows-visible controls move, try `joy.cpl` output/rumble first, then run `btgun-hid-output-sender.exe --strength 192 --duration-ms 120 --ttl-ms 500` only if `joy.cpl` cannot send output and that limitation is documented. Route output through real driver callback to `ControlServer.sendHapticCommand`; get user confirmation that Android phone physically vibrated. Update manifest rows to pass/fail with sanitized evidence refs only.
  </action>
  <verify>
    <automated>rg -n "\"status\":\"pass\"|phase6-pnp-hid-cli|phase6-joy-cpl-visible|phase6-live-android-gun-input|phase6-hid-output-report-phone-haptic|phase6-redaction-scan" docs/evidence/manifests/phase6-windows-virtual-joystick.jsonl</automated>
    <automated>rg -n "qr_secret|manual code|proof|stream key|HMAC key|private key|Bluetooth address|device id|screenshot" docs/evidence/manifests/phase6-windows-virtual-joystick.jsonl || true</automated>
  </verify>
  <acceptance_criteria>
    - `phase6-pnp-hid-cli` row passes with PnP/HID CLI evidence.
    - `phase6-joy-cpl-visible` row passes only after `joy.cpl` visual evidence and user confirmation.
    - `phase6-live-android-gun-input` row passes only with live Android/gun movement, not replay/fake input.
    - `phase6-hid-output-report-phone-haptic` row passes only with Windows-side real HID output report evidence and user phone-vibration confirmation.
    - Redaction scan row passes with no secret/private/device-id/screenshot leaks.
  </acceptance_criteria>
  <how-to-verify>
    1. Confirm `joy.cpl` shows `BT Gun VJoy`.
    2. Move live iPega gun/Android aim/buttons and confirm Windows-visible axes/buttons move.
    3. Confirm Android phone vibrates after real HID output report path.
    4. Confirm manifest contains pass rows and no sensitive evidence.
  </how-to-verify>
  <resume-signal>Type `approved` after visual/input/haptic proof passes, or describe the failing proof row.</resume-signal>
  <done>Phase 6 final Windows proof rows pass and user confirms `joy.cpl`, live input, and phone vibration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator -> Windows target | Approved commands can change driver signing/install state. |
| Windows target -> evidence manifest | CLI/visual/manual proof becomes committed evidence. |
| HID output -> Android phone | Windows output report causes physical phone vibration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-19 | Elevation of Privilege | Test-signing/install operations | mitigate | Blocking approval before `bcdedit`, reboot, cert import, install, devnode creation, or rollback per D-03. |
| T-06-20 | Spoofing | Final input proof | mitigate | Require live paired Android/gun stream and reject replay/fake paths for final D-09 proof. |
| T-06-21 | Tampering | Haptic proof | mitigate | Require real Windows HID output report evidence, `joy.cpl` attempt first, fallback sender documented, and user phone-vibration confirmation per D-16 through D-19. |
| T-06-22 | Information Disclosure | Evidence manifest | mitigate | Redact secrets, private keys, cert passwords, QR/proof/session data, Bluetooth addresses, device ids, and screenshots before commit. |
| T-06-SC | Tampering | Target artifact | mitigate | Use named CI artifact from Plan 05 and record package hash/metadata in summary. |
</threat_model>

<verification>
Final phase proof requires:
- PnP/HID CLI evidence.
- Mandatory `joy.cpl` visual/user confirmation.
- Live Android/gun input movement.
- Real HID output report to Android phone haptic.
- Sanitized manifest and redaction scan.
</verification>

<success_criteria>
- Windows 11 x64 sees `BT Gun VJoy` as a gamepad-style joystick in `joy.cpl`.
- Live Android/gun stream drives Windows-visible controls.
- Real Windows HID output report triggers Android phone vibration through authenticated control.
- PACK-02 docs and proof manifest are complete and sanitized.
</success_criteria>

<output>
Create `.planning/phases/06-windows-virtual-joystick-path/06-06-SUMMARY.md` when done.
</output>
