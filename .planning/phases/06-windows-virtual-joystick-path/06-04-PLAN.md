---
phase: 06-windows-virtual-joystick-path
plan: 04
type: execute
wave: 4
depends_on: [06-03]
files_modified:
  - desktop-companion/build.gradle.kts
  - desktop-companion/src/main/kotlin/com/btgun/desktop/Main.kt
  - desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt
  - desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsBackendRuntime.kt
  - desktop-companion/src/main/kotlin/com/btgun/desktop/smoke/WindowsVhfBackendSmokeMain.kt
  - desktop-companion/src/test/kotlin/com/btgun/desktop/backend/windows/WindowsBackendRuntimeTest.kt
autonomous: true
requirements: [DESK-02, DESK-05]
requirements_addressed: [DESK-02, DESK-05]
must_haves:
  truths:
    - "DESK-02: Live trusted UDP input can publish through the Windows VHF backend path."
    - "DESK-05: Windows output reports can route through ControlServer.sendHapticCommand to Android phone haptics."
    - "D-09: Replay and fake paths remain tests/debug only; final proof still requires live Android/gun stream."
  artifacts:
    - path: "desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsBackendRuntime.kt"
      provides: "ControlServer to Windows backend runtime wiring"
      exports: ["WindowsBackendRuntime", "WindowsBackendRuntimeConfig"]
    - path: "desktop-companion/src/main/kotlin/com/btgun/desktop/smoke/WindowsVhfBackendSmokeMain.kt"
      provides: "Windows real backend smoke entrypoint"
      exports: ["main"]
  key_links:
    - from: "ControlServer.onUdpInputReceived"
      to: "WindowsVirtualControllerBackend.publish"
      via: "WindowsBackendRuntime"
      pattern: "onUdpInputReceived"
    - from: "WindowsVirtualControllerBackend.drainOutputHaptics"
      to: "ControlServer.sendHapticCommand"
      via: "WindowsBackendRuntime"
      pattern: "sendHapticCommand"
---

<objective>
Wire the Windows backend into the desktop companion live session path.

Purpose: Make the existing LAN/control owner publish trusted input to the driver and send real output reports back through authenticated phone haptics.
Output: Runtime attachment, launch flag, and Windows VHF smoke entrypoint.
</objective>

<execution_context>
@/Users/lucas.rancez/.codex/gsd-core/workflows/execute-plan.md
@/Users/lucas.rancez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-windows-virtual-joystick-path/06-CONTEXT.md
@.planning/phases/06-windows-virtual-joystick-path/06-VALIDATION.md
@.planning/phases/06-windows-virtual-joystick-path/06-03-SUMMARY.md
@desktop-companion/src/main/kotlin/com/btgun/desktop/Main.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/control/ControlServer.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/backend/UdpControllerStateAdapter.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsVirtualControllerBackend.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/smoke/WindowsBackendSmokeMain.kt

<interfaces>
`ControlServer.onUdpInputReceived` receives trusted `UdpReceivedInput`.
`ControlServer.sendHapticCommand(command)` sends authenticated reliable haptic command and returns `HapticSendResult`.
`UdpControllerStateAdapter.toState(input)` maps receiver input to `SemanticControllerState`.
</interfaces>
</context>

## Artifacts this phase produces

- File: `desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsBackendRuntime.kt`
- Symbol: `data class WindowsBackendRuntimeConfig`
- Symbol: `class WindowsBackendRuntime`
- Function: `WindowsBackendRuntime.attach(controlServer: ControlServer)`
- Function: `WindowsBackendRuntime.close()`
- CLI/system property: `btgun.windows.driver.enabled=true`
- CLI/system property: `btgun.windows.driver.bridge.path=<path-to-btgun-driver-bridge.exe>`
- Gradle task: `smokeDesktopBackendWindowsVhf`
- Main class: `com.btgun.desktop.smoke.WindowsVhfBackendSmokeMainKt`
- Test class: `WindowsBackendRuntimeTestKt`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Runtime tests for live input and output haptic routing</name>
  <files>desktop-companion/build.gradle.kts, desktop-companion/src/test/kotlin/com/btgun/desktop/backend/windows/WindowsBackendRuntimeTest.kt</files>
  <read_first>
    @desktop-companion/build.gradle.kts
    @desktop-companion/src/main/kotlin/com/btgun/desktop/control/ControlServer.kt
    @desktop-companion/src/test/kotlin/com/btgun/desktop/control/ControlChannelTest.kt
    @desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsVirtualControllerBackend.kt
  </read_first>
  <behavior>
    - Runtime receives trusted UDP callback, converts with `UdpControllerStateAdapter.toState`, and publishes through `WindowsVirtualControllerBackend`.
    - Runtime observes backend output report haptic commands and calls `ControlServer.sendHapticCommand`.
    - If stream input is stale, published report follows D-15 behavior from Plan 01.
    - If no active Android control session exists, haptic send result is recorded as no-session without bypass.
  </behavior>
  <action>
    Add failing tests using fake `ControlServer` session setup and fake Windows backend/bridge. Register `WindowsBackendRuntimeTestKt`. Tests must assert D-11 user-mode companion owns UDP/control, D-12 driver owns only reports, D-15 stale behavior is preserved, and D-16 output path uses real output report bytes as the command source.
  </action>
  <verify>
    <automated>cd desktop-companion &amp;&amp; GRADLE_USER_HOME=/private/tmp/bt-gun-gradle gradle test --offline --no-daemon --console=plain</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before `WindowsBackendRuntime` exists.
    - Tests do not use Phase 5 simulated output as final proof substitute.
    - Tests do not require Windows target or installed driver.
  </acceptance_criteria>
  <done>Failing runtime tests define the live Windows companion path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Runtime wiring and Windows VHF launch flag</name>
  <files>desktop-companion/src/main/kotlin/com/btgun/desktop/Main.kt, desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt, desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsBackendRuntime.kt</files>
  <read_first>
    @desktop-companion/src/main/kotlin/com/btgun/desktop/Main.kt
    @desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt
    @desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsVirtualControllerBackend.kt
  </read_first>
  <action>
    Implement `WindowsBackendRuntime` and inject it from `Main.kt` only when `-Dbtgun.windows.driver.enabled=true` and `-Dbtgun.windows.driver.bridge.path` is set. Attach runtime to `ControlServer.onUdpInputReceived` without replacing existing UI/session callbacks. Add concise UI diagnostic text in `PairingWindow` for Windows backend lifecycle, last publish result, stale state, and last haptic send result. Do not add Phase 8 profile controls, visualizer UI, or game-specific mapping per D-14.
  </action>
  <verify>
    <automated>cd desktop-companion &amp;&amp; GRADLE_USER_HOME=/private/tmp/bt-gun-gradle gradle test --offline --no-daemon --console=plain</automated>
  </verify>
  <acceptance_criteria>
    - Default desktop launch behavior is unchanged when `btgun.windows.driver.enabled` is absent.
    - Windows enabled launch requires explicit bridge path and fails closed with visible diagnostic if missing.
    - `PairingWindow` still starts/stops `ControlServer`; backend runtime does not own pairing/auth/session lifecycle.
  </acceptance_criteria>
  <done>Desktop companion can route live trusted input to Windows backend when enabled.</done>
</task>

<task type="auto">
  <name>Task 3: Windows VHF smoke entrypoint</name>
  <files>desktop-companion/build.gradle.kts, desktop-companion/src/main/kotlin/com/btgun/desktop/smoke/WindowsVhfBackendSmokeMain.kt</files>
  <read_first>
    @desktop-companion/build.gradle.kts
    @desktop-companion/src/main/kotlin/com/btgun/desktop/smoke/WindowsBackendSmokeMain.kt
    @desktop-companion/src/main/kotlin/com/btgun/desktop/smoke/BackendSmokeRunner.kt
    @desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsVirtualControllerBackend.kt
  </read_first>
  <action>
    Add Gradle task `smokeDesktopBackendWindowsVhf` with main class `com.btgun.desktop.smoke.WindowsVhfBackendSmokeMainKt`. The entrypoint must require `btgun.windows.driver.bridge.path`, run only the real `windows-vhf` backend, write JUnit-style XML to `build/test-results/btgun-smoke/windows-vhf/TEST-btgun-windows-vhf.xml`, and include a case that publishes Plan 04 fixture input through the real backend. Keep replay smoke labelled debug/test only; final D-09 proof remains Plan 06 live Android/gun.
  </action>
  <verify>
    <automated>cd desktop-companion &amp;&amp; GRADLE_USER_HOME=/private/tmp/bt-gun-gradle gradle test --offline --no-daemon --console=plain</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "smokeDesktopBackendWindowsVhf|WindowsVhfBackendSmokeMainKt|TEST-btgun-windows-vhf.xml" desktop-companion/build.gradle.kts desktop-companion/src/main/kotlin/com/btgun/desktop/smoke` finds all expected hooks.
    - Smoke XML never contains fixture bytes, QR data, proof values, stream keys, or private key material.
    - Existing `smokeDesktopBackendWindowsStub` remains available and clearly distinct from real `windows-vhf`.
  </acceptance_criteria>
  <done>Windows real-backend smoke command is ready for target/CI artifact use.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trusted UDP callback -> backend publish | Authenticated input enters Windows backend runtime. |
| Backend output -> ControlServer haptic | Windows output report maps to Android phone haptic over authenticated control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Tampering | `WindowsBackendRuntime` | mitigate | Accept only `ControlServer` trusted callbacks; no direct UDP socket or unauthenticated input path. |
| T-06-12 | Repudiation | Runtime diagnostics | mitigate | Record last publish and haptic send result without storing secrets or raw packets. |
| T-06-13 | Tampering | Stale stream handling | mitigate | Preserve Phase 4/5 stale behavior: clear buttons/stick, keep aim, expose stale diagnostic per D-15. |
| T-06-14 | Information Disclosure | Smoke XML | mitigate | XML contains case names/results/timing only, no fixtures or secret material. |
| T-06-SC | Tampering | Package installs | accept | No dependency install in this plan. |
</threat_model>

<verification>
Run local companion tests:
`cd desktop-companion && GRADLE_USER_HOME=/private/tmp/bt-gun-gradle gradle test --offline --no-daemon --console=plain`
</verification>

<success_criteria>
- Live-session runtime can publish trusted input to real Windows backend when enabled.
- Real Windows output reports are routed to `ControlServer.sendHapticCommand`.
- Replay smoke path exists but cannot satisfy final live proof by itself.
</success_criteria>

<output>
Create `.planning/phases/06-windows-virtual-joystick-path/06-04-SUMMARY.md` when done.
</output>
