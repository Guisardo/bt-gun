---
phase: 06-windows-virtual-joystick-path
plan: 03
type: tdd
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - desktop-companion/build.gradle.kts
  - desktop-companion/src/main/kotlin/com/btgun/desktop/backend/BackendCapabilities.kt
  - desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsDriverBridge.kt
  - desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsVirtualControllerBackend.kt
  - desktop-companion/src/test/kotlin/com/btgun/desktop/backend/windows/WindowsVirtualControllerBackendTest.kt
autonomous: true
requirements: [DESK-02, DESK-05]
requirements_addressed: [DESK-02, DESK-05]
must_haves:
  truths:
    - "DESK-02: Windows backend publishes packed semantic input reports to the VHF driver bridge."
    - "DESK-05: Windows backend can read real output report bytes and map them to HapticCommand."
    - "D-11 and D-12: Desktop companion owns mapping and session security; driver remains a small report bridge."
  artifacts:
    - path: "desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsDriverBridge.kt"
      provides: "Kotlin user-mode bridge client"
      exports: ["WindowsDriverBridge", "WindowsDriverBridgeConfig", "WindowsDriverBridgeStatus"]
    - path: "desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsVirtualControllerBackend.kt"
      provides: "VirtualControllerBackend implementation for real Windows VHF path"
      exports: ["WindowsVirtualControllerBackend"]
  key_links:
    - from: "WindowsVirtualControllerBackend.publish"
      to: "WindowsDriverBridge.submitInputReport"
      via: "WindowsHidReportPacker.packInputReport"
      pattern: "packInputReport"
    - from: "WindowsVirtualControllerBackend.drainOutputHaptics"
      to: "WindowsOutputReportMapper.toHapticCommand"
      via: "read output report bytes"
      pattern: "toHapticCommand"
---

<objective>
Create the desktop companion Windows backend that talks to the driver bridge.

Purpose: Connect Phase 5 backend contract to Phase 6 VHF driver without moving LAN or auth into kernel.
Output: Tested Kotlin `VirtualControllerBackend` for Windows VHF and bridge status/output handling.
</objective>

<execution_context>
@/Users/lucas.rancez/.codex/gsd-core/workflows/execute-plan.md
@/Users/lucas.rancez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-windows-virtual-joystick-path/06-CONTEXT.md
@.planning/phases/06-windows-virtual-joystick-path/06-PATTERNS.md
@.planning/phases/06-windows-virtual-joystick-path/06-01-SUMMARY.md
@.planning/phases/06-windows-virtual-joystick-path/06-02-SUMMARY.md
@desktop-companion/src/main/kotlin/com/btgun/desktop/backend/VirtualControllerBackend.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/backend/StubVirtualControllerBackend.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/backend/BackendCapabilities.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsHidReportPacker.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsOutputReportMapper.kt

<interfaces>
`VirtualControllerBackend` requires descriptor, capabilities, lifecycleState, currentState, lastPublishResult, start(), publish(), simulateOutputReport(), and stop().
Bridge helper line protocol from Plan 02: SUBMIT_INPUT hex -> OK or ERR; READ_OUTPUT -> OUTPUT hex or OK; STATUS -> STATUS json.
</interfaces>
</context>

## Artifacts this phase produces

- File: `desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsDriverBridge.kt`
- Symbol: `data class WindowsDriverBridgeConfig`
- Symbol: `data class WindowsDriverBridgeStatus`
- Symbol: `sealed interface WindowsDriverBridgeResult`
- Symbol: `class WindowsDriverBridge`
- Function: `WindowsDriverBridge.submitInputReport(report: WindowsInputReport): WindowsDriverBridgeResult`
- Function: `WindowsDriverBridge.readOutputReport(): ByteArray?`
- Function: `WindowsDriverBridge.readStatus(): WindowsDriverBridgeStatus`
- File: `desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsVirtualControllerBackend.kt`
- Symbol: `class WindowsVirtualControllerBackend : VirtualControllerBackend`
- Function: `WindowsVirtualControllerBackend.drainOutputHaptics(nowElapsedNanos: Long): List<HapticCommand>`
- Function: `BackendCapabilityPresets.windowsVhf()`
- Test class: `WindowsVirtualControllerBackendTestKt`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for Windows backend bridge lifecycle</name>
  <files>desktop-companion/build.gradle.kts, desktop-companion/src/test/kotlin/com/btgun/desktop/backend/windows/WindowsVirtualControllerBackendTest.kt</files>
  <read_first>
    @desktop-companion/build.gradle.kts
    @desktop-companion/src/main/kotlin/com/btgun/desktop/backend/VirtualControllerBackend.kt
    @desktop-companion/src/main/kotlin/com/btgun/desktop/backend/StubVirtualControllerBackend.kt
    @desktop-companion/src/test/kotlin/com/btgun/desktop/backend/BackendCapabilitiesTest.kt
  </read_first>
  <behavior>
    - Publish before start returns `BackendPublishResult.Rejected`.
    - Start then publish packs state through `WindowsHidReportPacker` and sends the exact report bytes to a fake bridge.
    - Bridge submit error returns rejected publish result and records `lastPublishResult`.
    - Output report bytes read from fake bridge map to `HapticCommand` through `WindowsOutputReportMapper`.
    - Capabilities platform is `windows-vhf`, haptics outputReport is true, and limitations do not claim Phase 5 stub behavior.
  </behavior>
  <action>
    Add failing tests using an injected fake bridge interface, not an actual Windows device. Register `WindowsVirtualControllerBackendTestKt` in `desktop-companion/build.gradle.kts`. Tests must prove D-11 desktop companion mapping, D-12 driver boundary, D-15 stale behavior via Plan 01 packer, and D-16 output report mapping without using Phase 5 simulated output as final proof.
  </action>
  <verify>
    <automated>cd desktop-companion &amp;&amp; GRADLE_USER_HOME=/private/tmp/bt-gun-gradle gradle test --offline --no-daemon --console=plain</automated>
    <expected_failure>RED fails only on missing `WindowsDriverBridge`, `WindowsVirtualControllerBackend`, or `windowsVhf` symbols.</expected_failure>
  </verify>
  <acceptance_criteria>
    - RED tests cover lifecycle, bridge submit, output read, haptic mapping, and capability flags.
    - Tests use fake bridge bytes and do not need Windows target, WDK, or installed driver.
    - Test names cite fixed mapping and real output report requirements from D-13 and D-16.
  </acceptance_criteria>
  <done>RED gate commit exists for Windows backend bridge behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN Windows bridge and backend implementation</name>
  <files>desktop-companion/src/main/kotlin/com/btgun/desktop/backend/BackendCapabilities.kt, desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsDriverBridge.kt, desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsVirtualControllerBackend.kt</files>
  <read_first>
    @desktop-companion/src/main/kotlin/com/btgun/desktop/backend/BackendCapabilities.kt
    @desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsHidReportPacker.kt
    @desktop-companion/src/main/kotlin/com/btgun/desktop/backend/windows/WindowsOutputReportMapper.kt
    @windows/btgun-vjoy/include/BtGunVJoyIoctl.h
  </read_first>
  <action>
    Implement a small Kotlin bridge client around the Plan 02 helper line protocol and a `WindowsVirtualControllerBackend` implementing `VirtualControllerBackend`. `publish` must use Plan 01 `packInputReport`; `drainOutputHaptics` must read queued output bytes and call `WindowsOutputReportMapper.toHapticCommand` with `windows-output-report-&lt;counter&gt;`. Add `BackendCapabilityPresets.windowsVhf()` with platform `windows-vhf`, outputReport true, phoneHaptic true, pattern false, and explicit limitation text for pattern-only unsupported output. Keep `simulateOutputReport` available for interface compatibility but mark it test-only by returning mapper output only from `SimulatedOutputReport` conversion; final proof still requires real HID output per D-16.
  </action>
  <verify>
    <automated>cd desktop-companion &amp;&amp; GRADLE_USER_HOME=/private/tmp/bt-gun-gradle gradle test --offline --no-daemon --console=plain</automated>
  </verify>
  <acceptance_criteria>
    - All desktop companion tests pass.
    - `WindowsVirtualControllerBackend` never opens sockets, parses LAN packets, handles pairing, or stores secrets.
    - Bridge stderr/stdout handling never logs raw pairing proof, QR secret, stream key, private key, or Windows cert material.
  </acceptance_criteria>
  <done>GREEN gate commit exists; backend is ready for control-server wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Companion -> helper process | Kotlin sends report hex to Windows helper stdout/stdin boundary. |
| Helper output -> HapticCommand | Helper output bytes become phone haptic command candidates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Tampering | `WindowsDriverBridge` | mitigate | Parse only exact response tokens, reject bad hex/length, and expose errors as rejected publish results. |
| T-06-09 | Information Disclosure | Bridge process logs | mitigate | Never include LAN/session secrets, private keys, QR material, or raw proof values in exception/detail strings. |
| T-06-10 | Tampering | `WindowsVirtualControllerBackend` | mitigate | Use Plan 01 packer/mapper only; no direct byte construction outside tested helpers. |
| T-06-SC | Tampering | Package installs | accept | No new dependency install; Kotlin code uses JDK process APIs already available. |
</threat_model>

<verification>
Run:
`cd desktop-companion && GRADLE_USER_HOME=/private/tmp/bt-gun-gradle gradle test --offline --no-daemon --console=plain`
</verification>

<success_criteria>
- RED and GREEN commits exist.
- Windows real backend satisfies Phase 5 `VirtualControllerBackend`.
- Output-report haptics are mapped but not sent until authenticated `ControlServer` wiring in Plan 04.
</success_criteria>

<output>
Create `.planning/phases/06-windows-virtual-joystick-path/06-03-SUMMARY.md` when done.
</output>
