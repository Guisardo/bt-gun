---
phase: 07-macos-virtual-joystick-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android-host/app/build.gradle.kts
  - android-host/app/src/main/java/com/btgun/host/permissions/AndroidHidCapability.kt
  - android-host/app/src/main/java/com/btgun/host/permissions/HostCapabilityProbe.kt
  - android-host/app/src/main/java/com/btgun/host/permissions/PermissionGate.kt
  - android-host/app/src/test/java/com/btgun/host/permissions/AndroidHidCapabilityTest.kt
  - android-host/app/src/test/java/com/btgun/host/permissions/PermissionGateTest.java
  - docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl
autonomous: true
requirements:
  - ANDR-09
  - PACK-06
must_haves:
  truths:
    - "Android reports Bluetooth HID role readiness before user starts HID mode per D-01."
    - "Blocked matrix includes Bluetooth off, missing permission, HID_DEVICE proxy unavailable, registration failed, no host connected, and host disconnected per D-02."
    - "Evidence manifest exists for sanitized Android HID proof rows without claiming live proof yet per D-04."
  artifacts:
    - path: "android-host/app/src/main/java/com/btgun/host/permissions/AndroidHidCapability.kt"
      provides: "HID role status model and blocked-state labels"
    - path: "android-host/app/src/test/java/com/btgun/host/permissions/AndroidHidCapabilityTest.kt"
      provides: "Pure capability matrix tests"
    - path: "docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl"
      provides: "Sanitized manifest target for later live rows"
  key_links:
    - from: "HostCapabilityProbe.kt"
      to: "PermissionGate.kt"
      via: "Android HID capability input fields"
      pattern: "hid.*Capability|bluetoothHid"
    - from: "PermissionGateTest.java"
      to: "AndroidHidCapability.kt"
      via: "blocked-state assertions"
      pattern: "HID_DEVICE|host disconnected|registration failed"
---

<objective>
Create the Android HID capability foundation and the local test gate for Phase 7.

Purpose: `BluetoothHidDevice` support is phone/OEM dependent, so Phase 7 must show honest readiness and blocked states before any macOS proof can be trusted.

Output: Gradle startup gate, HID capability/status model, tests, and sanitized evidence manifest scaffold.
</objective>

## Phase Goal

**As a** user with the discontinued iPega AR gun, **I want to** use the Android phone as a Bluetooth HID gamepad for macOS Apple Silicon, **so that** macOS sees a normal controller without paid Apple virtual HID entitlements while the completed Windows VHF path remains fallback.

## Artifacts this phase produces

- Android HID capability model for `BluetoothHidDevice` role support.
- Capability tests for the full blocked-state matrix.
- Sanitized evidence manifest file for current-phone, alternate-phone, macOS input, output-report, unsupported, and fallback rows.

<execution_context>
@/Users/lucas.rancez/.codex/gsd-core/workflows/execute-plan.md
@/Users/lucas.rancez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-macos-virtual-joystick-path/07-CONTEXT.md
@.planning/phases/07-macos-virtual-joystick-path/07-RESEARCH.md
@.planning/phases/07-macos-virtual-joystick-path/07-PATTERNS.md
@.planning/phases/07-macos-virtual-joystick-path/07-VALIDATION.md
@android-host/app/build.gradle.kts
@android-host/app/src/main/java/com/btgun/host/permissions/HostCapabilityProbe.kt
@android-host/app/src/main/java/com/btgun/host/permissions/PermissionGate.kt
@android-host/app/src/test/java/com/btgun/host/permissions/PermissionGateTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove Android Gradle startup before relying on local tests</name>
  <files>android-host/app/build.gradle.kts, .planning/phases/07-macos-virtual-joystick-path/07-01-SUMMARY.md</files>
  <read_first>
    - `android-host/app/build.gradle.kts`
    - `.planning/phases/07-macos-virtual-joystick-path/07-RESEARCH.md`
    - `.planning/phases/07-macos-virtual-joystick-path/07-VALIDATION.md`
  </read_first>
  <action>Run the repaired local Gradle command using JDK 17, `ANDROID_HOME=/Users/lucas.rancez/Library/Android/sdk`, and `GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home` before marking any Android test pass. If startup still fails with `Failed to load native library 'libnative-platform.dylib' for Mac OS X aarch64`, stop execution and record the exact blocker in `07-01-SUMMARY.md`; do not claim automated test success from stale build output. Register only test classes created by this plan in `android-host/app/build.gradle.kts`; later plans register their own tests.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=/Users/lucas.rancez/Library/Android/sdk GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p android-host testDebugUnitTest --no-daemon --console=plain -Dorg.gradle.java.home=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home -Dkotlin.compiler.execution.strategy=in-process</automated>
  </verify>
  <acceptance_criteria>Command starts Gradle and reaches Android test execution, or summary records the native-platform startup blocker as the plan blocker before code-dependent verification proceeds.</acceptance_criteria>
  <done>Local Gradle status is trustworthy and no test pass is claimed while the startup blocker persists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Android HID capability matrix</name>
  <files>android-host/app/src/main/java/com/btgun/host/permissions/AndroidHidCapability.kt, android-host/app/src/main/java/com/btgun/host/permissions/HostCapabilityProbe.kt, android-host/app/src/main/java/com/btgun/host/permissions/PermissionGate.kt, android-host/app/src/test/java/com/btgun/host/permissions/AndroidHidCapabilityTest.kt, android-host/app/src/test/java/com/btgun/host/permissions/PermissionGateTest.java, android-host/app/build.gradle.kts</files>
  <read_first>
    - `android-host/app/src/main/java/com/btgun/host/permissions/HostCapabilityProbe.kt`
    - `android-host/app/src/main/java/com/btgun/host/permissions/PermissionGate.kt`
    - `android-host/app/src/test/java/com/btgun/host/permissions/PermissionGateTest.java`
    - `.planning/phases/07-macos-virtual-joystick-path/07-CONTEXT.md`
  </read_first>
  <behavior>
    - Test Bluetooth off -> HID role row says blocked with Bluetooth off detail.
    - Test missing `BLUETOOTH_CONNECT` on Android 12+ -> HID role row says permission blocked.
    - Test not-yet-probed state -> HID role row says explicit Start Bluetooth gamepad action is needed per D-01.
    - Test proxy unavailable, registration failed, no host connected, and host disconnected -> each has distinct label/detail per D-02.
  </behavior>
  <action>Write tests first. Add `AndroidHidCapability` and related enum/status values in package `com.btgun.host.permissions`. Extend `PermissionGateInput` and `PermissionGateState` with HID role fields that can express Bluetooth off, missing `BLUETOOTH_CONNECT`, `HID_DEVICE` proxy unavailable, app registration failed, no host connected, host disconnected, and connected. Extend `HostCapabilityProbe.input(...)` only with synchronous checks it can perform safely; async `BluetoothProfile.HID_DEVICE` proxy and registration results must be accepted from the later adapter status rather than guessed. Preserve existing `canStartSession` behavior for BLE gun session; HID readiness is separate per D-01 and D-03.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=/Users/lucas.rancez/Library/Android/sdk GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p android-host testDebugUnitTest --tests '*AndroidHidCapability*' --tests '*PermissionGate*' --no-daemon --console=plain</automated>
  </verify>
  <acceptance_criteria>`AndroidHidCapabilityTestKt` and `PermissionGateTest` prove all D-02 blocked rows and show the BLE live-session gate remains unchanged.</acceptance_criteria>
  <done>Android HID capability state is explicit, tested, and separate from the existing gun/LAN permission gate.</done>
</task>

<task type="auto">
  <name>Task 3: Create sanitized Phase 7 Android HID evidence manifest target</name>
  <files>docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl</files>
  <read_first>
    - `.planning/phases/07-macos-virtual-joystick-path/07-CONTEXT.md`
    - `.planning/phases/07-macos-virtual-joystick-path/07-VALIDATION.md`
    - `.planning/phases/04-input-stream-and-haptic-transport/04-CONTEXT.md`
  </read_first>
  <action>Create `docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl` with a schema row only. Define accepted `record_type` names in that row: `phase7-android-hid-proxy`, `phase7-android-hid-register-app`, `phase7-android-hid-pairing-window`, `phase7-macos-bluetooth-paired`, `phase7-gamecontroller-input`, `phase7-hid-output-callback`, `phase7-macos-output-unsupported`, `phase7-alternate-phone-tested`, and `phase7-windows-vhf-fallback-selected`. Do not invent pass/fail rows. Include redaction policy fields that forbid Bluetooth addresses, phone serials, stable device identifiers, account names, pairing material, stream secrets, keys, and screenshots with sensitive nearby-device names per D-16 and D-19.</action>
  <verify>
    <automated>test -f docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl && rg -n 'phase7-android-hid-proxy|phase7-gamecontroller-input|phase7-hid-output-callback|phase7-alternate-phone-tested|phase7-windows-vhf-fallback-selected' docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl</automated>
  </verify>
  <acceptance_criteria>Manifest file exists, contains only schema/policy content, and names every later live-proof row without claiming current-phone or macOS success.</acceptance_criteria>
  <done>Evidence destination exists and is safe for later proof rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Android app -> Android Bluetooth stack | Capability and proxy status can fail by permission, Bluetooth state, or OEM support. |
| Local proof -> committed evidence | Manual rows can leak device identifiers or overclaim unsupported behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing | HID capability UI | mitigate | Use explicit statuses; never treat unprobed or proxy-unavailable as connected. |
| T-07-02 | Tampering | Evidence manifest | mitigate | Schema row only in this plan; later live rows must use sanitized status values. |
| T-07-03 | Repudiation | Main-style Android tests | mitigate | Register `AndroidHidCapabilityTestKt` in Gradle so `testDebugUnitTest` executes it. |
| T-07-04 | Information Disclosure | Evidence fields | mitigate | Redaction policy forbids Bluetooth MACs, serials, account names, keys, and screenshots. |
| T-07-SC | Tampering | Package installs | accept | No npm/pip/cargo installs in this plan. |
</threat_model>

<verification>
Run the Gradle startup gate first. Then run the focused Android tests. If local Gradle remains blocked, stop with a blocker summary rather than continuing with unverified claims.
</verification>

<success_criteria>
ANDR-09 capability states exist and are tested. PACK-06 evidence destination exists with redaction policy. No live macOS, output-report, or fallback proof is claimed.
</success_criteria>

<output>
Create `.planning/phases/07-macos-virtual-joystick-path/07-01-SUMMARY.md` when done.
</output>
