---
phase: 07-macos-virtual-joystick-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/setup/macos-virtual-hid.md
  - docs/evidence/manifests/phase7-macos-virtual-hid.jsonl
  - native/macos-hid-helper/Package.swift
  - native/macos-hid-helper/Sources/BtGunMacosHidHelper/main.swift
  - native/macos-hid-helper/Entitlements.plist
  - native/macos-hid-helper/scripts/probe-macos-hid-environment.sh
  - native/macos-hid-helper/scripts/build-corehid-helper.sh
autonomous: false
requirements: [DESK-03, DESK-06, PACK-03]
requirements_addressed: [DESK-03, DESK-06, PACK-03]
user_setup:
  - service: apple-xcode
    why: "CoreHID Swift helper and any DriverKit fallback require a matching Xcode or CLT SDK/toolchain."
    env_vars: []
    dashboard_config:
      - task: "Install or select matching full Xcode only if automated CoreHID/IOHIDUserDevice probes cannot compile with current CLT."
        location: "Xcode.app plus xcode-select"
  - service: apple-signing
    why: "CoreHID/IOHIDUserDevice virtual HID entitlement or DriverKit fallback may require a local signing identity/provisioning profile."
    env_vars: []
    dashboard_config:
      - task: "Provide signing identity/provisioning only if ad-hoc signing cannot use the required entitlement."
        location: "Apple Developer account / Keychain"
must_haves:
  truths:
    - "D-01: CoreHID HIDVirtualDevice is the first attempted macOS path."
    - "D-02: Switching away from CoreHID requires official docs or local compile/runtime proof."
    - "D-10/D-11/D-12/D-13: Development proof docs cover launch, signing, permission prompts, macOS 26.2 build 25C56 arm64, ad-hoc signing first, and fallback commands."
    - "DESK-03/DESK-06/PACK-03: Helper feasibility, output callback feasibility, and setup docs exist before backend claims macOS support."
  artifacts:
    - path: "docs/setup/macos-virtual-hid.md"
      provides: "macOS CoreHID/IOHIDUserDevice/DriverKit development proof setup"
      contains: "HIDVirtualDevice"
    - path: "native/macos-hid-helper/Sources/BtGunMacosHidHelper/main.swift"
      provides: "minimal CoreHID helper feasibility probe"
      contains: "HIDVirtualDevice"
    - path: "docs/evidence/manifests/phase7-macos-virtual-hid.jsonl"
      provides: "sanitized Phase 7 evidence scaffold"
      contains: "phase7-corehid-feasibility"
  key_links:
    - from: "native/macos-hid-helper/scripts/probe-macos-hid-environment.sh"
      to: "docs/setup/macos-virtual-hid.md"
      via: "recorded toolchain/signing/entitlement findings"
      pattern: "xcodebuild|swift|codesign|hidutil|ioreg"
    - from: "CoreHID feasibility gate"
      to: "DriverKit fallback gate"
      via: "manifest status rows"
      pattern: "corehid-(pass|blocked|failed)"
---

## Phase Goal

**As a** macOS desktop user, **I want to** use the live Android gun stream as a regular macOS gamepad-style joystick with honest output capability reporting, **so that** I can validate v1 input and phone haptics before later desktop UX work.

<objective>
Establish the macOS CoreHID-first development proof foundation.

Purpose: Resolve toolchain, signing, entitlement, and proof-command uncertainty before Kotlin backend code depends on a macOS helper.
Output: PACK-03 setup docs, sanitized evidence scaffold, minimal CoreHID helper probe, and explicit fallback gate records.
</objective>

<execution_context>
@/Users/lucas.rancez/.codex/gsd-core/workflows/execute-plan.md
@/Users/lucas.rancez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-macos-virtual-joystick-path/07-CONTEXT.md
@.planning/phases/07-macos-virtual-joystick-path/07-RESEARCH.md
@.planning/phases/07-macos-virtual-joystick-path/07-PATTERNS.md
@.planning/phases/07-macos-virtual-joystick-path/07-VALIDATION.md

<interfaces>
CoreHID first path: `HIDVirtualDevice` with input report ID 1 and output report ID 2.
Fallback gate: record `corehid-pass`, `corehid-compile-blocked`, `corehid-runtime-blocked`, or `corehid-output-failed`; only the last three can unlock Plan 07-06 fallback work.
Proof target: macOS 26.2 build 25C56 on arm64 per D-11 unless local probe records a concrete blocker.
</interfaces>
</context>

## Artifacts this phase produces

- Doc: `docs/setup/macos-virtual-hid.md`
- Manifest: `docs/evidence/manifests/phase7-macos-virtual-hid.jsonl`
- Native package: `native/macos-hid-helper/Package.swift`
- Helper entrypoint: `native/macos-hid-helper/Sources/BtGunMacosHidHelper/main.swift`
- Entitlements file: `native/macos-hid-helper/Entitlements.plist`
- Script: `native/macos-hid-helper/scripts/probe-macos-hid-environment.sh`
- Script: `native/macos-hid-helper/scripts/build-corehid-helper.sh`
- Evidence id: `phase7-corehid-feasibility`
- Evidence id: `phase7-toolchain-signing-audit`
- Evidence id: `phase7-pack03-doc-foundation`

<tasks>

<task type="auto">
  <name>Task 1: macOS toolchain, entitlement, and PACK-03 foundation</name>
  <files>docs/setup/macos-virtual-hid.md, docs/evidence/manifests/phase7-macos-virtual-hid.jsonl, native/macos-hid-helper/scripts/probe-macos-hid-environment.sh</files>
  <read_first>
    @.planning/phases/07-macos-virtual-joystick-path/07-CONTEXT.md
    @.planning/phases/07-macos-virtual-joystick-path/07-RESEARCH.md
    @.planning/phases/07-macos-virtual-joystick-path/07-VALIDATION.md
    @.planning/REQUIREMENTS.md
  </read_first>
  <action>
    Create `docs/setup/macos-virtual-hid.md` with exact development proof commands for CoreHID first per D-01, fallback criteria per D-02/D-03/D-09, macOS target facts per D-11, ad-hoc signing-first policy per D-12, and exact launch/signing/permission/fallback command sections per D-10/D-13. Create `probe-macos-hid-environment.sh` to print sanitized `sw_vers`, `uname -m`, `xcodebuild -version`, `xcrun --show-sdk-path`, `swift --version`, `security find-identity -v -p codesigning`, `command -v hidutil`, `command -v ioreg`, and `command -v systemextensionsctl`; redact identity hashes and do not print private key paths. Create the Phase 7 JSONL manifest with pending rows for `phase7-toolchain-signing-audit`, `phase7-corehid-feasibility`, and `phase7-pack03-doc-foundation`. Keep scope to macOS development proof files and evidence only.
  </action>
  <verify>
    <automated>rg -n "HIDVirtualDevice|com.apple.developer.hid.virtual.device|HIDDriverKit|system extension|hidutil|ioreg|xcode-select|codesign|corehid-(pass|blocked|failed)|phase7-toolchain-signing-audit|phase7-corehid-feasibility" docs/setup/macos-virtual-hid.md docs/evidence/manifests/phase7-macos-virtual-hid.jsonl native/macos-hid-helper/scripts/probe-macos-hid-environment.sh</automated>
  </verify>
  <acceptance_criteria>
    - PACK-03 docs include CoreHID, IOHIDUserDevice shim, HIDDriverKit fallback, entitlement, launch, signing, permission, and exact proof commands.
    - Manifest rows are pending or blocked with sanitized reasons only; no raw signing identity hash, key path, QR/manual code, proof, stream key, HMAC key, Bluetooth address, device id, or screenshot text appears.
    - Task action cites D-01, D-02, D-03, D-09, D-10, D-11, D-12, and D-13 behavior in the docs.
  </acceptance_criteria>
  <done>PACK-03 foundation and sanitized Phase 7 feasibility rows exist.</done>
</task>

<task type="auto">
  <name>Task 2: CoreHID helper feasibility probe</name>
  <files>native/macos-hid-helper/Package.swift, native/macos-hid-helper/Sources/BtGunMacosHidHelper/main.swift, native/macos-hid-helper/Entitlements.plist, native/macos-hid-helper/scripts/build-corehid-helper.sh, docs/evidence/manifests/phase7-macos-virtual-hid.jsonl</files>
  <read_first>
    @.planning/phases/07-macos-virtual-joystick-path/07-RESEARCH.md
    @.planning/phases/07-macos-virtual-joystick-path/07-PATTERNS.md
    @docs/setup/macos-virtual-hid.md
  </read_first>
  <action>
    Create a minimal Swift CoreHID helper package that constructs a `HIDVirtualDevice` named `BT Gun Virtual Joystick`, vendor ID `0x1209`, product ID `0xB707`, input report ID `0x01`, and output report ID `0x02`. Include an entitlement plist for `com.apple.developer.hid.virtual.device`. `build-corehid-helper.sh` must run the environment probe first, compile the helper if Swift/CoreHID works, ad-hoc sign first when allowed, run the helper in probe mode, and collect `hidutil list --matching '{"VendorID":0x1209,"ProductID":0xB707}'` plus `ioreg -r -c IOHIDDevice -l -w 0` evidence into sanitized build output. If Swift CoreHID is blocked by the known CLT/SDK mismatch, record `corehid-compile-blocked` and include the exact next command in docs; do not silently switch away from CoreHID unless D-02 proof exists. If Swift is blocked only by tooling, leave IOHIDUserDevice Objective-C shim as documented same-wave fallback for the executor to choose during implementation, but keep runtime entitlement proof required.
  </action>
  <verify>
    <automated>test -f native/macos-hid-helper/Package.swift &amp;&amp; test -f native/macos-hid-helper/Sources/BtGunMacosHidHelper/main.swift &amp;&amp; test -f native/macos-hid-helper/Entitlements.plist</automated>
    <automated>rg -n "BT Gun Virtual Joystick|0x1209|0xB707|HIDVirtualDevice|dispatchInputReport|receivedSetReportRequestOfType|com.apple.developer.hid.virtual.device|hidutil list|ioreg -r -c IOHIDDevice" native/macos-hid-helper docs/setup/macos-virtual-hid.md</automated>
  </verify>
  <acceptance_criteria>
    - Helper source contains CoreHID first path and delegate set-report callback scaffolding.
    - Build script fails closed with a sanitized manifest reason when Swift/CoreHID/signing/entitlement is blocked.
    - Backend support is not claimed from this plan; this is feasibility and setup proof only.
  </acceptance_criteria>
  <done>Minimal CoreHID probe package exists with exact build/sign/run commands and sanitized gate output.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Human setup only if CoreHID proof is environment-blocked</name>
  <files>docs/setup/macos-virtual-hid.md, docs/evidence/manifests/phase7-macos-virtual-hid.jsonl, .planning/phases/07-macos-virtual-joystick-path/07-01-SUMMARY.md</files>
  <read_first>
    @docs/setup/macos-virtual-hid.md
    @docs/evidence/manifests/phase7-macos-virtual-hid.jsonl
    @.planning/phases/07-macos-virtual-joystick-path/07-CONTEXT.md
  </read_first>
  <action>
    Pause only if automated Swift CoreHID and documented IOHIDUserDevice shim attempts cannot compile/run because the machine lacks matching Xcode/CLT or usable signing/provisioning. Present exact requested setup: selected Xcode path, `xcode-select` command, signing identity/provisioning need, and whether the blocker is CoreHID compile, virtual HID entitlement, or DriverKit fallback entitlement. Do not ask for production notarization or Developer ID unless the fallback path proves it is required by D-12/D-13.
  </action>
  <verify>
    <automated>rg -n "corehid-compile-blocked|corehid-runtime-blocked|xcode-select|signing identity|provisioning|resume" docs/setup/macos-virtual-hid.md docs/evidence/manifests/phase7-macos-virtual-hid.jsonl .planning/phases/07-macos-virtual-joystick-path/07-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - Checkpoint is reached only after automated local probes fail.
    - Requested human action is specific and reversible where possible.
    - Phase does not proceed to claim DESK-03 or DESK-06 before a CoreHID pass or a D-02/D-03 fallback gate exists.
  </acceptance_criteria>
  <how-to-verify>
    1. Review the exact Xcode/signing/entitlement blocker in the summary.
    2. Complete the listed setup step or state that fallback must proceed.
    3. Reply `approved` or describe the remaining blocker.
  </how-to-verify>
  <resume-signal>Type `approved` after setup is complete, or describe the blocker/fallback decision.</resume-signal>
  <done>Human setup is recorded only when automation cannot progress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local shell -> evidence docs | Toolchain and signing facts become committed proof notes. |
| Native helper -> macOS HID stack | Helper can ask macOS to create a virtual HID device. |
| CoreHID gate -> fallback planning | Failed user-space proof can unlock heavier DriverKit work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing | Native helper binary | mitigate | Build script records helper path, version, bundle id/name, VID/PID, and fails closed if helper identity does not match docs. |
| T-07-02 | Information Disclosure | Environment/signing audit | mitigate | Redact signing hashes, private key paths, QR/manual codes, proof values, stream keys, HMAC keys, Bluetooth addresses, device ids, and screenshots from docs/manifest. |
| T-07-03 | Tampering | CoreHID fallback gate | mitigate | Require D-02 local compile/runtime proof or official docs before switching away from CoreHID. |
| T-07-04 | Elevation of Privilege | DriverKit fallback | mitigate | DriverKit stays locked behind D-03/D-09 gate; no system extension work starts in this plan. |
| T-07-SC | Tampering | Package installs | accept | No npm, pip, cargo, Homebrew, or Gradle dependency install in this plan; Apple platform APIs only. |
</threat_model>

<verification>
Run static proof after file creation:
`rg -n "HIDVirtualDevice|com.apple.developer.hid.virtual.device|HIDDriverKit|system extension|hidutil|ioreg|phase7-corehid-feasibility" docs/setup/macos-virtual-hid.md native/macos-hid-helper docs/evidence/manifests/phase7-macos-virtual-hid.jsonl`
</verification>

<success_criteria>
- CoreHID first path is represented by executable helper probe files.
- PACK-03 docs give exact local development setup, permission, signing, launch, enumeration, and fallback commands.
- Any CoreHID failure is classified as compile, runtime/entitlement, visibility, or output failure for later plans.
- Scope remains limited to macOS development proof files and evidence.
</success_criteria>

<output>
Create `.planning/phases/07-macos-virtual-joystick-path/07-01-SUMMARY.md` when done.
</output>
