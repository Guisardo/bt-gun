---
phase: 07-macos-virtual-joystick-path
plan: 05
type: execute
wave: 5
depends_on:
  - 07-04
files_modified:
  - tools/macos/BtGunGameControllerProbe.swift
  - docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl
autonomous: false
requirements:
  - DESK-03
  - DESK-06
  - ANDR-11
must_haves:
  truths:
    - "macOS pairs to Android Bluetooth HID and sees a controller without desktop companion LAN input per D-03, D-09, D-11, and D-12."
    - "Live gun controls and phone motion move Game Controller/tester buttons and axes per D-09 and D-12."
    - "Output-report proof records callback seen plus phone haptic result, or honest macOS unsupported status after live probe per D-13 and D-16."
  artifacts:
    - path: "tools/macos/BtGunGameControllerProbe.swift"
      provides: "macOS Game Controller probe for sanitized controller/input/output status"
    - path: "docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl"
      provides: "Live Android HID/macOS proof rows"
  key_links:
    - from: "macOS Bluetooth Settings"
      to: "AndroidBluetoothHidGamepad.kt"
      via: "Bluetooth HID pairing"
      pattern: "phase7-macos-bluetooth-paired"
    - from: "BtGunGameControllerProbe.swift"
      to: "docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl"
      via: "sanitized proof rows"
      pattern: "phase7-gamecontroller-input|phase7-hid-output-callback"
---

<objective>
Prove the live Android-phone Bluetooth HID gamepad path on macOS.

Purpose: DESK-03 and DESK-06 require real host OS behavior. Automated unit tests cannot prove macOS Bluetooth pairing, Game Controller visibility, live input movement, or output-report behavior.

Output: macOS probe tool and blocking live proof checkpoints with sanitized evidence rows.
</objective>

## Phase Goal

**As a** user with the discontinued iPega AR gun, **I want to** use the Android phone as a Bluetooth HID gamepad for macOS Apple Silicon, **so that** macOS sees a normal controller without paid Apple virtual HID entitlements while the completed Windows VHF path remains fallback.

## Artifacts this phase produces

- macOS Game Controller probe source that reads controller visibility and button/axis movement.
- Sanitized manifest rows for Android HID proxy/register, macOS Bluetooth connection, Game Controller input, and output/unsupported result.
- Human confirmation checkpoints for live phone/macOS proof.

<execution_context>
@/Users/lucas.rancez/.codex/gsd-core/workflows/execute-plan.md
@/Users/lucas.rancez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-macos-virtual-joystick-path/07-CONTEXT.md
@.planning/phases/07-macos-virtual-joystick-path/07-RESEARCH.md
@.planning/phases/07-macos-virtual-joystick-path/07-PATTERNS.md
@.planning/phases/07-macos-virtual-joystick-path/07-VALIDATION.md
@.planning/phases/07-macos-virtual-joystick-path/07-04-SUMMARY.md
@docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add macOS Game Controller probe and sanitized output format</name>
  <files>tools/macos/BtGunGameControllerProbe.swift, docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl</files>
  <read_first>
    - `.planning/phases/07-macos-virtual-joystick-path/07-RESEARCH.md`
    - `.planning/phases/07-macos-virtual-joystick-path/07-VALIDATION.md`
    - `docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl`
  </read_first>
  <action>Create `tools/macos/BtGunGameControllerProbe.swift` using Apple `GameController` APIs to enumerate `GCController.controllers()`, prefer controllers whose vendor/product label matches the Android SDP name, and print sanitized lines for controller visible, extended gamepad available, trigger/reload/X/Y/A/B observed, stickX/stickY observed, aimX/aimY observed, and haptic/output probe attempted. Do not print Bluetooth addresses, personal device names, nearby devices, usernames, screenshots, or raw identifiers. Add manifest schema fields that match the probe output keys; do not add pass rows in this auto task. If local `xcrun swiftc` is unavailable, record `swiftc-unavailable` in the plan summary and rely on source/static verification until the live checkpoint.</action>
  <verify>
    <automated>xcrun swiftc -typecheck tools/macos/BtGunGameControllerProbe.swift || rg -n 'import GameController|GCController|extendedGamepad|haptics' tools/macos/BtGunGameControllerProbe.swift</automated>
  </verify>
  <acceptance_criteria>Probe source exists, typechecks when Swift tooling exists or passes static API-name check, and its output contract contains only sanitized fields needed by D-09, D-12, D-13, and D-16.</acceptance_criteria>
  <done>Live proof has a local macOS probe path without putting desktop companion in the input path.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live macOS Bluetooth pairing and input proof</name>
  <files>docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl, .planning/phases/07-macos-virtual-joystick-path/07-05-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/07-macos-virtual-joystick-path/07-CONTEXT.md`
    - `.planning/phases/07-macos-virtual-joystick-path/07-VALIDATION.md`
    - `docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl`
  </read_first>
  <action>After Plans 07-01 through 07-04 pass, install/run the Android host on the current phone, start the live gun session, tap `Start Bluetooth gamepad`, open pairing mode, pair from macOS Bluetooth Settings, and run `tools/macos/BtGunGameControllerProbe.swift` or the equivalent built probe. Press trigger, reload, X/Y/A/B, move the physical stick, and move phone aim. Append sanitized manifest rows `phase7-android-hid-proxy`, `phase7-android-hid-register-app`, `phase7-android-hid-pairing-window`, `phase7-macos-bluetooth-paired`, and `phase7-gamecontroller-input` only from observed results. Do not run desktop companion LAN input for DESK-03 proof. If current phone fails HID proxy/register/pairing/input, record blocked status and leave fallback decision to Plan 07-06 alternate-phone gate per D-04 and D-19.</action>
  <verify>
    <automated>rg -n 'phase7-android-hid-proxy|phase7-android-hid-register-app|phase7-macos-bluetooth-paired|phase7-gamecontroller-input' docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl</automated>
    <human-check>Confirm macOS Bluetooth shows the Android phone as connected controller and Game Controller/tester shows real trigger/reload/X/Y/A/B, stickX/stickY, and aimX/aimY movement.</human-check>
  </verify>
  <acceptance_criteria>DESK-03 proof passes only when macOS Bluetooth and Game Controller/tester both show live Android HID input with desktop companion absent from the input path. Blocked current-phone result is recorded honestly if proof fails.</acceptance_criteria>
  <done>Live current-phone macOS input proof is approved or blocked with sanitized evidence.</done>
  <resume-signal>Type `approved` after live proof passes, or describe exact blocked state for alternate-phone gate.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live HID output report or unsupported proof</name>
  <files>docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl, .planning/phases/07-macos-virtual-joystick-path/07-05-SUMMARY.md</files>
  <read_first>
    - `android-host/app/src/main/java/com/btgun/host/hid/BtGunHidOutputReportMapper.kt`
    - `android-host/app/src/main/java/com/btgun/host/hid/AndroidBluetoothHidGamepad.kt`
    - `.planning/phases/07-macos-virtual-joystick-path/07-CONTEXT.md`
    - `docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl`
  </read_first>
  <action>With the same macOS Bluetooth HID connection active, attempt a host-origin output/haptic probe from the Game Controller probe or another macOS host path that targets the Android HID controller. Watch Android dashboard/status for `onSetReport` or `onInterruptData`, output validation result, and phone haptic result. Append `phase7-hid-output-callback` when a callback is observed; set status to phone haptic pass only if valid host-origin HID output triggers Android phone vibration. If macOS sends no usable output report after the documented probe, append `phase7-macos-output-unsupported` with callback-not-seen or unusable-output reason. Do not claim DESK-06 from LAN haptics, Phase 5 simulated output, or direct local phone haptic test per D-15.</action>
  <verify>
    <automated>rg -n 'phase7-hid-output-callback|phase7-macos-output-unsupported' docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl</automated>
    <human-check>Confirm either host-origin HID output caused Android phone vibration, or Android/macOS evidence clearly shows no usable output report arrived after live probe.</human-check>
  </verify>
  <acceptance_criteria>DESK-06 closes as pass only from host-origin HID output plus phone vibration. Unsupported status is accepted only with live callback/probe evidence and clear limitation row preserving phone haptic support through other approved paths.</acceptance_criteria>
  <done>Output-report behavior is proven or explicitly unsupported with live evidence.</done>
  <resume-signal>Type `approved` after output behavior is recorded, or describe missing callback/unsupported evidence.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| macOS Bluetooth host -> Android HID app | Live OS behavior determines DESK-03 and DESK-06 truth. |
| Human proof -> evidence manifest | Manual rows can overclaim or leak identifiers. |
| Probe output -> committed docs | Probe output must be sanitized before commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-16 | Spoofing | macOS proof | mitigate | Require Bluetooth Settings plus Game Controller/tester evidence; desktop companion LAN path forbidden. |
| T-07-17 | Repudiation | Human checkpoint | mitigate | Manifest rows and human approval both required for DESK-03/DESK-06. |
| T-07-18 | Information Disclosure | Probe/evidence | mitigate | Probe and rows exclude Bluetooth addresses, personal device names, screenshots, serials, and raw identifiers. |
| T-07-19 | Tampering | Output proof | mitigate | DESK-06 pass requires host-origin callback and phone vibration; LAN/direct haptics cannot satisfy it. |
| T-07-SC | Tampering | Package installs | accept | No npm/pip/cargo installs in this plan. |
</threat_model>

<verification>
Automated: Swift typecheck/static probe check plus manifest row `rg` checks. Human: macOS Bluetooth/Game Controller input approval and output/unsupported approval.
</verification>

<success_criteria>
DESK-03 is proven with live macOS Bluetooth HID input, or current-phone blocker is recorded for alternate-phone gate. DESK-06 is proven by host-origin HID output plus phone vibration, or unsupported behavior is recorded honestly.
</success_criteria>

<output>
Create `.planning/phases/07-macos-virtual-joystick-path/07-05-SUMMARY.md` when done.
</output>
