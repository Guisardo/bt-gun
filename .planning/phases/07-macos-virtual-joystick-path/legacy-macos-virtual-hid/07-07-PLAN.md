---
phase: 07-macos-virtual-joystick-path
plan: 07
type: execute
wave: 7
depends_on: [07-05, 07-06]
files_modified:
  - docs/macos/phase7-proof-checklist.md
  - docs/setup/macos-virtual-hid.md
  - docs/evidence/manifests/phase7-macos-virtual-hid.jsonl
  - tools/macos/collect-btgun-macos-hid-evidence.sh
  - tools/macos/redact-btgun-macos-hid-evidence.sh
autonomous: false
requirements: [DESK-03, DESK-06, PACK-03]
requirements_addressed: [DESK-03, DESK-06, PACK-03]
must_haves:
  truths:
    - "DESK-03: macOS Apple Silicon sees an OS-visible regular gamepad-style virtual joystick."
    - "DESK-03/D-04/D-05/D-06: Final proof includes CLI HID enumeration, macOS-visible UI/tester evidence, live paired Android/gun stream, agent-captured evidence, and user confirmation."
    - "DESK-06/D-07/D-08: Final haptic proof uses macOS OS-origin output/rumble mapped to Android phone haptic; direct phone-haptic command and simulateOutputReport are not sufficient."
    - "PACK-03/D-10/D-11/D-12/D-13: Setup docs include exact launch/signing/permission/fallback commands for development proof on macOS 26.2 arm64."
    - "D-14/D-15/D-16: Final live runtime mirrors Windows callback shape, preserves stale aim axes, and keeps native helper/DriverKit as HID byte/event bridge only."
  artifacts:
    - path: "docs/macos/phase7-proof-checklist.md"
      provides: "final macOS live proof checklist"
      contains: "live Android/gun"
    - path: "docs/evidence/manifests/phase7-macos-virtual-hid.jsonl"
      provides: "sanitized final Phase 7 evidence manifest"
      contains: "phase7-live-android-gun-input"
    - path: "tools/macos/collect-btgun-macos-hid-evidence.sh"
      provides: "read-only macOS HID evidence collector"
      contains: "hidutil"
  key_links:
    - from: "Live Android/gun stream"
      to: "macOS-visible joystick"
      via: "ControlServer.onUdpInputReceived -> MacosBackendRuntime -> helper/DriverKit"
      pattern: "phase7-live-android-gun-input"
    - from: "macOS OS-origin output"
      to: "Android phone haptic"
      via: "helper/DriverKit output report -> MacosOutputReportMapper -> ControlServer.sendHapticCommand"
      pattern: "phase7-macos-output-report-phone-haptic"
---

## Phase Goal

**As a** macOS desktop user, **I want to** use the live Android gun stream as a regular macOS gamepad-style joystick with honest output capability reporting, **so that** I can validate v1 input and phone haptics before later desktop UX work.

<objective>
Close Phase 7 with live macOS joystick and OS-output-to-phone-haptic proof.

Purpose: Convert CoreHID or conditional DriverKit implementation into final user-visible macOS acceptance evidence.
Output: Proof checklist, sanitized evidence collector/redactor, final manifest rows, and user verification checkpoint.
</objective>

<execution_context>
@/Users/lucas.rancez/.codex/gsd-core/workflows/execute-plan.md
@/Users/lucas.rancez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-macos-virtual-joystick-path/07-CONTEXT.md
@.planning/phases/07-macos-virtual-joystick-path/07-RESEARCH.md
@.planning/phases/07-macos-virtual-joystick-path/07-VALIDATION.md
@.planning/phases/07-macos-virtual-joystick-path/07-05-SUMMARY.md
@.planning/phases/07-macos-virtual-joystick-path/07-06-SUMMARY.md
@.planning/phases/05-desktop-backend-contract-and-smoke-harness/05-05-SUMMARY.md
@.planning/phases/06-windows-virtual-joystick-path/06-06-SUMMARY.md
@docs/setup/macos-virtual-hid.md
@docs/setup/macos-driverkit-fallback.md
@docs/evidence/manifests/phase7-macos-virtual-hid.jsonl
@desktop-companion/src/main/kotlin/com/btgun/desktop/backend/macos/MacosBackendRuntime.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/backend/macos/MacosVirtualControllerBackend.kt

<interfaces>
Final proof ids: `phase7-macos-cli-hid-enumeration`, `phase7-macos-ui-visible`, `phase7-live-android-gun-input`, `phase7-macos-output-report-phone-haptic`, `phase7-user-confirmation`, `phase7-redaction-scan`, `phase7-pack03-docs-complete`.
Launch flags: `btgun.macos.hid.enabled=true` and `btgun.macos.hid.helper.path` for CoreHID helper path unless Plan 07-06 selected DriverKit fallback-specific launch.
Final proof excludes replay/fake fixtures except as debug evidence.
</interfaces>
</context>

## Artifacts this phase produces

- Doc: `docs/macos/phase7-proof-checklist.md`
- Manifest updates: `docs/evidence/manifests/phase7-macos-virtual-hid.jsonl`
- Script: `tools/macos/collect-btgun-macos-hid-evidence.sh`
- Script: `tools/macos/redact-btgun-macos-hid-evidence.sh`
- Evidence id: `phase7-macos-cli-hid-enumeration`
- Evidence id: `phase7-macos-ui-visible`
- Evidence id: `phase7-live-android-gun-input`
- Evidence id: `phase7-macos-output-report-phone-haptic`
- Evidence id: `phase7-user-confirmation`
- Evidence id: `phase7-redaction-scan`
- Evidence id: `phase7-pack03-docs-complete`
- CLI flag: `-Dbtgun.macos.hid.enabled=true`
- CLI flag: `-Dbtgun.macos.hid.helper.path=/path/to/BtGunMacosHidHelper`
- Report id: `MACOS_INPUT_REPORT_ID = 0x01`
- Report id: `MACOS_OUTPUT_REPORT_ID = 0x02`

<tasks>

<task type="auto">
  <name>Task 1: Final proof checklist and sanitized evidence helpers</name>
  <files>docs/macos/phase7-proof-checklist.md, tools/macos/collect-btgun-macos-hid-evidence.sh, tools/macos/redact-btgun-macos-hid-evidence.sh, docs/setup/macos-virtual-hid.md, docs/evidence/manifests/phase7-macos-virtual-hid.jsonl</files>
  <read_first>
    @.planning/phases/07-macos-virtual-joystick-path/07-CONTEXT.md
    @.planning/phases/07-macos-virtual-joystick-path/07-VALIDATION.md
    @.planning/phases/07-macos-virtual-joystick-path/07-05-SUMMARY.md
    @.planning/phases/07-macos-virtual-joystick-path/07-06-SUMMARY.md
    @docs/setup/macos-virtual-hid.md
    @docs/evidence/manifests/phase7-macos-virtual-hid.jsonl
  </read_first>
  <action>
    Create `docs/macos/phase7-proof-checklist.md` and evidence helper scripts. Checklist must require D-04 CLI HID enumeration plus macOS UI/tester evidence, D-05 live paired Android/gun stream, D-06 agent-captured evidence plus user confirmation, D-07/D-08 macOS OS-origin output-to-phone-haptic proof, and D-10 through D-13 development setup docs. Evidence collector must run read-only `hidutil`, `ioreg`, `systemextensionsctl list` when fallback was used, helper/backend status commands, and file metadata for the selected CoreHID/DriverKit proof artifact. Redactor must remove QR/manual codes, proof values, stream keys, HMAC keys, private keys, signing identity hashes, provisioning UUIDs, Bluetooth addresses, device ids, usernames in paths where possible, and screenshots. Update manifest rows for all final proof ids as pending before live proof.
  </action>
  <verify>
    <automated>rg -n "phase7-macos-cli-hid-enumeration|phase7-macos-ui-visible|phase7-live-android-gun-input|phase7-macos-output-report-phone-haptic|phase7-user-confirmation|phase7-redaction-scan|phase7-pack03-docs-complete|hidutil|ioreg|systemextensionsctl|live Android/gun|OS-origin" docs/macos/phase7-proof-checklist.md docs/evidence/manifests/phase7-macos-virtual-hid.jsonl tools/macos</automated>
  </verify>
  <acceptance_criteria>
    - Checklist rejects replay/fake input for final DESK-03.
    - Checklist rejects `simulateOutputReport` and direct haptic commands for final DESK-06.
    - Collector/redactor are read-only with respect to OS driver state and sanitized for committed evidence.
    - PACK-03 docs link exact CoreHID and DriverKit fallback commands.
  </acceptance_criteria>
  <done>Final proof materials exist before live target actions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live Android/gun macOS joystick proof</name>
  <files>docs/evidence/manifests/phase7-macos-virtual-hid.jsonl, .planning/phases/07-macos-virtual-joystick-path/07-07-SUMMARY.md</files>
  <read_first>
    @docs/macos/phase7-proof-checklist.md
    @docs/setup/macos-virtual-hid.md
    @docs/evidence/manifests/phase7-macos-virtual-hid.jsonl
    @desktop-companion/src/main/kotlin/com/btgun/desktop/backend/macos/MacosBackendRuntime.kt
  </read_first>
  <action>
    Launch the selected proven backend path: CoreHID helper if Plan 07-05 recorded `corehid-pass`, otherwise DriverKit fallback only if Plan 07-06 produced fallback pass rows. Pair the Android host and physical iPega gun to the desktop companion. Run the desktop companion with `-Dbtgun.macos.hid.enabled=true` and the selected helper/fallback path. Collect `hidutil`/`ioreg` evidence that `BT Gun Virtual Joystick` is OS-visible. Open a macOS-visible game/controller tester or equivalent UI, capture agent evidence, and have the user confirm that live trigger/reload/X/Y/A/B/stick/aim movements from the physical Android/gun stream move the macOS-visible joystick. Update `phase7-macos-cli-hid-enumeration`, `phase7-macos-ui-visible`, and `phase7-live-android-gun-input` rows with sanitized pass/fail evidence.
  </action>
  <verify>
    <automated>rg -n "\"phase7-macos-cli-hid-enumeration\"|\"phase7-macos-ui-visible\"|\"phase7-live-android-gun-input\"|\"status\":\"pass\"|user-confirmed" docs/evidence/manifests/phase7-macos-virtual-hid.jsonl .planning/phases/07-macos-virtual-joystick-path/07-07-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - CLI enumeration row passes only with `hidutil`/`ioreg` evidence for selected backend.
    - UI-visible row passes only with agent-captured UI/tester evidence and user confirmation.
    - Live input row passes only with physical Android/gun stream, not replay/fake fixtures.
    - Stale behavior evidence or test reference confirms active buttons/stick clear while aim axes persist per D-15.
  </acceptance_criteria>
  <how-to-verify>
    1. Confirm macOS shows `BT Gun Virtual Joystick` in CLI and UI/tester.
    2. Move live gun trigger/reload/X/Y/A/B/stick/aim and confirm macOS-visible joystick moves.
    3. Confirm no replay/fake-only evidence is being used for final pass.
  </how-to-verify>
  <resume-signal>Type `approved` after live macOS input proof passes, or describe the failing proof row.</resume-signal>
  <done>DESK-03 final macOS OS-visible live input proof passes with user confirmation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: macOS OS-origin output report to Android phone haptic proof</name>
  <files>docs/evidence/manifests/phase7-macos-virtual-hid.jsonl, docs/setup/macos-virtual-hid.md, .planning/phases/07-macos-virtual-joystick-path/07-07-SUMMARY.md</files>
  <read_first>
    @docs/macos/phase7-proof-checklist.md
    @docs/setup/macos-virtual-hid.md
    @docs/setup/macos-driverkit-fallback.md
    @docs/evidence/manifests/phase7-macos-virtual-hid.jsonl
    @.planning/phases/05-desktop-backend-contract-and-smoke-harness/05-05-SUMMARY.md
  </read_first>
  <action>
    Trigger macOS-origin output through the selected proven path. Use the separate CoreHID output probe from Plan 07-05 when CoreHID passed, or the DriverKit `setReport`/output probe path from Plan 07-06 when fallback passed. The output must enter the helper/DriverKit from macOS HID APIs, be drained by `MacosVirtualControllerBackend`, map through `MacosOutputReportMapper`, and route through `ControlServer.sendHapticCommand` to the paired Android phone. Obtain user confirmation that the Android phone physically vibrated. Update `phase7-macos-output-report-phone-haptic`, `phase7-user-confirmation`, `phase7-redaction-scan`, and `phase7-pack03-docs-complete` rows. If OS-origin output cannot be proven and no DriverKit fallback pass exists, mark Phase 7 inconclusive; do not claim DESK-06.
  </action>
  <verify>
    <automated>rg -n "\"phase7-macos-output-report-phone-haptic\"|\"phase7-user-confirmation\"|\"phase7-redaction-scan\"|\"phase7-pack03-docs-complete\"|\"status\":\"pass\"|macos-output-report|phone-haptic" docs/evidence/manifests/phase7-macos-virtual-hid.jsonl .planning/phases/07-macos-virtual-joystick-path/07-07-SUMMARY.md</automated>
    <automated>bash -lc '! rg -n "qr_secret=|manualCode|proofHex|streamSessionIdHex|hmacSha256KeyBase64Url|-----BEGIN .*PRIVATE KEY|Bluetooth address:|device id:|screenshot_path|provisioning_uuid|signing_identity_hash" docs/evidence/manifests/phase7-macos-virtual-hid.jsonl docs/macos/phase7-proof-checklist.md docs/setup/macos-virtual-hid.md docs/setup/macos-driverkit-fallback.md tools/macos'</automated>
  </verify>
  <acceptance_criteria>
    - Output haptic row passes only with macOS HID API-origin output report evidence and user phone-vibration confirmation.
    - Direct companion haptic button, Phase 5 simulated output, and `simulateOutputReport` are rejected as final proof.
    - Redaction scan records no committed secrets, private signing material, device ids, or screenshots.
    - PACK-03 docs are complete for selected path and fallback path, with exact commands and known limits.
  </acceptance_criteria>
  <how-to-verify>
    1. Confirm output was triggered through macOS HID output/set-report path.
    2. Confirm Android phone physically vibrated.
    3. Confirm manifest rows all pass and redaction scan is clean.
    4. Confirm setup docs list exact launch/signing/permission/fallback commands.
  </how-to-verify>
  <resume-signal>Type `approved` after OS-origin output-to-phone-haptic proof passes, or describe the failing proof row.</resume-signal>
  <done>DESK-06 and PACK-03 final proof rows pass with user confirmation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live Android/gun -> macOS joystick | Authenticated UDP stream becomes OS-visible joystick input. |
| macOS HID output -> Android phone | OS-origin output report becomes physical phone vibration. |
| Evidence capture -> committed docs | Local proof data becomes repository evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-27 | Spoofing | Final joystick proof | mitigate | Require live paired Android/gun stream, OS-visible CLI/UI evidence, and user confirmation per D-04/D-05/D-06. |
| T-07-28 | Tampering | OS-origin output haptic | mitigate | Require helper/DriverKit output report bytes to pass `MacosOutputReportMapper`, TTL bounds, and authenticated `ControlServer.sendHapticCommand`; reject direct haptic commands as proof. |
| T-07-29 | Repudiation | User confirmation | mitigate | Record explicit user approval rows for UI-visible input and physical phone vibration. |
| T-07-30 | Information Disclosure | Evidence manifest/docs | mitigate | Redact QR/manual codes, proof values, stream keys, HMAC keys, private keys, signing identities, provisioning UUIDs, Bluetooth addresses, device ids, usernames where possible, and screenshots. |
| T-07-31 | Denial of Service | Stale stream handling | mitigate | Verify D-15 stale behavior from runtime tests/evidence: clear active buttons/stick, preserve aim axes, expose stale diagnostic state. |
| T-07-SC | Tampering | Package installs | accept | No npm, pip, cargo, Homebrew, or new dependency install in this plan. |
</threat_model>

<verification>
Final Phase 7 gate requires:
- `phase7-macos-cli-hid-enumeration` pass.
- `phase7-macos-ui-visible` pass with agent evidence and user confirmation.
- `phase7-live-android-gun-input` pass from real Android/gun stream.
- `phase7-macos-output-report-phone-haptic` pass from macOS OS-origin output report and user phone-vibration confirmation.
- `phase7-redaction-scan` pass.
- `phase7-pack03-docs-complete` pass.
</verification>

<success_criteria>
- macOS Apple Silicon sees `BT Gun Virtual Joystick` as a regular gamepad-style joystick.
- Live Android/gun stream drives macOS-visible buttons and axes.
- macOS OS-origin output report triggers Android phone haptic through authenticated control path.
- Development setup docs are complete and honest about CoreHID, DriverKit fallback, signing, permissions, commands, and known limits.
</success_criteria>

<output>
Create `.planning/phases/07-macos-virtual-joystick-path/07-07-SUMMARY.md` when done.
</output>
