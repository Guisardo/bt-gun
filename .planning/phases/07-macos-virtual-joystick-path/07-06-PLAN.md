---
phase: 07-macos-virtual-joystick-path
plan: 06
type: execute
wave: 6
depends_on:
  - 07-05
files_modified:
  - docs/setup/android-bluetooth-hid-gamepad.md
  - docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl
  - .planning/phases/07-macos-virtual-joystick-path/07-VALIDATION.md
autonomous: false
requirements:
  - PACK-03
  - PACK-06
  - DESK-03
  - DESK-06
  - ANDR-09
must_haves:
  truths:
    - "Docs name Android Bluetooth HID gamepad as the primary macOS strategy and CoreHID/DriverKit as retained blocked/fallback evidence per D-17 and PACK-03."
    - "Docs include phone compatibility checks, macOS pairing, descriptor/report bytes, output behavior, redaction, and Windows VHF fallback per PACK-06."
    - "If current phone blocks HID proof, alternate Android phone is tested before Windows VHF fallback decision per D-04 and D-19."
  artifacts:
    - path: "docs/setup/android-bluetooth-hid-gamepad.md"
      provides: "Phase 7 setup, proof, output behavior, and fallback docs"
    - path: "docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl"
      provides: "Final sanitized evidence rows"
    - path: ".planning/phases/07-macos-virtual-joystick-path/07-VALIDATION.md"
      provides: "Validation status updated from executed evidence"
  key_links:
    - from: "docs/setup/android-bluetooth-hid-gamepad.md"
      to: "phase7-android-bluetooth-hid.jsonl"
      via: "setup/proof row names"
      pattern: "phase7-gamecontroller-input|phase7-macos-output-unsupported"
    - from: "docs/setup/android-bluetooth-hid-gamepad.md"
      to: ".planning/STATE.md"
      via: "Windows VHF fallback decision"
      pattern: "Windows VHF fallback"
---

<objective>
Close Phase 7 docs, redaction, alternate-phone gate, and fallback decision.

Purpose: Phase 7 must leave repeatable Android Bluetooth HID setup and honest compatibility limits without reviving the legacy macOS virtual HID path or weakening Windows fallback boundaries.

Output: setup docs, sanitized manifest, validation updates, alternate-phone checkpoint, and Windows VHF fallback decision row when needed.
</objective>

## Phase Goal

**As a** user with the discontinued iPega AR gun, **I want to** use the Android phone as a Bluetooth HID gamepad for macOS Apple Silicon, **so that** macOS sees a normal controller without paid Apple virtual HID entitlements while the completed Windows VHF path remains fallback.

## Artifacts this phase produces

- `docs/setup/android-bluetooth-hid-gamepad.md` with setup, compatibility, pairing, descriptor, output behavior, redaction, and fallback guidance.
- Final sanitized evidence manifest for Phase 7 Android HID proof.
- Validation status updated from actual Plan 07-01 through 07-05 results.

<execution_context>
@/Users/lucas.rancez/.codex/gsd-core/workflows/execute-plan.md
@/Users/lucas.rancez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-macos-virtual-joystick-path/07-CONTEXT.md
@.planning/phases/07-macos-virtual-joystick-path/07-RESEARCH.md
@.planning/phases/07-macos-virtual-joystick-path/07-PATTERNS.md
@.planning/phases/07-macos-virtual-joystick-path/07-VALIDATION.md
@.planning/phases/07-macos-virtual-joystick-path/07-05-SUMMARY.md
@.planning/phases/06-windows-virtual-joystick-path/06-CONTEXT.md
@docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Android Bluetooth HID setup and strategy docs</name>
  <files>docs/setup/android-bluetooth-hid-gamepad.md</files>
  <read_first>
    - `.planning/phases/07-macos-virtual-joystick-path/07-CONTEXT.md`
    - `.planning/phases/07-macos-virtual-joystick-path/07-RESEARCH.md`
    - `.planning/phases/07-macos-virtual-joystick-path/07-05-SUMMARY.md`
    - `.planning/phases/06-windows-virtual-joystick-path/06-CONTEXT.md`
  </read_first>
  <action>Create `docs/setup/android-bluetooth-hid-gamepad.md`. Include: Android phone compatibility gate for `BluetoothProfile.HID_DEVICE`, required Bluetooth permission states, Start Bluetooth gamepad action, pairing-mode countdown, macOS Bluetooth pairing steps, Game Controller/probe steps, descriptor/report table with report IDs/button order/axis endian/range, output report behavior, unsupported-output interpretation, redaction rules, Android HID as primary no-subscription macOS strategy per D-03 and D-17, CoreHID/DriverKit retained only as blocked/fallback evidence per D-17 and D-18, and completed Windows VHF fallback boundary per D-19. Do not include SIP/system-extension activation instructions, desktop companion LAN input as DESK-03 proof, LAN haptics as DESK-06 proof, profile mapping controls, visualizer UI, physical gun motor rumble, or game-specific guidance.</action>
  <verify>
    <automated>rg -n 'Android Bluetooth HID|HID_DEVICE|Start Bluetooth gamepad|pairing-mode|Game Controller|report ID|onSetReport|unsupported|Windows VHF fallback|corehid-runtime-blocked|redaction' docs/setup/android-bluetooth-hid-gamepad.md</automated>
  </verify>
  <acceptance_criteria>PACK-03 and PACK-06 doc topics are present, legacy macOS virtual HID is framed only as blocked/fallback evidence, and Windows VHF fallback boundary is explicit.</acceptance_criteria>
  <done>Setup docs can guide repeatable Android HID/macOS proof without hidden legacy-driver steps.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Alternate-phone fallback gate before Windows VHF selection</name>
  <files>docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl, .planning/phases/07-macos-virtual-joystick-path/07-06-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/07-macos-virtual-joystick-path/07-05-SUMMARY.md`
    - `.planning/phases/07-macos-virtual-joystick-path/07-CONTEXT.md`
    - `.planning/phases/06-windows-virtual-joystick-path/06-CONTEXT.md`
    - `docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl`
  </read_first>
  <action>If Plan 07-05 current-phone DESK-03 proof passed, record that alternate-phone gate is not required and do not add fallback-selected row. If current phone failed HID proxy, register, macOS pairing, or Game Controller input, repeat the Android HID proxy/register/pairing/input proof on an alternate Android phone before declaring primary path blocked per D-04. Append `phase7-alternate-phone-tested` with sanitized support/result fields. Append `phase7-windows-vhf-fallback-selected` only if both current and alternate phone fail the Android HID/macOS proof and cite Phase 6 Windows VHF acceptance as the fallback per D-19. Do not discard Windows work and do not revive CoreHID/DriverKit as primary path.</action>
  <verify>
    <automated>rg -n 'phase7-gamecontroller-input|phase7-alternate-phone-tested|phase7-windows-vhf-fallback-selected' docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl</automated>
    <human-check>Confirm either current phone passed DESK-03, or alternate phone was tested before Windows VHF fallback selection.</human-check>
  </verify>
  <acceptance_criteria>Fallback decision is impossible without a current-phone result and an alternate-phone attempt when current phone blocks the proof. Fallback row cites completed Windows VHF path only after both Android phone attempts fail.</acceptance_criteria>
  <done>Primary Android HID path is passed or fallback is selected with required alternate-phone evidence.</done>
  <resume-signal>Type `approved` after alternate-phone gate or fallback decision is correct.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Redaction scan, validation update, and source audit closeout</name>
  <files>docs/setup/android-bluetooth-hid-gamepad.md, docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl, .planning/phases/07-macos-virtual-joystick-path/07-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/07-macos-virtual-joystick-path/07-VALIDATION.md`
    - `.planning/phases/07-macos-virtual-joystick-path/07-CONTEXT.md`
    - `.planning/REQUIREMENTS.md`
    - `docs/setup/android-bluetooth-hid-gamepad.md`
    - `docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl`
  </read_first>
  <action>Update `07-VALIDATION.md` statuses from actual plan summaries and manifest rows. Add a short source audit section in the summary: GOAL Android Bluetooth HID primary path covered; REQ ANDR-09/ANDR-10/ANDR-11/DESK-03/DESK-06/PACK-03/PACK-06 covered; RESEARCH BluetoothHidDevice, output callback, macOS Game Controller proof, Gradle startup blocker, and OEM alternate-phone risk covered; CONTEXT D-01 through D-19 covered. Run redaction scan over setup docs and manifest. If scan hits Bluetooth MACs, serials, account names, screenshots, keys, pairing material, stream auth material, proof values, or stable identifiers, fix before closeout. Do not edit the legacy archive under `legacy-macos-virtual-hid/`.</action>
  <verify>
    <automated>rg -n 'ANDR-09|ANDR-10|ANDR-11|DESK-03|DESK-06|PACK-03|PACK-06' .planning/phases/07-macos-virtual-joystick-path/07-VALIDATION.md docs/setup/android-bluetooth-hid-gamepad.md && ! rg -n '([0-9A-Fa-f]{2}:){5}[0-9A-Fa-f]{2}|qr_secret|stream key|HMAC key|private key|proof value|manual_code=|pairing_secret|android_id|deviceId|serialNumber|screenshot_path' docs/evidence/manifests/phase7-android-bluetooth-hid.jsonl docs/setup/android-bluetooth-hid-gamepad.md</automated>
  </verify>
  <acceptance_criteria>Validation statuses reflect executed evidence, all Phase 7 requirement IDs are named in docs/validation, redaction scan is clean, source audit reports no missing source items, and legacy archive remains untouched.</acceptance_criteria>
  <done>Phase 7 plan set closes with docs, evidence, validation, and redaction in sync.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live proof -> fallback decision | One phone failure cannot decide project path. |
| Evidence/docs -> repository | Setup docs and manifest must not leak sensitive hardware/user identifiers. |
| Legacy macOS virtual HID -> active strategy | Blocked CoreHID/DriverKit work must not be reframed as primary path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-20 | Repudiation | Fallback decision | mitigate | Require current-phone result and alternate-phone attempt before Windows VHF fallback row. |
| T-07-21 | Information Disclosure | Docs/evidence | mitigate | Redaction scan blocks MACs, serials, account names, screenshots, keys, pairing material, and stable identifiers. |
| T-07-22 | Tampering | Strategy docs | mitigate | Docs state Android HID primary and CoreHID/DriverKit fallback evidence only. |
| T-07-23 | Elevation of Privilege | Legacy macOS security-state steps | mitigate | Docs forbid SIP/system-extension/security-state changes without later explicit approval per D-18. |
| T-07-SC | Tampering | Package installs | accept | No npm/pip/cargo installs in this plan. |
</threat_model>

<verification>
Run docs topic `rg`, alternate-phone/fallback manifest `rg`, validation requirement `rg`, and redaction negative scan. Human verifies fallback gate correctness.
</verification>

<success_criteria>
PACK-03 and PACK-06 pass. If Android HID proof passes, docs and validation reflect it. If blocked, fallback selection follows D-04 and D-19 with alternate-phone evidence and preserves completed Windows VHF route.
</success_criteria>

<output>
Create `.planning/phases/07-macos-virtual-joystick-path/07-06-SUMMARY.md` when done.
</output>
