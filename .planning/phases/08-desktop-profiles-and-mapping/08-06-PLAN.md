---
phase: 08-desktop-profiles-and-mapping
plan: 06
type: execute
wave: 4
depends_on: [08-04]
files_modified:
  - desktop-companion/src/main/kotlin/com/btgun/desktop/control/ProfileMetadata.kt
  - desktop-companion/src/main/kotlin/com/btgun/desktop/control/ControlServer.kt
  - desktop-companion/src/main/kotlin/com/btgun/desktop/transport/UdpInputFrameCodec.kt
  - desktop-companion/src/main/kotlin/com/btgun/desktop/transport/UdpReceivedInput.kt
  - desktop-companion/src/main/kotlin/com/btgun/desktop/backend/UdpControllerStateAdapter.kt
  - desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt
  - desktop-companion/src/test/kotlin/com/btgun/desktop/control/ControlChannelTest.kt
  - desktop-companion/src/test/kotlin/com/btgun/desktop/transport/UdpInputFrameCodecTest.kt
  - desktop-companion/src/test/kotlin/com/btgun/desktop/backend/UdpControllerStateAdapterTest.kt
  - desktop-companion/src/test/kotlin/com/btgun/desktop/ui/PairingWindowTest.kt
autonomous: true
requirements: [PROF-05, PROF-06]
requirements_addressed: [PROF-05, PROF-06]
must_haves:
  truths:
    - "D-05: Desktop only displays active Android profile id/name/revision and mapped-stream status; no edit/create/delete controls."
    - "D-02 and D-04: Desktop consumes mapped product stream and cannot request raw debug extras."
    - "PROF-05 and PROF-06: Runtime Android profile metadata and Default Visualizer metadata are visible on desktop without desktop profile storage."
  artifacts:
    - path: "desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt"
      provides: "Read-only Active Android profile and Mapped stream diagnostics"
    - path: "desktop-companion/src/main/kotlin/com/btgun/desktop/backend/UdpControllerStateAdapter.kt"
      provides: "Mapped UDP input to SemanticControllerState for Windows fallback"
    - path: "desktop-companion/src/main/kotlin/com/btgun/desktop/control/ControlServer.kt"
      provides: "Android profile metadata acceptance without desktop default authority"
  key_links:
    - from: "ControlServer.kt"
      to: "PairingWindow.kt"
      via: "profile metadata callback"
      pattern: "Active Android profile"
    - from: "UdpInputFrameCodec.kt"
      to: "UdpControllerStateAdapter.kt"
      via: "mapped aim fields"
      pattern: "FLAG_MAPPED_PRODUCT_STREAM|aimX|aimY"
---

<objective>
Update desktop companion as read-only mapped-stream consumer.

Purpose: Windows fallback and desktop diagnostics must reflect Android active profile metadata without reintroducing desktop profile authority.
Output: desktop protocol decode, adapter update, read-only UI rows, and tests.
</objective>

## Artifacts this phase produces

- `ProfileMetadata` fields `profileId`, `displayName`, `revision`, `source`, `rawDebugEnabled`.
- Desktop control callback for active Android profile metadata.
- Desktop mapped-stream diagnostics rows `Active Android profile`, `Profile source`, `Mapped stream`, and `Last profile update`.
- Updated UDP decoder/adapter fields for mapped product aim.

<execution_context>
@/Users/lucas.rancez/.codex/gsd-core/workflows/execute-plan.md
@/Users/lucas.rancez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-desktop-profiles-and-mapping/08-CONTEXT.md
@.planning/phases/08-desktop-profiles-and-mapping/08-RESEARCH.md
@.planning/phases/08-desktop-profiles-and-mapping/08-UI-SPEC.md
@desktop-companion/src/main/kotlin/com/btgun/desktop/control/ProfileMetadata.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/control/ControlServer.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/transport/UdpReceivedInput.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/backend/UdpControllerStateAdapter.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Accept Android profile metadata and remove desktop default authority</name>
  <files>desktop-companion/src/main/kotlin/com/btgun/desktop/control/ProfileMetadata.kt, desktop-companion/src/main/kotlin/com/btgun/desktop/control/ControlServer.kt, desktop-companion/src/test/kotlin/com/btgun/desktop/control/ControlChannelTest.kt</files>
  <read_first>
    - `desktop-companion/src/main/kotlin/com/btgun/desktop/control/ProfileMetadata.kt`
    - `desktop-companion/src/main/kotlin/com/btgun/desktop/control/ControlServer.kt`
    - `desktop-companion/src/test/kotlin/com/btgun/desktop/control/ControlChannelTest.kt`
    - `android-host/app/src/main/java/com/btgun/host/session/DesktopControlClient.kt`
  </read_first>
  <behavior>
    - `PROFILE_METADATA` sent from Android is accepted and parsed as active Android profile metadata.
    - Desktop no longer sends a default profile metadata envelope as if it owns `Default profile`.
    - Metadata with missing id/name/revision/source is rejected or ignored without crashing.
    - `source` must display as `android` for Android-owned profile metadata.
  </behavior>
  <action>Extend desktop `ProfileMetadata` with `source: String` and `rawDebugEnabled: Boolean`. Add `ControlServer.onProfileMetadataReceived` or equivalent callback. In `sendInitialMetadata`, keep diagnostics/session/input stream config but remove the desktop default `PROFILE_METADATA` envelope. Parse incoming `PROFILE_METADATA` in `handleAcceptedEnvelope` and invoke the callback after envelope auth/session checks. Do not add desktop save/edit APIs.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p desktop-companion test --tests '*ControlChannel*' --no-daemon --console=plain</automated>
  </verify>
  <acceptance_criteria>Tests prove Android metadata is read-only desktop state and desktop default profile authority is absent.</acceptance_criteria>
  <done>Desktop accepts active Android profile metadata without owning profiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Decode mapped product frames for Windows fallback</name>
  <files>desktop-companion/src/main/kotlin/com/btgun/desktop/transport/UdpInputFrameCodec.kt, desktop-companion/src/main/kotlin/com/btgun/desktop/transport/UdpReceivedInput.kt, desktop-companion/src/main/kotlin/com/btgun/desktop/backend/UdpControllerStateAdapter.kt, desktop-companion/src/test/kotlin/com/btgun/desktop/transport/UdpInputFrameCodecTest.kt, desktop-companion/src/test/kotlin/com/btgun/desktop/backend/UdpControllerStateAdapterTest.kt</files>
  <read_first>
    - `desktop-companion/src/main/kotlin/com/btgun/desktop/transport/UdpInputFrameCodec.kt`
    - `desktop-companion/src/main/kotlin/com/btgun/desktop/transport/UdpReceivedInput.kt`
    - `desktop-companion/src/main/kotlin/com/btgun/desktop/backend/UdpControllerStateAdapter.kt`
    - `android-host/app/src/main/java/com/btgun/host/transport/UdpInputFrameCodec.kt`
  </read_first>
  <behavior>
    - Decoder accepts `FLAG_MAPPED_PRODUCT_STREAM` and exposes mapped `aimX/aimY`.
    - Decoder exposes `rawDebugEnabled` from `FLAG_RAW_DEBUG_EXTRAS`.
    - `UdpControllerStateAdapter.toState` uses mapped product aim, not raw motion fallback, for `SemanticControllerState.aimX/aimY`.
    - Frames without mapped product flag are rejected or marked incompatible for Phase 8 product path.
  </behavior>
  <action>Mirror Android UDP frame flag constants and mapped aim field names in desktop codec. Update `UdpReceivedMotion` or split it into `UdpReceivedMappedAim` plus optional raw debug fields so default path cannot be mistaken for raw provider data. Update adapter tests so Windows fallback receives remapped buttons and mapped aim exactly as sent by Android. Preserve replay guard, auth, HMAC, sequence, stale behavior, and haptic routing.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p desktop-companion test --tests '*UdpInputFrameCodec*' --tests '*UdpControllerStateAdapter*' --no-daemon --console=plain</automated>
  </verify>
  <acceptance_criteria>Desktop tests prove mapped flag decode, raw debug flag decode, mapped aim use, and rejection/diagnostic for unmapped legacy product frames.</acceptance_criteria>
  <done>Windows fallback consumes Android-mapped product state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add read-only desktop diagnostics rows and forbidden-control guard</name>
  <files>desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt, desktop-companion/src/test/kotlin/com/btgun/desktop/ui/PairingWindowTest.kt</files>
  <read_first>
    - `desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt`
    - `.planning/phases/08-desktop-profiles-and-mapping/08-UI-SPEC.md`
    - `desktop-companion/src/main/kotlin/com/btgun/desktop/control/ProfileMetadata.kt`
  </read_first>
  <behavior>
    - Diagnostics include `Active Android profile: {name} | id={profile_id} | rev={revision}`.
    - Diagnostics include `Profile source: android` or `unknown`.
    - Diagnostics include `Mapped stream: {active/grace/stale/stopped} | mapped={true/false} | raw_debug={on/off}`.
    - Diagnostics include `Last profile update: none` or elapsed/status text.
    - Desktop UI has no profile create/edit/rename/delete/sensitivity/dead-zone/smoothing/inversion/recenter/raw-request controls.
  </behavior>
  <action>Add read-only metadata fields to `PairingWindow` diagnostics HTML. Wire `ControlServer.onProfileMetadataReceived` and UDP mapped/raw flags into these fields. Keep allowed desktop controls limited to Start pairing, Restart pairing, and existing authenticated haptic smoke button. Add source guards in tests for forbidden labels from UI-SPEC.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p desktop-companion test --tests '*PairingWindow*' --no-daemon --console=plain</automated>
    <automated>! rg -n "Edit desktop profile|Desktop profile editor|Request raw stream|Save profile|Duplicate profile|Hold-to-recenter" desktop-companion/src/main/kotlin desktop-companion/src/test/kotlin</automated>
  </verify>
  <acceptance_criteria>Desktop diagnostics show active Android profile and mapped stream status; forbidden-control guard is clean.</acceptance_criteria>
  <done>Desktop companion stays read-only for Phase 8 profiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Android metadata -> desktop UI | Metadata must not become desktop authority or leak secrets. |
| Authenticated UDP -> Windows fallback backend | Desktop must use mapped product values, not raw debug values, for product state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Tampering | Desktop profile metadata | mitigate | Desktop accepts metadata only after authenticated session and exposes read-only rows. |
| T-08-11 | Information Disclosure | Desktop mapped diagnostics | mitigate | Raw debug state displayed only as on/off; no raw values in default UI. |
| T-08-SC | Tampering | Package installs | accept | No dependency install in this plan. |
</threat_model>

<verification>
Run desktop control, transport, adapter, and PairingWindow tests plus forbidden-label grep.
</verification>

<success_criteria>
Desktop companion displays active Android profile metadata and mapped-stream state without profile editing or raw-stream request authority.
</success_criteria>

<output>
Create `.planning/phases/08-desktop-profiles-and-mapping/08-06-SUMMARY.md` when done.
</output>
