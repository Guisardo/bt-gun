---
phase: 08-desktop-profiles-and-mapping
plan: 04
type: execute
wave: 3
depends_on: [08-03]
files_modified:
  - android-host/app/src/main/java/com/btgun/host/HostSessionService.kt
  - android-host/app/src/main/java/com/btgun/host/hid/BtGunHidReportPacker.kt
  - android-host/app/src/main/java/com/btgun/host/transport/AndroidUdpInputSender.kt
  - android-host/app/src/main/java/com/btgun/host/transport/UdpInputFrameCodec.kt
  - android-host/app/src/main/java/com/btgun/host/session/DesktopControlClient.kt
  - android-host/app/src/test/java/com/btgun/host/HostSessionServiceLivenessTest.kt
  - android-host/app/src/test/java/com/btgun/host/hid/BtGunHidReportPackerTest.kt
  - android-host/app/src/test/java/com/btgun/host/transport/AndroidUdpInputSenderTest.kt
  - android-host/app/src/test/java/com/btgun/host/session/DesktopControlClientTest.kt
autonomous: true
requirements: [PROF-02, PROF-03, PROF-04, PROF-05, PROF-06]
requirements_addressed: [PROF-02, PROF-03, PROF-04, PROF-05, PROF-06]
must_haves:
  truths:
    - "D-01 through D-05: HostSessionService applies Android active profile before HID/LAN fanout and sends active Android profile metadata to desktop."
    - "D-02 through D-04: UDP product frames are mapped by default; raw provider/motion extras appear only when the Android raw debug toggle is on."
    - "D-08 through D-13: Runtime output preserves profile smoothing, button remap, and physical recenter semantics without changing HID descriptor shape."
  artifacts:
    - path: "android-host/app/src/main/java/com/btgun/host/HostSessionService.kt"
      provides: "Active profile load/apply and live mapped output fanout"
    - path: "android-host/app/src/main/java/com/btgun/host/hid/BtGunHidReportPacker.kt"
      provides: "Mapped state to stable Android Bluetooth HID report"
    - path: "android-host/app/src/main/java/com/btgun/host/transport/AndroidUdpInputSender.kt"
      provides: "Mapped default UDP stream with Android raw debug flag"
  key_links:
    - from: "ProfileMapper.kt"
      to: "HostSessionService.kt"
      via: "active profile mapped before outputs"
      pattern: "ProfileMapper.map|MappedControllerState"
    - from: "HostSessionService.kt"
      to: "DesktopControlClient.kt"
      via: "Android profile metadata envelope"
      pattern: "PROFILE_METADATA"
---

<objective>
Wire Android active profiles into HID, LAN, and control metadata.

Purpose: Profile changes must affect live output at runtime without Android rebuild, while desktop remains a consumer of mapped state.
Output: service integration, mapped HID reports, mapped UDP product frames, raw debug toggle plumbing, Android profile metadata send path, and tests.
</objective>

## Artifacts this phase produces

- Host service active profile state and profile reload/apply hook.
- `BtGunHidReportPacker.packInputReport(mappedState, stale)` overload or equivalent.
- UDP `streamFlags` constants `FLAG_MAPPED_PRODUCT_STREAM=0x0001` and `FLAG_RAW_DEBUG_EXTRAS=0x0002`.
- Android control envelope sender for active profile metadata.
- Tests proving runtime profile changes affect HID/LAN output without rebuild.

<execution_context>
@/Users/lucas.rancez/.codex/gsd-core/workflows/execute-plan.md
@/Users/lucas.rancez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-desktop-profiles-and-mapping/08-CONTEXT.md
@.planning/phases/08-desktop-profiles-and-mapping/08-RESEARCH.md
@.planning/phases/08-desktop-profiles-and-mapping/08-PATTERNS.md
@android-host/app/src/main/java/com/btgun/host/HostSessionService.kt
@android-host/app/src/main/java/com/btgun/host/hid/BtGunHidReportPacker.kt
@android-host/app/src/main/java/com/btgun/host/transport/AndroidUdpInputSender.kt
@android-host/app/src/main/java/com/btgun/host/session/DesktopControlClient.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add active profile state to HostSessionService</name>
  <files>android-host/app/src/main/java/com/btgun/host/HostSessionService.kt, android-host/app/src/test/java/com/btgun/host/HostSessionServiceLivenessTest.kt</files>
  <read_first>
    - `android-host/app/src/main/java/com/btgun/host/HostSessionService.kt`
    - `android-host/app/src/main/java/com/btgun/host/profile/ProfileStore.kt`
    - `android-host/app/src/main/java/com/btgun/host/profile/ProfileMapper.kt`
    - `android-host/app/src/main/java/com/btgun/host/recenter/ReloadHoldRecenter.kt`
  </read_first>
  <behavior>
    - Service loads `ProfileStore` default profile on startup and exposes active profile id/name/revision in `HostSessionState`.
    - Profile reload/select action updates active profile without app rebuild or service restart.
    - Recenter hold uses `profile.recenterPhysicalControl` against physical input while normal mapped virtual reload events still publish.
    - Mapping happens before `hidSessionController.fanOutLiveInput` and before UDP snapshot/edge send.
  </behavior>
  <action>Add profile store and mapper dependencies to `HostSessionService`. Add explicit service action `ACTION_RELOAD_ACTIVE_PROFILE` or an internal public method used by Activity after save/select. Store `activeProfile`, `mappedControllerState`, `profileValidationError`, `rawDebugEnabled`, and smoothing status in `HostSessionState`. Replace reload-only recenter checks with selected physical recenter control while preserving two-second hold timing. Do not add desktop profile edits or Phase 9 visualizer data.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=/Users/lucas.rancez/Library/Android/sdk GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p android-host testDebugUnitTest --tests '*HostSessionService*' --tests '*ProfileMapper*' --no-daemon --console=plain</automated>
  </verify>
  <acceptance_criteria>Tests prove active profile load, runtime profile reload, mapped fanout order, and physical recenter selection independent of virtual reload mapping.</acceptance_criteria>
  <done>Foreground service applies the active Android profile at runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Feed mapped state into Android Bluetooth HID without descriptor drift</name>
  <files>android-host/app/src/main/java/com/btgun/host/hid/BtGunHidReportPacker.kt, android-host/app/src/test/java/com/btgun/host/hid/BtGunHidReportPackerTest.kt, android-host/app/src/main/java/com/btgun/host/HostSessionService.kt</files>
  <read_first>
    - `android-host/app/src/main/java/com/btgun/host/hid/BtGunHidDescriptor.kt`
    - `android-host/app/src/main/java/com/btgun/host/hid/BtGunHidReportPacker.kt`
    - `android-host/app/src/main/java/com/btgun/host/profile/ProfileMapper.kt`
    - `android-host/app/src/test/java/com/btgun/host/hid/BtGunHidReportPackerTest.kt`
  </read_first>
  <behavior>
    - Mapped virtual trigger/reload/X/Y/A/B bits pack into the existing report ID 1 payload.
    - Mapped stick and aim axes use existing signed int16 little-endian byte positions.
    - Stale report still clears buttons/stick and preserves selected aim behavior.
    - `BtGunHidDescriptor` report bytes do not change.
  </behavior>
  <action>Add a mapped-state packing path while preserving the existing raw `GunInputState` compatibility path until callers migrate. Update `HostSessionHidController.fanOutLiveInput` to send mapped state. Keep report ID, payload length, button bit positions, axis endian, and Y inversion exactly as Phase 7 tests expect.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=/Users/lucas.rancez/Library/Android/sdk GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p android-host testDebugUnitTest --tests '*BtGunHidReportPacker*' --tests '*HostSessionService*' --no-daemon --console=plain</automated>
  </verify>
  <acceptance_criteria>Golden HID tests pass with mapped input; descriptor constants and payload length remain unchanged.</acceptance_criteria>
  <done>Android Bluetooth HID output consumes profile-mapped state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Send mapped UDP product stream and Android profile metadata</name>
  <files>android-host/app/src/main/java/com/btgun/host/transport/AndroidUdpInputSender.kt, android-host/app/src/main/java/com/btgun/host/transport/UdpInputFrameCodec.kt, android-host/app/src/main/java/com/btgun/host/session/DesktopControlClient.kt, android-host/app/src/test/java/com/btgun/host/transport/AndroidUdpInputSenderTest.kt, android-host/app/src/test/java/com/btgun/host/session/DesktopControlClientTest.kt</files>
  <read_first>
    - `android-host/app/src/main/java/com/btgun/host/transport/AndroidUdpInputSender.kt`
    - `android-host/app/src/main/java/com/btgun/host/transport/UdpInputFrameCodec.kt`
    - `android-host/app/src/main/java/com/btgun/host/session/DesktopControlClient.kt`
    - `docs/protocol/lan-pairing-v1.md`
  </read_first>
  <behavior>
    - Default UDP frames set `FLAG_MAPPED_PRODUCT_STREAM` and do not include raw provider/yaw/pitch/roll/raw aim extras.
    - When Android raw debug is enabled, frames also set `FLAG_RAW_DEBUG_EXTRAS` and include provider/capability/yaw/pitch/roll/raw aim/source timestamp fields.
    - Mapped product aim is carried as finite normalized `aimX/aimY` at the existing aim offsets with explicit mapped semantics.
    - Android sends `PROFILE_METADATA` with `profileId`, `displayName`, `revision`, `source=android`, and `rawDebugEnabled` after authentication and whenever active profile/raw-debug state changes.
  </behavior>
  <action>Add `streamFlags` to Android UDP frame codec at offset 6, using bit 0 for mapped product stream and bit 1 for raw debug extras. Rename internal fields away from `rawAimX/rawAimY` where practical, or add accessors that make product aim semantics explicit. Default raw debug off: provider/capability/yaw/pitch/roll/source sensor fields should be neutral/absent by documented convention. Add `DesktopControlClient.sendProfileMetadata(profileMetadata)` or equivalent and call it from HostSessionService after authenticated session and profile changes. Do not add a desktop request for raw mode.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=/Users/lucas.rancez/Library/Android/sdk GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p android-host testDebugUnitTest --tests '*AndroidUdpInputSender*' --tests '*DesktopControlClient*' --no-daemon --console=plain</automated>
  </verify>
  <acceptance_criteria>Tests prove mapped flag/default raw-off behavior, raw-debug-on behavior, metadata send body, and absence of any desktop raw-stream request path.</acceptance_criteria>
  <done>Android publishes mapped product stream and active profile metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Profile store -> service output | Runtime active profile controls HID and LAN behavior. |
| Android UDP sender -> desktop receiver | Raw debug data must not leak by default. |
| Android control client -> desktop diagnostics | Profile metadata must contain no secrets and no desktop authority transfer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering | `HostSessionService` output fanout | mitigate | Tests prove mapped state is the only HID/LAN product source. |
| T-08-07 | Information Disclosure | UDP debug extras | mitigate | Raw extras require Android session toggle and explicit frame flag. |
| T-08-08 | Spoofing | Profile metadata | mitigate | Metadata travels only inside authenticated control session and contains no authority grant. |
| T-08-SC | Tampering | Package installs | accept | No dependency install in this plan. |
</threat_model>

<verification>
Run focused service, HID, UDP sender, and control-client tests.
</verification>

<success_criteria>
PROF-02 through PROF-06 apply at runtime: selected Android profile affects HID and LAN output, metadata reaches desktop, and raw debug remains off unless Android user enables it.
</success_criteria>

<output>
Create `.planning/phases/08-desktop-profiles-and-mapping/08-04-SUMMARY.md` when done.
</output>
