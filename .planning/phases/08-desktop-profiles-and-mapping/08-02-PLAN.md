---
phase: 08-desktop-profiles-and-mapping
plan: 02
type: execute
wave: 1
depends_on: [08-01]
files_modified:
  - android-host/app/src/main/java/com/btgun/host/profile/ProfileModels.kt
  - android-host/app/src/main/java/com/btgun/host/profile/ProfileValidation.kt
  - android-host/app/src/main/java/com/btgun/host/profile/ProfileStore.kt
  - android-host/app/src/test/java/com/btgun/host/profile/ProfileValidationTest.kt
  - android-host/app/src/test/java/com/btgun/host/profile/ProfileStoreTest.kt
autonomous: true
requirements: [PROF-01, PROF-04, PROF-06]
requirements_addressed: [PROF-01, PROF-04, PROF-06]
must_haves:
  truths:
    - "D-01 and PROF-01: Android stores all Phase 8 profiles locally; desktop profile storage stays absent."
    - "D-10, D-11, D-12, D-13, and D-14 plus PROF-04: physical trigger/reload/X/Y/A/B can map only to v1 virtual buttons, stick/aim axes stay semantic, hold-to-recenter can move to another physical button, and exactly one physical hold-to-recenter control is required."
    - "D-15, D-16, D-17, and D-18 plus PROF-06: `Default Visualizer` is immutable and built in; users create profiles by duplicating, then rename, edit, select, and delete user-created profiles; reset restores default-derived settings; import/export is deferred."
  artifacts:
    - path: "android-host/app/src/main/java/com/btgun/host/profile/ProfileModels.kt"
      provides: "Versioned profile schema and defaults"
    - path: "android-host/app/src/main/java/com/btgun/host/profile/ProfileValidation.kt"
      provides: "Profile save gate and validation labels"
    - path: "android-host/app/src/main/java/com/btgun/host/profile/ProfileStore.kt"
      provides: "SharedPreferences-backed Android-local profile store"
  key_links:
    - from: "ProfileStore.kt"
      to: "ProfileValidation.kt"
      via: "save rejects invalid profiles"
      pattern: "SaveProfileResult|ProfileValidationError"
    - from: "ProfileModels.kt"
      to: "08-UI-SPEC.md"
      via: "required copy/default values"
      pattern: "Default Visualizer|Low|0.03"
---

<objective>
Create Android profile schema, store, and validation foundation.

Purpose: Android must have a durable, testable profile authority before mapper, UI, HID, or LAN output changes.
Output: profile model classes, default document, validator, store, and focused tests.
</objective>

## Artifacts this phase produces

- Package `com.btgun.host.profile`.
- Enum `PhysicalButton` with `trigger`, `reload`, `button_x`, `button_y`, `button_a`, `button_b`.
- Enum `VirtualButton` with the same six outputs.
- Enum `SmoothingMode` with `OFF`, `LOW`, `BALANCED`, `HIGH`, `ADAPTIVE`.
- Data classes `AimMappingSettings`, `ProviderAimOverrides`, `BtGunProfile`, `ProfileDocument`.
- Constant/profile id `default_visualizer`.
- Validation labels from `08-UI-SPEC.md`.
- Class `ProfileStore`.

<execution_context>
@/Users/lucas.rancez/.codex/gsd-core/workflows/execute-plan.md
@/Users/lucas.rancez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-desktop-profiles-and-mapping/08-CONTEXT.md
@.planning/phases/08-desktop-profiles-and-mapping/08-RESEARCH.md
@.planning/phases/08-desktop-profiles-and-mapping/08-UI-SPEC.md
@.planning/phases/08-desktop-profiles-and-mapping/08-PATTERNS.md
@android-host/app/src/main/java/com/btgun/host/session/TrustedDesktopStore.kt
@android-host/app/src/main/java/com/btgun/host/motion/AimCalibration.kt
@android-host/app/build.gradle.kts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define profile model defaults and immutable built-in profile</name>
  <files>android-host/app/src/main/java/com/btgun/host/profile/ProfileModels.kt, android-host/app/src/test/java/com/btgun/host/profile/ProfileStoreTest.kt</files>
  <read_first>
    - `.planning/phases/08-desktop-profiles-and-mapping/08-UI-SPEC.md`
    - `android-host/app/src/main/java/com/btgun/host/model/NormalizedEvents.kt`
    - `android-host/app/src/main/java/com/btgun/host/hid/BtGunHidDescriptor.kt`
  </read_first>
  <behavior>
    - `ProfileDocument.defaults()` contains one built-in profile named `Default Visualizer` with `profileId=default_visualizer`, `revision=1`, `builtIn=true`, active by default.
    - Default aim settings are `sensitivity=1.0`, `invertX=false`, `invertY=false`, `deadZone=0.03`, `smoothing=LOW`.
    - Default button mapping is one-to-one for trigger, reload, X, Y, A, B.
    - Default recenter physical control is `reload`.
  </behavior>
  <action>Add serializable profile model types in `ProfileModels.kt`. Use exact control ids `trigger`, `reload`, `button_x`, `button_y`, `button_a`, `button_b` for Android-side physical controls and virtual outputs. Include provider override keys for `calibrated_fused_rotation`, `gyro_raw_aim`, and `tilt_fallback`, each defaulting to shared settings. Add helpers `BtGunProfile.defaultVisualizer()` and `ProfileDocument.defaults()`. Do not add desktop types or import/export fields beyond an explicit deferred placeholder/comment in tests if needed.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=/Users/lucas.rancez/Library/Android/sdk GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p android-host testDebugUnitTest --tests '*ProfileStore*' --no-daemon --console=plain</automated>
  </verify>
  <acceptance_criteria>Tests assert default id/name/revision, default aim settings, one-to-one mapping, built-in immutability flag, and default active profile id.</acceptance_criteria>
  <done>Android has a versioned built-in profile model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add profile validator with blocking labels</name>
  <files>android-host/app/src/main/java/com/btgun/host/profile/ProfileValidation.kt, android-host/app/src/test/java/com/btgun/host/profile/ProfileValidationTest.kt</files>
  <read_first>
    - `android-host/app/src/main/java/com/btgun/host/profile/ProfileModels.kt`
    - `.planning/phases/08-desktop-profiles-and-mapping/08-UI-SPEC.md`
    - `.planning/phases/08-desktop-profiles-and-mapping/08-CONTEXT.md`
  </read_first>
  <behavior>
    - Blank profile name returns `Name required`.
    - Missing each virtual output returns `Missing trigger output`, `Missing reload output`, `Missing X output`, `Missing Y output`, `Missing A output`, or `Missing B output`.
    - Duplicate virtual output returns `Duplicate output`.
    - Missing recenter control returns `Recenter required`.
    - Any axis/button crossing attempt returns `Unsupported axis mapping`.
  </behavior>
  <action>Implement `ProfileValidator.validate(profile): List<ProfileValidationError>` and stable `label` strings matching `08-UI-SPEC.md`. Validate numeric aim settings are finite, sensitivity is positive, dead zone is within `0.0..0.5`, and provider overrides either use shared settings or valid aim settings. Do not silently repair invalid mappings. Keep recenter validation based on physical button id, not virtual output.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=/Users/lucas.rancez/Library/Android/sdk GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p android-host testDebugUnitTest --tests '*ProfileValidation*' --no-daemon --console=plain</automated>
  </verify>
  <acceptance_criteria>Every validation label from the UI spec is covered by tests; valid Default Visualizer returns no errors.</acceptance_criteria>
  <done>Invalid profiles are blocked with exact labels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist Android-local profiles and revisions</name>
  <files>android-host/app/src/main/java/com/btgun/host/profile/ProfileStore.kt, android-host/app/src/test/java/com/btgun/host/profile/ProfileStoreTest.kt</files>
  <read_first>
    - `android-host/app/src/main/java/com/btgun/host/session/TrustedDesktopStore.kt`
    - `android-host/app/src/main/java/com/btgun/host/motion/AimCalibration.kt`
    - `android-host/app/src/main/java/com/btgun/host/profile/ProfileModels.kt`
    - `android-host/app/src/main/java/com/btgun/host/profile/ProfileValidation.kt`
  </read_first>
  <behavior>
    - Empty store loads default document.
    - Malformed JSON loads default document and records a non-crashing rejected/repair status for dashboard use.
    - Duplicate creates a user-editable copy with new id, `builtIn=false`, name suffix, and revision `1`.
    - Rename/edit/select/delete increments document/profile revision where applicable.
    - Built-in profile cannot be edited or deleted.
    - Reset restores `ProfileDocument.defaults()`.
  </behavior>
  <action>Add `ProfileStore(context)` backed by SharedPreferences name `bt_gun_profiles` and key `profiles_v1`. Use existing `kotlinx.serialization-json` only. Add result types `ProfileStoreLoadResult` and `SaveProfileResult` so callers can distinguish loaded/defaulted/rejected states. Ensure `deleteProfile(default_visualizer)` returns rejected. Ensure active profile falls back to default if the selected user profile is deleted. Do not write desktop files or any import/export path.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=/Users/lucas.rancez/Library/Android/sdk GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p android-host testDebugUnitTest --tests '*ProfileStore*' --tests '*ProfileValidation*' --no-daemon --console=plain</automated>
  </verify>
  <acceptance_criteria>Store tests prove default fallback, malformed JSON safety, duplicate/edit/select/delete/reset semantics, revision increments, and immutable built-in behavior.</acceptance_criteria>
  <done>Android can load and save local profiles safely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SharedPreferences JSON -> runtime profile | Local persisted data can be malformed or stale. |
| Profile editor candidate -> saved active profile | User changes can create unsafe mapping unless blocked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Denial of Service | `ProfileStore` | mitigate | Malformed JSON defaults safely and never crashes foreground service. |
| T-08-04 | Tampering | `ProfileValidator` | mitigate | Reject missing, duplicate, unsupported, or recenter-free mappings before save. |
| T-08-SC | Tampering | Package installs | accept | No new dependency install in this plan. |
</threat_model>

<verification>
Run focused Android profile store and validation tests.
</verification>

<success_criteria>
PROF-01, PROF-04, and PROF-06 have an Android-local, validated, built-in default profile foundation with no desktop profile store.
</success_criteria>

<output>
Create `.planning/phases/08-desktop-profiles-and-mapping/08-02-SUMMARY.md` when done.
</output>
