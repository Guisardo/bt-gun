---
phase: 08-desktop-profiles-and-mapping
plan: 05
type: execute
wave: 4
depends_on: [08-02, 08-03, 08-04]
files_modified:
  - android-host/app/src/main/java/com/btgun/host/ui/DashboardState.kt
  - android-host/app/src/main/java/com/btgun/host/MainActivity.kt
  - android-host/app/src/test/java/com/btgun/host/ui/DashboardStateTest.kt
  - android-host/app/src/test/java/com/btgun/host/profile/ProfileValidationTest.kt
  - android-host/app/src/test/java/com/btgun/host/profile/ProfileStoreTest.kt
autonomous: true
requirements: [PROF-01, PROF-02, PROF-03, PROF-04, PROF-05, PROF-06]
requirements_addressed: [PROF-01, PROF-02, PROF-03, PROF-04, PROF-05, PROF-06]
must_haves:
  truths:
    - "D-03, D-04, and D-19: Android UI exposes raw debug toggle and full provider override editing in the main profile flow."
    - "D-14: Save is blocked with visible validation labels and no silent auto-repair."
    - "D-15, D-16, and D-17: UI supports immutable built-in Default Visualizer plus duplicate, rename, edit, select, delete, and reset for user profiles."
  artifacts:
    - path: "android-host/app/src/main/java/com/btgun/host/ui/DashboardState.kt"
      provides: "Profile dashboard rows and validation/raw-debug formatters"
    - path: "android-host/app/src/main/java/com/btgun/host/MainActivity.kt"
      provides: "Native Android profile list/editor/actions"
  key_links:
    - from: "MainActivity.kt"
      to: "ProfileStore.kt"
      via: "profile list/editor commands"
      pattern: "Duplicate profile|Save profile|Use profile"
    - from: "DashboardState.kt"
      to: "HostSessionState"
      via: "active profile and smoothing status"
      pattern: "Active profile|Profile mapping|Raw debug stream"
---

<objective>
Build Android profile management UI and dashboard profile rows.

Purpose: User needs an advanced but compact native Android UI to create/select/edit profiles and see active mapped state before Phase 9 visualizer.
Output: dashboard rows, Edit profiles action, profile list, profile editor, validation display, and tests.
</objective>

## Artifacts this phase produces

- Dashboard rows `Active profile`, `Profile mapping`, `Recenter control`, `Raw debug stream`, and `Profile error`.
- Activity action `Edit profiles`.
- Native profile list and editor surface in `MainActivity` or a small helper class.
- UI controls for shared aim settings, provider overrides, button mapping, recenter button, and raw debug toggle.

<execution_context>
@/Users/lucas.rancez/.codex/gsd-core/workflows/execute-plan.md
@/Users/lucas.rancez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-desktop-profiles-and-mapping/08-CONTEXT.md
@.planning/phases/08-desktop-profiles-and-mapping/08-UI-SPEC.md
@.planning/phases/08-desktop-profiles-and-mapping/08-PATTERNS.md
@android-host/app/src/main/java/com/btgun/host/MainActivity.kt
@android-host/app/src/main/java/com/btgun/host/ui/DashboardState.kt
@android-host/app/src/main/java/com/btgun/host/profile/ProfileModels.kt
@android-host/app/src/main/java/com/btgun/host/profile/ProfileStore.kt
@android-host/app/src/main/java/com/btgun/host/profile/ProfileValidation.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add profile dashboard rows and raw debug status</name>
  <files>android-host/app/src/main/java/com/btgun/host/ui/DashboardState.kt, android-host/app/src/test/java/com/btgun/host/ui/DashboardStateTest.kt</files>
  <read_first>
    - `android-host/app/src/main/java/com/btgun/host/ui/DashboardState.kt`
    - `android-host/app/src/main/java/com/btgun/host/HostSessionService.kt`
    - `.planning/phases/08-desktop-profiles-and-mapping/08-UI-SPEC.md`
  </read_first>
  <behavior>
    - Dashboard renders `Active profile: {name} | id={profile_id} | rev={revision} | built_in={true/false}`.
    - Dashboard renders `Profile mapping: mapped | sensitivity={value} | dead_zone={value} | smoothing={mode} | filter_lag<={ms}ms`.
    - Dashboard renders `Recenter control: hold {physical_button} for 2000ms`.
    - Dashboard renders `Raw debug stream: off` or `Raw debug stream: on | Android session controlled`.
    - Dashboard renders `Profile error: none` or a validation label.
  </behavior>
  <action>Extend `DashboardState` with profile fields sourced from `HostSessionState`. Keep scan order from UI-SPEC: active profile state, profile actions, validation/raw-debug status, diagnostics. Add deterministic tests for built-in active, user profile active, validation blocked, raw debug on/off, and smoothing adaptive fallback text.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=/Users/lucas.rancez/Library/Android/sdk GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p android-host testDebugUnitTest --tests '*DashboardState*' --no-daemon --console=plain</automated>
  </verify>
  <acceptance_criteria>Dashboard tests assert exact row copy and no desktop-owned copy labels.</acceptance_criteria>
  <done>Dashboard shows profile state before lower-priority diagnostics.</done>
</task>

<task type="auto">
  <name>Task 2: Add native profile list and editor flow</name>
  <files>android-host/app/src/main/java/com/btgun/host/MainActivity.kt, android-host/app/src/test/java/com/btgun/host/profile/ProfileStoreTest.kt</files>
  <read_first>
    - `android-host/app/src/main/java/com/btgun/host/MainActivity.kt`
    - `android-host/app/src/main/java/com/btgun/host/profile/ProfileStore.kt`
    - `android-host/app/src/main/java/com/btgun/host/profile/ProfileModels.kt`
    - `.planning/phases/08-desktop-profiles-and-mapping/08-UI-SPEC.md`
  </read_first>
  <action>Add `Edit profiles` action to `MainActivity`. Implement a scrollable native profile management surface using existing programmatic Views: built-in `Default Visualizer` row first, `Built-in` and `Active` labels, built-in actions `Use profile` and `Duplicate profile`, user actions `Use profile`, `Edit profile`, `Duplicate profile`, `Delete profile`. Show empty state `No user profiles` and `Duplicate Default Visualizer to create an editable profile.` only when no user-created profiles exist. Keep all buttons at least 48dp high and use existing action-group layout patterns.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=/Users/lucas.rancez/Library/Android/sdk GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p android-host testDebugUnitTest --tests '*ProfileStore*' --tests '*DashboardState*' --no-daemon --console=plain</automated>
    <automated>rg -n "Edit profiles|Default Visualizer|Duplicate profile|Use profile|No user profiles" android-host/app/src/main/java/com/btgun/host/MainActivity.kt</automated>
  </verify>
  <acceptance_criteria>Source contains required profile list copy; built-in delete/edit is absent; user delete path cannot delete `default_visualizer`.</acceptance_criteria>
  <done>Android has profile list and selection entry point.</done>
</task>

<task type="auto">
  <name>Task 3: Add advanced editor controls and validation-blocked save</name>
  <files>android-host/app/src/main/java/com/btgun/host/MainActivity.kt, android-host/app/src/test/java/com/btgun/host/profile/ProfileValidationTest.kt</files>
  <read_first>
    - `android-host/app/src/main/java/com/btgun/host/MainActivity.kt`
    - `android-host/app/src/main/java/com/btgun/host/profile/ProfileValidation.kt`
    - `android-host/app/src/main/java/com/btgun/host/profile/ProfileModels.kt`
    - `.planning/phases/08-desktop-profiles-and-mapping/08-UI-SPEC.md`
  </read_first>
  <action>Add editor order exactly as UI-SPEC: profile name; shared aim settings; provider override groups `Calibrated and fused rotation`, `Gyro and raw aim`, `Accelerometer and gravity tilt fallback`; button mapping rows for physical trigger/reload/X/Y/A/B; single-choice `Hold-to-recenter button`; validation summary; `Save profile`. Use sliders, checkboxes, spinners, or numeric inputs available in Android native Views. Show all provider groups in the main flow with a `Use shared settings` checkbox. Disable or reject `Save profile` when validation errors exist and show `Save blocked` plus inline labels. Add `Send raw debug data` toggle that updates Android session/store state and never sends a desktop raw request.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=/Users/lucas.rancez/Library/Android/sdk GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p android-host testDebugUnitTest --tests '*ProfileValidation*' --tests '*ProfileStore*' --tests '*DashboardState*' --no-daemon --console=plain</automated>
    <automated>rg -n "Save profile|Save blocked|Hold-to-recenter button|Calibrated and fused rotation|Gyro and raw aim|Accelerometer and gravity tilt fallback|Send raw debug data" android-host/app/src/main/java/com/btgun/host/MainActivity.kt</automated>
  </verify>
  <acceptance_criteria>Editor exposes provider overrides in main flow, blocks invalid saves with UI-SPEC labels, offers only six button outputs, and has no axis remap controls.</acceptance_criteria>
  <done>Android user can edit complete Phase 8 profile model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User profile form -> saved profile | UI can submit invalid or unsafe mapping. |
| Raw debug UI -> LAN stream | Toggle can expose raw/provider motion extras. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Tampering | Profile editor | mitigate | Save button/result is blocked by `ProfileValidator`; no silent repair. |
| T-08-09 | Information Disclosure | Raw debug toggle | mitigate | UI labels toggle as Android-session controlled and default off. |
| T-08-SC | Tampering | Package installs | accept | No new UI dependency or package install in this plan. |
</threat_model>

<verification>
Run focused Android dashboard/profile tests and source-copy guards.
</verification>

<success_criteria>
User can duplicate, rename, edit, select, delete user profiles, see validation errors, and toggle raw debug from Android UI only.
</success_criteria>

<output>
Create `.planning/phases/08-desktop-profiles-and-mapping/08-05-SUMMARY.md` when done.
</output>
