---
phase: 08-desktop-profiles-and-mapping
plan: 03
type: execute
wave: 2
depends_on: [08-02]
files_modified:
  - android-host/app/src/main/java/com/btgun/host/profile/AdaptiveAimSmoother.kt
  - android-host/app/src/main/java/com/btgun/host/profile/ProfileMapper.kt
  - android-host/app/src/test/java/com/btgun/host/profile/AdaptiveAimSmootherTest.kt
  - android-host/app/src/test/java/com/btgun/host/profile/ProfileMapperTest.kt
autonomous: true
requirements: [PROF-02, PROF-03, PROF-04]
requirements_addressed: [PROF-02, PROF-03, PROF-04]
must_haves:
  truths:
    - "D-06 and D-07: Default profile uses balanced responsive feel while normal editing supports shared settings plus provider-specific overrides."
    - "D-08 and D-09: Smoothing is latency-capped and reports/limits added lag, falling back to Low when adaptive lag threatens the latency target."
    - "D-10, D-11, D-12, and D-13: Button remap is limited to the six v1 buttons; stick/aim axes stay semantic, recenter can move off reload to another physical button, and recenter remains exactly one physical control."
  artifacts:
    - path: "android-host/app/src/main/java/com/btgun/host/profile/ProfileMapper.kt"
      provides: "MappedControllerState from GunInputState plus MotionSample plus profile"
    - path: "android-host/app/src/main/java/com/btgun/host/profile/AdaptiveAimSmoother.kt"
      provides: "Latency-capped smoothing with fallback status"
  key_links:
    - from: "ProfileMapper.kt"
      to: "BtGunHidReportPacker.kt"
      via: "mapped state handed to output adapters in Plan 04"
      pattern: "MappedControllerState"
    - from: "AdaptiveAimSmoother.kt"
      to: "DashboardState.kt"
      via: "smoothing status fields in Plan 05"
      pattern: "estimatedFilterLagMillis|adaptiveFallback"
---

<objective>
Implement pure Android profile mapping and adaptive smoothing.

Purpose: The risky math and remap rules must be deterministic and testable before service, HID, LAN, and UI wiring.
Output: pure mapper/smoother classes and tests for provider overrides, remap rules, and latency fallback.
</objective>

## Artifacts this phase produces

- Data class `MappedControllerState`.
- Data class `MappedAimStatus`.
- Class/object `ProfileMapper`.
- Class `AdaptiveAimSmoother`.
- Constants for smoothing tau values and `MAX_ADDED_FILTER_LAG_MS=12`.

<execution_context>
@/Users/lucas.rancez/.codex/gsd-core/workflows/execute-plan.md
@/Users/lucas.rancez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-desktop-profiles-and-mapping/08-CONTEXT.md
@.planning/phases/08-desktop-profiles-and-mapping/08-RESEARCH.md
@.planning/phases/08-desktop-profiles-and-mapping/08-UI-SPEC.md
@.planning/phases/08-desktop-profiles-and-mapping/08-PATTERNS.md
@android-host/app/src/main/java/com/btgun/host/model/NormalizedEvents.kt
@android-host/app/src/main/java/com/btgun/host/motion/PreviewAimMapper.kt
@android-host/app/src/main/java/com/btgun/host/profile/ProfileModels.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add adaptive smoother tests and implementation</name>
  <files>android-host/app/src/main/java/com/btgun/host/profile/AdaptiveAimSmoother.kt, android-host/app/src/test/java/com/btgun/host/profile/AdaptiveAimSmootherTest.kt</files>
  <read_first>
    - `.planning/phases/08-desktop-profiles-and-mapping/08-RESEARCH.md`
    - `android-host/app/src/main/java/com/btgun/host/model/NormalizedEvents.kt`
    - `android-host/app/src/main/java/com/btgun/host/profile/ProfileModels.kt`
  </read_first>
  <behavior>
    - `OFF` returns current aim with `filterLagMillis=0`.
    - `LOW`, `BALANCED`, and `HIGH` use deterministic tau values `12`, `24`, and `40` ms.
    - `ADAPTIVE` uses a smaller tau during fast motion and larger tau during jitter.
    - If estimated added lag exceeds `12 ms` or `aimLatencyMillis` leaves less than `20 ms` headroom to the 50 ms target, output status reports `mode=LOW` and `adaptiveFallback=true`.
  </behavior>
  <action>Implement timestamp-driven smoothing with `alpha = dt / (tau + dt)` using elapsed nanoseconds. Clamp outputs to `[-1.0, 1.0]`. Return `SmoothingResult(x, y, modeLabel, estimatedFilterLagMillis, adaptiveFallback)` for dashboard and mapper use. Avoid wall-clock timestamps and Android framework APIs.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=/Users/lucas.rancez/Library/Android/sdk GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p android-host testDebugUnitTest --tests '*AdaptiveAimSmoother*' --no-daemon --console=plain</automated>
  </verify>
  <acceptance_criteria>Tests cover all modes, fallback trigger, finite/clamped output, and monotonic timestamp behavior.</acceptance_criteria>
  <done>Smoothing behavior is deterministic and latency bounded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Map profile aim settings and provider overrides</name>
  <files>android-host/app/src/main/java/com/btgun/host/profile/ProfileMapper.kt, android-host/app/src/test/java/com/btgun/host/profile/ProfileMapperTest.kt</files>
  <read_first>
    - `android-host/app/src/main/java/com/btgun/host/model/NormalizedEvents.kt`
    - `android-host/app/src/main/java/com/btgun/host/motion/PreviewAimMapper.kt`
    - `android-host/app/src/main/java/com/btgun/host/profile/ProfileModels.kt`
    - `android-host/app/src/main/java/com/btgun/host/profile/AdaptiveAimSmoother.kt`
  </read_first>
  <behavior>
    - Calibrated/fused providers use calibrated `aimX/aimY` when available.
    - Gyro/raw override can use `rawAimX/rawAimY` when configured.
    - Tilt fallback provider uses the tilt fallback override when provider is `TILT_FALLBACK`.
    - Sensitivity, invert X/Y, and dead zone are applied in that order before smoothing.
    - `MotionProvider.UNAVAILABLE` maps aim to center with source `center`.
  </behavior>
  <action>Implement `ProfileMapper.map(profile, gunInputState, motionSample, nowElapsedNanos): MappedControllerState`. Select shared settings or provider override according to the motion provider/source. Apply dead zone as zeroing values whose absolute magnitude is below the setting. Apply sensitivity with clamp to `[-1, 1]`. Include `aimSource`, `providerName`, `smoothingMode`, `estimatedFilterLagMillis`, and `adaptiveFallback` in the mapped state.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=/Users/lucas.rancez/Library/Android/sdk GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p android-host testDebugUnitTest --tests '*ProfileMapper*' --tests '*AdaptiveAimSmoother*' --no-daemon --console=plain</automated>
  </verify>
  <acceptance_criteria>Mapper tests prove provider override selection, sensitivity/inversion/dead-zone math, smoothing status, unavailable center fallback, and clamped finite output.</acceptance_criteria>
  <done>Profile aim settings can produce mapped product aim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Map buttons and recenter without axis remap</name>
  <files>android-host/app/src/main/java/com/btgun/host/profile/ProfileMapper.kt, android-host/app/src/test/java/com/btgun/host/profile/ProfileMapperTest.kt</files>
  <read_first>
    - `android-host/app/src/main/java/com/btgun/host/model/NormalizedEvents.kt`
    - `android-host/app/src/main/java/com/btgun/host/recenter/ReloadHoldRecenter.kt`
    - `android-host/app/src/main/java/com/btgun/host/profile/ProfileValidation.kt`
  </read_first>
  <behavior>
    - Physical `trigger`, `reload`, `button_x`, `button_y`, `button_a`, `button_b` remap to virtual outputs only.
    - Stick `stickAxisX` and `stickAxisY` pass through unchanged.
    - Aim axes come only from aim mapping, not button mappings.
    - `recenterPhysicalControl` is exposed separately for HostSessionService and does not suppress normal virtual reload press/release.
  </behavior>
  <action>Extend `MappedControllerState` with `pressedVirtualControls`, `stickAxisX`, `stickAxisY`, and `recenterPhysicalControl`. Convert raw physical pressed controls to virtual output ids with no axis targets. Preserve unknown controls only in debug metadata if needed; never publish them as product buttons. Add helper `isRecenterPressed(rawGunInputState)` based on physical recenter control.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=/Users/lucas.rancez/Library/Android/sdk GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p android-host testDebugUnitTest --tests '*ProfileMapper*' --no-daemon --console=plain</automated>
  </verify>
  <acceptance_criteria>Tests prove remap, stick pass-through, no axis mapping, and recenter physical gesture independence from virtual reload output.</acceptance_criteria>
  <done>Button and recenter mapping rules are isolated in pure code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw input/motion -> mapped product state | Profile math can change user-visible trigger/aim behavior. |
| Timestamped motion -> smoothing | Filter state can add lag and hide target misses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering | `ProfileMapper` | mitigate | Unit tests cover every allowed physical/virtual mapping and forbid axis remap. |
| T-08-06 | Denial of Service | `AdaptiveAimSmoother` | mitigate | Finite/clamped output and fallback to Low when lag bound is exceeded. |
| T-08-SC | Tampering | Package installs | accept | No dependency install in this plan. |
</threat_model>

<verification>
Run profile mapper and adaptive smoother focused Android tests.
</verification>

<success_criteria>
PROF-02, PROF-03, and PROF-04 have pure tested mapping behavior before service or UI wiring.
</success_criteria>

<output>
Create `.planning/phases/08-desktop-profiles-and-mapping/08-03-SUMMARY.md` when done.
</output>
