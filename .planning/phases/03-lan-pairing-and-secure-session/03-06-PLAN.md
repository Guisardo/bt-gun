---
phase: "03-lan-pairing-and-secure-session"
plan: "06"
type: execute
wave: 5
depends_on:
  - "03-05"
files_modified:
  - "desktop-companion/build.gradle.kts"
  - "desktop-companion/src/main/kotlin/com/btgun/desktop/Main.kt"
  - "desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt"
  - "desktop-companion/src/main/kotlin/com/btgun/desktop/control/ControlServer.kt"
autonomous: true
requirements:
  - "TRAN-01"
  - "TRAN-03"
  - "TRAN-06"
must_haves:
  truths:
    - "Desktop user can start/restart pairing and see QR, endpoint, 6-digit code, and fingerprint suffix per D-01, D-02, and D-06."
    - "Desktop companion launch has no Windows/macOS HID assumptions."
    - "Desktop UI shows session states including pairing ready, authenticated, degraded, disconnected, expired, and rate limited."
    - "Desktop UI never shows QR secret, proof, private key, or full one-time token text."
  artifacts:
    - path: "desktop-companion/src/main/kotlin/com/btgun/desktop/Main.kt"
      provides: "Desktop companion launch entry"
    - path: "desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt"
      provides: "Desktop pairing/control window"
    - path: "desktop-companion/build.gradle.kts"
      provides: "Application entry for `gradle -p desktop-companion run`"
  key_links:
    - from: "desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt"
      to: "desktop-companion/src/main/kotlin/com/btgun/desktop/control/ControlServer.kt"
      via: "Pairing window starts/stops server with session state"
      pattern: "ControlServer"
---

<objective>
Wire the desktop companion launch and visible pairing/control surface.

Purpose: A desktop user must be able to start the portable companion, create/restart pairing, and see QR/manual/session state without entering platform driver scope.
Output: Runnable desktop companion entry point, Swing pairing window lifecycle wiring, and focused desktop tests.
</objective>

<execution_context>
@/Users/lucas.rancez/.codex/gsd-core/workflows/execute-plan.md
@/Users/lucas.rancez/.codex/gsd-core/templates/summary.md
@/Users/lucas.rancez/.codex/gsd-core/references/planner-mvp-mode.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-lan-pairing-and-secure-session/03-CONTEXT.md
@.planning/phases/03-lan-pairing-and-secure-session/03-PATTERNS.md
@.planning/phases/03-lan-pairing-and-secure-session/03-UI-SPEC.md
@desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt
@desktop-companion/src/main/kotlin/com/btgun/desktop/control/ControlServer.kt
</context>

## Phase Goal

**As a** user with the Android host app and desktop companion, **I want to** pair Android and desktop locally without manual IP entry, **so that** the devices establish a trusted authenticated session for later input streaming.

## Source Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | End-user desktop pairing slice | This plan wires desktop launch and visible pairing/control UI |
| REQ | TRAN-01 | Desktop launch and pairing UI exercised here after Plan 03-01 foundations |
| REQ | TRAN-03 | Authenticated and trust/security states surfaced in desktop UI |
| REQ | TRAN-06 | Control-channel state surfaced in desktop UI |
| CONTEXT | D-01, D-02, D-06, D-17 | Covered by task actions below |

## Execution Boundary Note

This plan owns desktop launch and desktop UI lifecycle only. Android QR scanner/manual fallback moves to Plan 03-07. Do not edit Android files here, and do not add virtual joystick, OS driver, UDP input stream, packet-loss metrics, visualizer latency, or phone haptic execution behavior.

<tasks>

<task type="auto">
  <name>Task 1: Wire desktop companion launch entry</name>
  <files>desktop-companion/build.gradle.kts, desktop-companion/src/main/kotlin/com/btgun/desktop/Main.kt</files>
  <read_first>
    @desktop-companion/build.gradle.kts
    @desktop-companion/src/main/kotlin/com/btgun/desktop/Main.kt
    @.planning/phases/03-lan-pairing-and-secure-session/03-UI-SPEC.md
  </read_first>
  <action>Wire `Main.kt` so `gradle -p desktop-companion run` opens `PairingWindow`, initializes `DesktopIdentityStore`, starts or injects `ControlServer`, and leaves no OS-specific Windows or macOS HID assumptions. Keep the application entry in `desktop-companion/build.gradle.kts` explicit. Follow `03-PATTERNS.md`: keep launch code thin and route state through the existing pairing/control classes instead of duplicating protocol logic.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/Cellar/openjdk@17/17.0.19/libexec/openjdk.jdk/Contents/Home GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p desktop-companion test --tests '*PairingSession*' --tests '*ControlChannel*'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `desktop-companion/build.gradle.kts` exposes the application entry point for `gradle -p desktop-companion run`.
    - Source assertion: `Main.kt` references `PairingWindow` and does not reference virtual joystick, HID backend, UDP input, packet loss, jitter, or visualizer latency.
  </acceptance_criteria>
  <done>Desktop companion launch entry is wired and desktop tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire desktop pairing window lifecycle and state rendering</name>
  <files>desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt, desktop-companion/src/main/kotlin/com/btgun/desktop/control/ControlServer.kt</files>
  <read_first>
    @desktop-companion/src/main/kotlin/com/btgun/desktop/ui/PairingWindow.kt
    @desktop-companion/src/main/kotlin/com/btgun/desktop/control/ControlServer.kt
    @desktop-companion/src/main/kotlin/com/btgun/desktop/pairing/PairingSessionRegistry.kt
    @.planning/phases/03-lan-pairing-and-secure-session/03-UI-SPEC.md
  </read_first>
  <action>Follow `03-PATTERNS.md`: keep `PairingWindow.kt` as a thin render surface like `MainActivity.kt`, keep `ControlServer.kt` on the Ktor WSS research path, and use `SecretRedactor` for diagnostic text. `PairingWindow` must keep `Start pairing` visible, change to `Restart pairing` after a session starts, render QR as the largest object at 240x240 or larger, show active LAN endpoint, expiry countdown, manual fallback endpoint/port/6-digit code/fingerprint suffix per D-01, D-02, and D-06, and render session states `idle`, `pairing ready`, `android connected`, `authenticated`, `degraded`, `disconnected`, `expired`, and `rate limited` per D-17. Do not show QR secret, proof, private key, or full one-time token text.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/Cellar/openjdk@17/17.0.19/libexec/openjdk.jdk/Contents/Home GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p desktop-companion test --tests '*PairingSession*' --tests '*ControlChannel*'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `PairingWindow.kt` renders endpoint, port, 6-digit code, fingerprint suffix, QR image, and expiry countdown.
    - Source assertion: desktop UI does not reference virtual joystick, HID backend, UDP input, packet loss, jitter, or visualizer latency.
    - Behavior assertion: desktop tests remain green after lifecycle wiring.
  </acceptance_criteria>
  <done>Desktop companion launch opens a pairing/control window with QR, visible manual fallback, and liveness state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Desktop UI to user | Pairing data is displayed and can expose secrets if rendered incorrectly. |
| Desktop UI to control server | UI starts/stops authenticated control service. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information Disclosure | `PairingWindow` | mitigate | Render endpoint/code/fingerprint suffix only; route diagnostics through `SecretRedactor`. |
| T-03-04 | Elevation of Privilege | desktop launch/control lifecycle | mitigate | Launch uses existing proof-gated `ControlServer`; no trusted handling bypass in UI. |
</threat_model>

<verification>
Focused:
- `JAVA_HOME=/opt/homebrew/Cellar/openjdk@17/17.0.19/libexec/openjdk.jdk/Contents/Home GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p desktop-companion test --tests '*PairingSession*' --tests '*ControlChannel*'`

Wave closeout:
- `JAVA_HOME=/opt/homebrew/Cellar/openjdk@17/17.0.19/libexec/openjdk.jdk/Contents/Home GRADLE_USER_HOME=/private/tmp/bt-gun-gradle-home gradle -p desktop-companion test`
</verification>

<success_criteria>
- Desktop companion can launch with `gradle -p desktop-companion run`.
- Desktop UI can start/restart pairing and show QR/manual fallback.
- Desktop UI shows authenticated/degraded/disconnected state.
- No platform driver, UDP input stream, or haptic execution scope is introduced.
</success_criteria>

<output>
Create `.planning/phases/03-lan-pairing-and-secure-session/03-06-SUMMARY.md` when done.
</output>
