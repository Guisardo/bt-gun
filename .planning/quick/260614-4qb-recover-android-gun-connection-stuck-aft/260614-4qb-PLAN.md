---
quick_id: 260614-4qb
slug: recover-android-gun-connection-stuck-aft
status: complete
created: 2026-06-14
---

# Quick Task 260614-4qb: Recover Android Gun Connection Stuck After App Closed Without Stop Session

## Goal

Recover the physical Android-to-gun session after the app closed without Stop session, and make the app less likely to leave the gun/BLE path wedged again.

## Tasks

1. Diagnose the live device state.
   - Files: Android device logs and service state.
   - Action: Inspect `com.btgun.host` process, foreground service, BLE scanner/GATT logs, and Bluetooth manager state.
   - Verify: Identify whether failure is app lifecycle, permission, Bluetooth scan, or GATT connection.
   - Done: Found stale foreground service first, then repeated GATT `status=133` while the gun was discoverable.

2. Patch app lifecycle/restart behavior.
   - Files: `android-host/app/src/main/java/com/btgun/host/HostSessionService.kt`.
   - Action: Add foreground notification Stop session action, task-removal cleanup, duplicate Start guard, failed-foreground restart path, and non-crashing motion sensor registration failure handling.
   - Verify: Unit tests and hardware retest.
   - Done: App no longer crashes on repeated Start, and users can stop an active session from the notification.

3. Add focused regression coverage.
   - Files: `android-host/app/src/test/java/com/btgun/host/HostSessionServiceLivenessTest.kt`.
   - Action: Cover notification action spec, task removal cleanup, duplicate Start guard, and failed-session restart predicates.
   - Verify: Focused liveness test and full Android host unit test suite.
   - Done: Tests passed and APK installed.

## Verification

- `git diff --check`
- `gradle -p android-host testDebugUnitTest --tests '*HostSessionServiceLivenessTest*' --no-daemon --console=plain`
- `gradle -p android-host testDebugUnitTest --no-daemon --console=plain`
- `gradle -p android-host :app:assembleDebug --no-daemon --console=plain`
- `adb install -r android-host/app/build/outputs/apk/debug/app-debug.apk`
- Physical retest: after phone reboot, gun GATT connection changed from `status=133` failures to `status=0`, ACL connected, service discovery, and `fff3` notification setup.
